---
phase: 21-nyquist-validation-sign-off
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/16-integration-fix-and-doc-hygiene/16-VALIDATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "16-VALIDATION.md frontmatter reads nyquist_compliant: true, wave_0_complete: true, status: approved"
    - "All 4 Per-Task Verification Map rows show status: green"
    - "All 6 Validation Sign-Off checklist items are checked"
  artifacts:
    - path: ".planning/phases/16-integration-fix-and-doc-hygiene/16-VALIDATION.md"
      provides: "Phase 16 Nyquist sign-off record"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/16-integration-fix-and-doc-hygiene/16-VALIDATION.md"
      to: "tests/test_context_and_vig_completion.py"
      via: "Per-Task row 16-01-01 cites these 2 passing tests"
      pattern: "test_fetch_player_props"
---

<objective>
Achieve nyquist_compliant: true for Phase 16 (Integration Fix & Documentation Hygiene) by verifying the two automated tests pass and then performing an atomic sweep of the VALIDATION.md file: frontmatter upgrade, Per-Task Map status column, Wave 0 section, and Validation Sign-Off block.

Purpose: Phase 16's VALIDATION.md was written during planning before tests were finalized. The test (`test_fetch_player_props_called_with_nba_when_sport_is_nba` and `test_fetch_player_props_called_with_nfl_when_sport_absent`) already passes. The documentation debt must be cleared to make the compliance record accurate.
Output: 16-VALIDATION.md with all fields set to approved/true/green state.
</objective>

<execution_context>
@C:/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-nyquist-validation-sign-off/21-RESEARCH.md
@.planning/phases/16-integration-fix-and-doc-hygiene/16-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 16 tests pass and update 16-VALIDATION.md</name>
  <files>.planning/phases/16-integration-fix-and-doc-hygiene/16-VALIDATION.md</files>
  <action>
Step 1 — Run the two automated verification commands from the research to confirm pre-conditions:

```
python -m pytest tests/test_context_and_vig_completion.py -k "test_fetch_player_props" -x -q
```
Expected: 2 passed.

```
grep -c 'fetch_player_props(sport)' src/sportsbet/graph/agents.py
```
Expected: 1 (confirms the dynamic routing fix is in place).

```
grep -c 'PROP-04' src/sportsbet/graph/graph.py
```
Expected: >= 1 (confirms the PROP-04 comment is in graph.py).

Step 2 — Only if all three checks pass, open `.planning/phases/16-integration-fix-and-doc-hygiene/16-VALIDATION.md` and make the following targeted changes as a single atomic edit:

**Frontmatter** (lines 1-8): change three fields and add one:
- `status: draft` → `status: approved`
- `nyquist_compliant: false` → `nyquist_compliant: true`
- `wave_0_complete: false` → `wave_0_complete: true`
- Add new line: `updated: 2026-03-26`

**Wave 0 Requirements section**: The existing text says "Existing infrastructure covers all phase requirements." Add two specific test names below it as a brief checklist so the sign-off is self-evidently verifiable:
```
- [x] `tests/test_context_and_vig_completion.py::test_fetch_player_props_called_with_nba_when_sport_is_nba` — PASSING
- [x] `tests/test_context_and_vig_completion.py::test_fetch_player_props_called_with_nfl_when_sport_absent` — PASSING
```

**Per-Task Verification Map**: Update the Status column for all 4 rows from `⬜ pending` to `✅ green`:
- Row 16-01-01 (unit test): `⬜ pending` → `✅ green`
- Row 16-01-02 (manual - graph.py comment): `⬜ pending` → `✅ green`
- Row 16-01-03 (manual - REQUIREMENTS.md): `⬜ pending` → `✅ green`
- Row 16-01-04 (manual - SUMMARY files): `⬜ pending` → `✅ green`

**Validation Sign-Off block**: Change all 6 `- [ ]` items to `- [x]`.

**Approval line**: Change `**Approval:** pending` to:
`**Approval:** Self-approved 2026-03-26 — Phase 16 behaviors verified: fetch_player_props(sport) dynamic routing test passes (2 green); PROP-04 comment in graph.py confirmed; documentation sweep confirmed complete.`

Do NOT change any other lines. Do NOT change the test command strings in the Per-Task Map (they are historical artifacts per the RESEARCH.md anti-pattern note).
  </action>
  <verify>
    <automated>grep "nyquist_compliant: true" .planning/phases/16-integration-fix-and-doc-hygiene/16-VALIDATION.md && grep "status: approved" .planning/phases/16-integration-fix-and-doc-hygiene/16-VALIDATION.md && grep "wave_0_complete: true" .planning/phases/16-integration-fix-and-doc-hygiene/16-VALIDATION.md</automated>
  </verify>
  <done>16-VALIDATION.md frontmatter shows nyquist_compliant: true, wave_0_complete: true, status: approved. All 4 Per-Task rows show green. Wave 0 section has 2 checked items. All 6 Sign-Off items are checked. Approval line cites 2026-03-26 with specific behavioral evidence.</done>
</task>

</tasks>

<verification>
After task completion:
1. `grep "nyquist_compliant: true" .planning/phases/16-integration-fix-and-doc-hygiene/16-VALIDATION.md` — must return a match
2. `grep -c "✅ green" .planning/phases/16-integration-fix-and-doc-hygiene/16-VALIDATION.md` — must return 4 (all Per-Task rows)
3. `grep -c "\- \[x\]" .planning/phases/16-integration-fix-and-doc-hygiene/16-VALIDATION.md` — must return >= 8 (Wave 0 items + Sign-Off items)
4. Full suite must stay green: `python -m pytest tests/ -x -q` → 201 passed, 11 skipped, 2 xfailed
</verification>

<success_criteria>
- Phase 16 VALIDATION.md has nyquist_compliant: true, wave_0_complete: true, status: approved in frontmatter
- All 4 Per-Task Verification Map status cells show "✅ green"
- Wave 0 Requirements section has 2 checked test items
- All 6 Validation Sign-Off items are checked
- Full pytest suite remains 201 passed, 11 skipped, 2 xfailed (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/21-nyquist-validation-sign-off/21-01-SUMMARY.md`
</output>
