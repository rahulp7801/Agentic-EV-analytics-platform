---
phase: 21-nyquist-validation-sign-off
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-situational-game-log-prop-queries/18-VALIDATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "18-VALIDATION.md frontmatter reads nyquist_compliant: true, wave_0_complete: true, status: approved"
    - "All 7 Wave 0 checklist items are checked (including xfail items)"
    - "All 7 Per-Task Verification Map rows show status: green"
    - "All 6 Validation Sign-Off checklist items are checked"
  artifacts:
    - path: ".planning/phases/18-situational-game-log-prop-queries/18-VALIDATION.md"
      provides: "Phase 18 Nyquist sign-off record"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/18-situational-game-log-prop-queries/18-VALIDATION.md"
      to: "tests/test_nba_gamelogs_schema.py"
      via: "Per-Task row 18-01-01 — 3 tests PASSING"
      pattern: "test_nba_gamelogs_schema"
    - from: ".planning/phases/18-situational-game-log-prop-queries/18-VALIDATION.md"
      to: "tests/test_gamelog_injury_join.py"
      via: "Per-Task row 18-01-03 — 2 tests XFAIL (acceptable per Phase 17 precedent)"
      pattern: "xfail"
---

<objective>
Achieve nyquist_compliant: true for Phase 18 (Situational Game-Log Prop Queries) by verifying all 7 wave-0 test files pass or xfail, then performing an atomic sweep of the VALIDATION.md file: frontmatter upgrade, Wave 0 checklist, Per-Task Map status column, and Validation Sign-Off block.

Purpose: Phase 18's VALIDATION.md was written during planning with all wave-0 items marked as missing gaps (❌ W0). All 7 test files were created during Phase 18 execution and are now present and passing (25 passing + 2 xfailing). The documentation debt must be cleared.
Output: 18-VALIDATION.md with all fields set to approved/true/green state. xfail tests marked as acceptable per Phase 17 precedent.
</objective>

<execution_context>
@C:/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-nyquist-validation-sign-off/21-RESEARCH.md
@.planning/phases/18-situational-game-log-prop-queries/18-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 18 test suite and update 18-VALIDATION.md</name>
  <files>.planning/phases/18-situational-game-log-prop-queries/18-VALIDATION.md</files>
  <action>
Step 1 — Run the Phase 18 wave-0 batch verify command from the research:

```
python -m pytest tests/test_nba_gamelogs_schema.py tests/test_nba_gamelogs_ingest.py tests/test_gamelog_injury_join.py tests/test_prop_params_situational.py tests/test_context_agent_params.py tests/test_prop_query_builder_situational.py tests/test_executor_small_sample.py -v --tb=short
```
Expected: 25 passed, 2 xfailed. Note: xfail is acceptable per the Phase 17 precedent established in STATE.md — "Retroactive Nyquist sign-off valid when tests existed and passed before VALIDATION.md updated".

Step 2 — Only if the batch verify matches the expected outcome (25 passed, 2 xfailed), open `.planning/phases/18-situational-game-log-prop-queries/18-VALIDATION.md` and make the following targeted changes as a single atomic edit:

**Frontmatter** (lines 1-8): change three fields and add one:
- `status: draft` → `status: approved`
- `nyquist_compliant: false` → `nyquist_compliant: true`
- `wave_0_complete: false` → `wave_0_complete: true`
- Add new line: `updated: 2026-03-26`

**Wave 0 Requirements section**: Change all 7 `- [ ]` items to `- [x]`. The `test_gamelog_injury_join.py` item should include a note:
```
- [x] `tests/test_gamelog_injury_join.py` — stubs for SC-2 (injury-join for NBA and NFL game_id) — XFAIL acceptable; SC-2 production behavior covered by GREEN test in test_prop_query_builder_situational.py
```
The other 6 items just need `[ ]` → `[x]`.

**Per-Task Verification Map**: Update the Status column and File Exists column for all 7 rows:
- File Exists for all rows: `❌ W0` → `✅` (all 7 files now exist)
- Status for all rows: `⬜ pending` → `✅ green`
- For row 18-01-03 (test_gamelog_injury_join.py): Status → `✅ green` with a note appended in the Status cell: `✅ green (2 xfail — SC-2 covered by test_prop_query_builder_situational.py GREEN)`

**Validation Sign-Off block**: Change all 6 `- [ ]` items to `- [x]`.

**Approval line**: Change `**Approval:** pending` to:
`**Approval:** Self-approved 2026-03-26 — all 27 Phase 18 tests GREEN or XFAIL (25 passed, 2 xfailed); test_gamelog_injury_join.py xfail stubs acceptable per Phase 17 precedent; full suite 201 passed, 11 skipped-DB, 2 xfailed.`

Do NOT change any other lines. Do NOT change the test command strings in the Per-Task Map (they are historical artifacts per RESEARCH.md anti-pattern note).
  </action>
  <verify>
    <automated>grep "nyquist_compliant: true" .planning/phases/18-situational-game-log-prop-queries/18-VALIDATION.md && grep "status: approved" .planning/phases/18-situational-game-log-prop-queries/18-VALIDATION.md && grep "wave_0_complete: true" .planning/phases/18-situational-game-log-prop-queries/18-VALIDATION.md</automated>
  </verify>
  <done>18-VALIDATION.md frontmatter shows nyquist_compliant: true, wave_0_complete: true, status: approved. All 7 Wave 0 items checked (xfail item annotated). All 7 Per-Task rows show green (xfail row annotated). All 6 Sign-Off items checked. Approval line cites 2026-03-26 with test count evidence.</done>
</task>

</tasks>

<verification>
After task completion:
1. `grep "nyquist_compliant: true" .planning/phases/18-situational-game-log-prop-queries/18-VALIDATION.md` — must return a match
2. `grep -c "✅ green" .planning/phases/18-situational-game-log-prop-queries/18-VALIDATION.md` — must return >= 7 (all Per-Task rows)
3. `grep -c "\- \[x\]" .planning/phases/18-situational-game-log-prop-queries/18-VALIDATION.md` — must return >= 13 (7 Wave 0 items + 6 Sign-Off items)
4. Full suite must stay green: `python -m pytest tests/ -x -q` → 201 passed, 11 skipped, 2 xfailed
</verification>

<success_criteria>
- Phase 18 VALIDATION.md has nyquist_compliant: true, wave_0_complete: true, status: approved in frontmatter
- All 7 Wave 0 checklist items are checked (xfail item annotated with SC-2 cross-reference)
- All 7 Per-Task Verification Map status cells show "✅ green"
- All 6 Validation Sign-Off items are checked
- Full pytest suite remains 201 passed, 11 skipped, 2 xfailed (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/21-nyquist-validation-sign-off/21-02-SUMMARY.md`
</output>
