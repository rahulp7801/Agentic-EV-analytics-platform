---
phase: 16-integration-fix-and-doc-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sportsbet/graph/agents.py
  - src/sportsbet/graph/graph.py
  - tests/test_context_and_vig_completion.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/05-arbitrage-kelly-and-risk-controls/05-02-SUMMARY.md
  - .planning/phases/10-player-prop-and-nba-data-layer/10-01-SUMMARY.md
  - .planning/phases/13-player-prop-arbitrage-and-pipeline-wiring/13-01-SUMMARY.md
  - .planning/phases/14-prop-integration-gap-closure/14-01-SUMMARY.md
  - .planning/phases/15-context-and-vig-completion/15-01-SUMMARY.md
autonomous: true
requirements: [PROP-01, CTXT-04, PROP-04]

must_haves:
  truths:
    - "fetch_player_props(sport) is called with the dynamic sport variable — the string literal 'nfl' at agents.py line 246 is replaced with the in-scope sport variable"
    - "A test asserts fetch_player_props is called with 'nba' (not 'nfl') when GraphState has sport='nba'"
    - "graph.py contains a PROP-04 two-invocation checkpoint comment after the kinematic_agent edge at line 275"
    - "REQUIREMENTS.md has all checkboxes set to [x] with no [ ] entries remaining; traceability rows DATA-01, DATA-03, QUANT-01, QUANT-03, CTXT-02 status changed from Pending to Complete; stale footnote updated"
    - "ROADMAP.md has 01-03-PLAN.md and all Phase 2 plan sub-items set to [x]"
    - "Five flagged SUMMARY files have requirements-completed field; 14-01-SUMMARY.md renames requirements_closed to requirements-completed"
  artifacts:
    - path: "src/sportsbet/graph/agents.py"
      provides: "GAP-INT-1 fix: fetch_player_props(sport) dynamic routing"
      contains: "fetch_player_props(sport)"
    - path: "src/sportsbet/graph/graph.py"
      provides: "GAP-INT-2: PROP-04 two-invocation comment"
      contains: "PROP-04 kinematic boost: two-invocation checkpoint pattern"
    - path: "tests/test_context_and_vig_completion.py"
      provides: "Test asserting NBA sport path calls fetch_player_props with 'nba'"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All v1 requirement checkboxes correct; traceability Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 plan sub-items and 01-03-PLAN.md checked"
    - path: ".planning/phases/15-context-and-vig-completion/15-01-SUMMARY.md"
      provides: "requirements-completed: [QUANT-02, CTXT-01, CTXT-04]"
  key_links:
    - from: "src/sportsbet/graph/agents.py line 246"
      to: "sport variable (line 187)"
      via: "variable reference"
      pattern: "fetch_player_props\\(sport\\)"
    - from: "src/sportsbet/graph/graph.py"
      to: "PROP-04 documentation"
      via: "inline comment after line 275"
      pattern: "PROP-04 kinematic boost"
---

<objective>
Close two v1.0 audit integration findings and repair all stale documentation debt across
REQUIREMENTS.md, ROADMAP.md, and five SUMMARY files.

Purpose: GAP-INT-1 causes NBA prop snapshots to be silently ingested as NFL props when
sport="nba" is set in GraphState. GAP-INT-2 leaves the PROP-04 kinematic two-invocation
pattern undocumented, making the checkpoint pattern invisible to API consumers. The
documentation debt causes stale traceability signals that misrepresent v1 completion status.

Output:
- agents.py: one-line fix + updated Step 1c comment
- graph.py: PROP-04 two-invocation comment block after kinematic edge
- test_context_and_vig_completion.py: NBA sport path assertion
- REQUIREMENTS.md: all [x] checkboxes, Complete traceability
- ROADMAP.md: Phase 2 plan sub-items and 01-03 checked
- 5 SUMMARY files: requirements-completed field added/renamed
</objective>

<execution_context>
@C:/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-integration-fix-and-doc-hygiene/16-RESEARCH.md
@.planning/phases/16-integration-fix-and-doc-hygiene/16-VALIDATION.md

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From src/sportsbet/graph/agents.py (lines 184-246):
```python
# Line 187 — sport variable already in scope at line 246:
sport = state.get("sport") or "nfl"  # type: ignore[attr-defined]

# Line 240-241 — Step 1c comment (stale, needs update):
# --- Step 1c: Fetch and persist NFL player prop snapshots (PROP-01) ---
# Scoped to NFL only in Phase 14; NBA prop ingestion is Phase 15 territory.

# Line 246 — BUG (hardcoded "nfl"):
raw_props = await poller.fetch_player_props("nfl")
```

From src/sportsbet/graph/graph.py (lines 274-275):
```python
# kinematic_agent always terminates at END (independent pipeline)
builder.add_edge("kinematic_agent", END)
# <- PROP-04 two-invocation comment block goes HERE (after line 275)
```

SUMMARY frontmatter key convention (confirmed from 01-02, 03-02, 03-03):
```yaml
requirements-completed: [REQ-ID, ...]   # hyphen, not underscore
```

SUMMARY files requiring additions (key: current state):
- 05-02-SUMMARY.md: no requirements-completed field
- 10-01-SUMMARY.md: no requirements-completed field
- 13-01-SUMMARY.md: no requirements-completed field
- 14-01-SUMMARY.md: has requirements_closed (wrong key) = [PROP-01, PROP-06, INFRA-01]
- 15-01-SUMMARY.md: no requirements-completed field

Correct values:
- 05-02-SUMMARY.md: requirements-completed: []  (ARBT-03/ARBT-04 wired in 05-03, not here)
- 10-01-SUMMARY.md: requirements-completed: [PROP-01, PROP-02]
- 13-01-SUMMARY.md: requirements-completed: [PROP-06, PROP-07]
- 14-01-SUMMARY.md: rename requirements_closed -> requirements-completed, keep [PROP-01, PROP-06, INFRA-01]
- 15-01-SUMMARY.md: requirements-completed: [QUANT-02, CTXT-01, CTXT-04]

REQUIREMENTS.md stale entries (lines 99-143):
- DATA-01 traceability: "Pending" -> "Complete"
- DATA-03 checkbox: [ ] -> [x]; traceability: "Pending" -> "Complete"
- QUANT-01 checkbox: [ ] -> [x]; traceability: "Pending" -> "Complete"
- QUANT-03 checkbox: [ ] -> [x]; traceability: "Pending" -> "Complete"
- CTXT-02 checkbox: [ ] -> [x]; traceability: "Pending" -> "Complete"
- Line 138 stale footnote: "Pending (gap closure phases 14-15): 5 (PROP-01, PROP-06, NBA-02, QUANT-02, CTXT-04)" -> remove or update to reflect all closed

ROADMAP.md stale entries:
- Line 49: "- [ ] 01-03-PLAN.md" -> "- [x] 01-03-PLAN.md"
- Lines 63-65: All Phase 2 plan sub-items "[ ]" -> "[x]"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix GAP-INT-1 — dynamic sport routing in fetch_player_props</name>
  <files>src/sportsbet/graph/agents.py, tests/test_context_and_vig_completion.py</files>
  <behavior>
    - Test: when make_context_agent closure is invoked with state["sport"] = "nba", fetch_player_props is called with argument "nba" (not "nfl")
    - Test: when make_context_agent closure is invoked with state["sport"] = "nfl" (or absent), fetch_player_props is called with argument "nfl"
  </behavior>
  <action>
In src/sportsbet/graph/agents.py:

1. At line 246, change:
   ```python
   raw_props = await poller.fetch_player_props("nfl")
   ```
   to:
   ```python
   raw_props = await poller.fetch_player_props(sport)
   ```
   The `sport` variable is already assigned at line 187 as `sport = state.get("sport") or "nfl"`.

2. Update the Step 1c comment block (lines 240-242) to remove the stale "NFL only" scope note:
   ```python
   # --- Step 1c: Fetch and persist player prop snapshots (PROP-01, sport-routed) ---
   # sport variable resolved at line 187; "nba" routes NBA prop markets, "nfl" routes NFL markets.
   # Sync engine reuses _sync_engine_cache initialized in Step 1b.
   ```

In tests/test_context_and_vig_completion.py:

Add a test (or extend an existing NBA test) that:
- Patches `fetch_player_props` on the OddsAPIPoller
- Invokes the context agent closure with `state = {..., "sport": "nba"}`
- Asserts `mock_fetch_player_props.call_args == call("nba")`

Use the existing mock pattern in this test file — find where `fetch_player_props` is already mocked and add the assertion. Do NOT restructure unrelated tests.

Run tests after each change:
`python -m pytest tests/test_context_and_vig_completion.py tests/test_prop_integration_gap_closure.py tests/test_context.py -x -q`

Ensure all existing tests remain green — this is an additive fix to an in-scope variable; it does not change the NFL path behavior.
  </action>
  <verify>
    <automated>python -m pytest tests/test_context_and_vig_completion.py tests/test_prop_integration_gap_closure.py tests/test_context.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `grep -n 'fetch_player_props("nfl")' src/sportsbet/graph/agents.py` returns 0 matches
    - `grep -n 'fetch_player_props(sport)' src/sportsbet/graph/agents.py` returns 1 match at the former line 246
    - All existing tests green; new NBA sport path assertion passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix GAP-INT-2 — add PROP-04 two-invocation comment to graph.py</name>
  <files>src/sportsbet/graph/graph.py</files>
  <action>
In src/sportsbet/graph/graph.py, after the kinematic_agent END edge (line 275), add the following comment block. Insert it immediately after `builder.add_edge("kinematic_agent", END)` with one blank line of separation:

```python
    # PROP-04 kinematic boost: two-invocation checkpoint pattern (GAP-INT-2).
    # To incorporate kinematic separation/press-man signals into NFL receiving prop estimates:
    #   Invocation 1: ainvoke({"request_type": "kinematic_analysis", ...},
    #                          config={"configurable": {"thread_id": tid}})
    #                 -> kinematic_agent writes KinematicAnalysis to state["kinematic_result"]
    #                 -> AsyncSqliteSaver persists it in the checkpoint under thread_id
    #   Invocation 2: ainvoke({"request_type": "prop_analysis", ...},
    #                          config={"configurable": {"thread_id": tid}})
    #                 -> prop_quant_agent reads state.get("kinematic_result") from checkpoint
    #                 -> _apply_kinematic_adjustment() boosts probability if RECEIVING_PROPS match
    # Both invocations MUST use the same thread_id. Single-invocation path not supported.
```

No other changes to graph.py. Run `python -m pytest -x -q` to confirm no regressions.
  </action>
  <verify>
    <automated>python -m pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `grep -c "PROP-04 kinematic boost" src/sportsbet/graph/graph.py` returns 1
    - Full test suite remains green
  </done>
</task>

<task type="auto">
  <name>Task 3: Documentation sweep — REQUIREMENTS.md, ROADMAP.md, and 5 SUMMARY files</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/phases/05-arbitrage-kelly-and-risk-controls/05-02-SUMMARY.md,
    .planning/phases/10-player-prop-and-nba-data-layer/10-01-SUMMARY.md,
    .planning/phases/13-player-prop-arbitrage-and-pipeline-wiring/13-01-SUMMARY.md,
    .planning/phases/14-prop-integration-gap-closure/14-01-SUMMARY.md,
    .planning/phases/15-context-and-vig-completion/15-01-SUMMARY.md
  </files>
  <action>
IMPORTANT: Read each file before editing. Use the Edit tool for targeted changes — do not rewrite entire files.

**REQUIREMENTS.md changes:**

1. Line for DATA-01 in requirements section: the `[x]` checkbox is already correct. Only the traceability table row needs "Pending" -> "Complete".

2. DATA-03 requirement entry: change `- [ ] **DATA-03**` to `- [x] **DATA-03**`
   DATA-03 traceability row: "Pending" -> "Complete"

3. QUANT-01 requirement entry: change `- [ ] **QUANT-01**` to `- [x] **QUANT-01**`
   QUANT-01 traceability row: "Pending" -> "Complete"

4. QUANT-03 requirement entry: change `- [ ] **QUANT-03**` to `- [x] **QUANT-03**`
   QUANT-03 traceability row: "Pending" -> "Complete"

5. CTXT-02 requirement entry: change `- [ ] **CTXT-02**` to `- [x] **CTXT-02**`
   CTXT-02 traceability row: "Pending" -> "Complete"

6. DATA-01 traceability row: "Pending" -> "Complete"

7. The stale footnote at the bottom of the traceability section:
   "Pending (gap closure phases 14–15): 5 (PROP-01, PROP-06, NBA-02, QUANT-02, CTXT-04)"
   Replace with: "All v1 requirements satisfied — closed by Phase 16 (2026-03-24)"

   Also update the last-updated line: "Last updated: 2026-03-24 — all v1 requirements closed by Phase 16"

**ROADMAP.md changes:**

1. Find `- [ ] 01-03-PLAN.md` (under Phase 1 Plans section) and change to `- [x] 01-03-PLAN.md`

2. Find the three Phase 2 plan sub-items (under ### Phase 2):
   - `- [ ] 02-01-PLAN.md` -> `- [x] 02-01-PLAN.md`
   - `- [ ] 02-02-PLAN.md` -> `- [x] 02-02-PLAN.md`
   - `- [ ] 02-03-PLAN.md` -> `- [x] 02-03-PLAN.md`

**SUMMARY files — add requirements-completed field (hyphen convention):**

For each file, add the field into the YAML frontmatter block (between `---` delimiters), after the existing fields. Do not alter any other content.

05-02-SUMMARY.md: Add after the existing frontmatter fields:
```yaml
requirements-completed: []
```
(ARBT-03 and ARBT-04 are wired in 05-03-SUMMARY.md, not completed in 05-02 alone)

10-01-SUMMARY.md: Add:
```yaml
requirements-completed: [PROP-01, PROP-02]
```

13-01-SUMMARY.md: Add:
```yaml
requirements-completed: [PROP-06, PROP-07]
```

14-01-SUMMARY.md: Find `requirements_closed: [PROP-01, PROP-06, INFRA-01]` and replace with:
```yaml
requirements-completed: [PROP-01, PROP-06, INFRA-01]
```

15-01-SUMMARY.md: Add:
```yaml
requirements-completed: [QUANT-02, CTXT-01, CTXT-04]
```

After all edits, verify with grep commands:
```bash
grep -c '^\- \[ \]' .planning/REQUIREMENTS.md   # must return 0
grep -c '\[ \].*-PLAN\.md' .planning/ROADMAP.md  # check all plan items
grep 'requirements-completed\|requirements_closed' .planning/phases/*/??-??-SUMMARY.md | grep 'requirements_closed'  # must return 0
```
  </action>
  <verify>
    <automated>grep -c '^\- \[ \]' .planning/REQUIREMENTS.md && echo "REQS_OPEN_COUNT_ABOVE (must be 0)"</automated>
  </verify>
  <done>
    - `grep -c '^\- \[ \]' .planning/REQUIREMENTS.md` returns 0
    - `grep '01-03-PLAN\|02-0[123]-PLAN' .planning/ROADMAP.md` shows all [x]
    - `grep 'requirements_closed' .planning/phases/14-prop-integration-gap-closure/14-01-SUMMARY.md` returns 0 (renamed)
    - All 5 SUMMARY files have `requirements-completed:` field in frontmatter
  </done>
</task>

</tasks>

<verification>
After all tasks complete, run the full verification sequence:

1. Code fix verification:
   ```
   grep 'fetch_player_props("nfl")' src/sportsbet/graph/agents.py
   ```
   Must return 0 matches.

2. PROP-04 comment verification:
   ```
   grep 'PROP-04 kinematic boost' src/sportsbet/graph/graph.py
   ```
   Must return 1 match.

3. Requirements documentation:
   ```
   grep -c '^\- \[ \]' .planning/REQUIREMENTS.md
   ```
   Must return 0.

4. ROADMAP plan checkboxes:
   ```
   grep '\[ \].*-PLAN\.md' .planning/ROADMAP.md | head -20
   ```
   Phase 1 plan 03 and all Phase 2 plan items must not appear in output.

5. SUMMARY key name consistency:
   ```
   grep 'requirements_closed' .planning/phases/14-prop-integration-gap-closure/14-01-SUMMARY.md
   ```
   Must return 0 matches.

6. Full test suite:
   ```
   python -m pytest -x -q
   ```
   Must be green.
</verification>

<success_criteria>
- `fetch_player_props(sport)` at agents.py line 246 uses the dynamic variable — no string literal `"nfl"` at that callsite
- Test suite includes assertion that NBA sport state routes `fetch_player_props("nba")`; all existing tests remain green
- graph.py PROP-04 comment block documents the two-invocation checkpoint pattern with thread_id requirement
- REQUIREMENTS.md: zero `[ ]` checkboxes; DATA-01, DATA-03, QUANT-01, QUANT-03, CTXT-02 traceability rows show "Complete"
- ROADMAP.md: 01-03-PLAN.md and all three 02-0x-PLAN.md items show [x]
- All 5 flagged SUMMARY files have `requirements-completed:` field with correct requirement IDs
- `python -m pytest -x -q` passes with no failures
</success_criteria>

<output>
After completion, create `.planning/phases/16-integration-fix-and-doc-hygiene/16-01-SUMMARY.md`
</output>
