---
phase: 02-agent-infrastructure
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/sportsbet/graph/state.py
  - src/sportsbet/graph/router.py
  - src/sportsbet/graph/agents.py
  - src/sportsbet/graph/graph.py
  - src/sportsbet/graph/__init__.py
  - tests/test_graph.py
  - pyproject.toml
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "GraphState TypedDict has all required fields (session_id, request_type, game context, error, quant_result, ev_signal) with Annotated reducers on multi-writer fields"
    - "Master Router dispatches quant_analysis to quant_agent, odds_check to arbitrage_agent, context_update to context_agent via conditional edges"
    - "Master Router routes to END when error field is set — no further node execution"
    - "Stub agents return static fixture instances of their typed output models (QuantResult, EVSignal) stored on GraphState so Phase 3 can develop against a real interface contract immediately"
    - "Compiled StateGraph invokes end-to-end without exception using ainvoke with a test payload"
    - "Concurrent writes to Annotated reducer fields do not produce silent data loss"
  artifacts:
    - path: src/sportsbet/graph/state.py
      provides: "GraphState TypedDict with Annotated reducers"
      exports:
        - GraphState
    - path: src/sportsbet/graph/router.py
      provides: "master_router node function and route_from_master conditional edge function"
      exports:
        - master_router
        - route_from_master
    - path: src/sportsbet/graph/agents.py
      provides: "quant_agent, arbitrage_agent, context_agent stub nodes returning hardcoded fixture instances of QuantResult and EVSignal on GraphState"
      exports:
        - quant_agent
        - arbitrage_agent
        - context_agent
    - path: src/sportsbet/graph/graph.py
      provides: "create_graph() factory returning compiled CompiledStateGraph"
      exports:
        - create_graph
    - path: tests/test_graph.py
      provides: "GraphState reducer tests, router dispatch tests"
  key_links:
    - from: src/sportsbet/graph/router.py
      to: src/sportsbet/graph/state.py
      via: "reads state['request_type'] and state['error'] to determine routing"
      pattern: "state\\[.request_type.\\]"
    - from: src/sportsbet/graph/agents.py
      to: src/sportsbet/graph/models.py
      via: "stub agents return static instances of QuantResult, EVSignal stored in state['quant_result'] / state['ev_signal']"
      pattern: "from sportsbet\\.graph\\.models import"
    - from: src/sportsbet/graph/graph.py
      to: src/sportsbet/graph/router.py
      via: "add_conditional_edges(START_NODE, route_from_master, {...})"
      pattern: "add_conditional_edges"
---

<objective>
Build the LangGraph graph skeleton: GraphState TypedDict with Annotated reducers, Master Router with conditional edges to 3 specialist stub agents, and a compiled StateGraph that can be invoked end-to-end.

Purpose: All downstream phases (3, 4, 5, 6) will import `create_graph()` and replace stub agents with real implementations. This plan establishes the wiring and routing contracts — stub agents return hardcoded fixture instances of QuantResult and EVSignal so Phase 3 can develop against a real interface contract immediately (locked CONTEXT.md decision).
Output: Compiled `StateGraph` that routes to the correct stub node based on `request_type` and terminates cleanly, with fixture model instances on GraphState after each invocation.
</objective>

<execution_context>
@C:/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-agent-infrastructure/02-CONTEXT.md
@.planning/phases/02-agent-infrastructure/02-01-SUMMARY.md

<interfaces>
<!-- Phase 1 connection layer — agents will use create_async_pool() for DB queries. -->

From src/sportsbet/db/connection.py:
```python
async def create_async_pool(min_size: int = 2, max_size: int = 10) -> asyncpg.Pool: ...
```

<!-- Models defined in Plan 02-01 — stub agents return static instances of these stored on GraphState. -->

From src/sportsbet/graph/models.py (created in Plan 02-01):
```python
class QuantResult(BaseModel):
    model_config = ConfigDict(strict=True)
    true_probability: Decimal | None = None
    sample_size: int | None = None
    confidence_interval: tuple[Decimal, Decimal] | None = None
    data_source: str | None = None

class EVSignal(BaseModel):
    model_config = ConfigDict(strict=True)
    ev_percentage: Decimal          # > 0
    true_probability: Decimal
    implied_probability: Decimal
    kelly_fraction: Decimal         # in (0, 0.25]
    trade_plan: list[str]           # max 3 items
    market_type: str

class GameState(BaseModel):
    model_config = ConfigDict(strict=True)
    game_id: str
    season: int
    week: int
    home_team: str
    away_team: str
    injury_flags: dict[str, str]
    weather_json: dict | None
```

<!-- LangGraph 0.2.x / 0.3.x API — use these exact patterns. -->
<!-- StateGraph constructor: StateGraph(GraphState) -->
<!-- Conditional edges: graph.add_conditional_edges("master_router", route_fn, {"quant": "quant_agent", ...}) -->
<!-- Compile: graph.compile() returns CompiledStateGraph -->
<!-- Invocation: await compiled.ainvoke(initial_state, config={"configurable": {"thread_id": "..."}}) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GraphState TypedDict and test stubs</name>
  <files>src/sportsbet/graph/state.py, tests/test_graph.py</files>
  <behavior>
    - test_graphstate_reducer: create two partial state dicts both setting the `error` field, apply the reducer, confirm later write wins without silent drop
    - test_graphstate_has_required_fields: GraphState TypedDict has session_id, request_type, created_at, game_id, season, week, home_team, away_team, injury_flags, weather_json, error, quant_result, ev_signal
    - test_graphstate_annotated_error_field: error field is Annotated with a reducer — concurrent writes do not raise TypeError
  </behavior>
  <action>
    Write tests/test_graph.py first (RED stubs using pytest.raises or direct assertion).

    Then implement src/sportsbet/graph/state.py:
    - `from __future__ import annotations` at top
    - `from typing import Annotated, Any, TypedDict` and `from datetime import datetime`
    - `from operator import add` for list-append reducers if needed
    - GraphState fields exactly as locked in CONTEXT.md plus stub output holders:
      - `session_id: str` — UUID string, set by caller
      - `request_type: str` — "quant_analysis" | "odds_check" | "context_update"
      - `created_at: datetime`
      - `game_id: str`
      - `season: int`
      - `week: int`
      - `home_team: str`
      - `away_team: str`
      - `injury_flags: dict[str, str]`
      - `weather_json: dict | None`
      - `error: Annotated[str | None, lambda a, b: b]` — last-write-wins reducer (agents set error, Master Router reads it; only one agent runs per request so no true concurrent conflict, but Annotated required for INFRA-01 compliance)
      - `quant_result: Any | None` — holds QuantResult fixture instance set by quant_agent stub; typed as Any in Phase 2 (Phase 3 will narrow to QuantResult)
      - `ev_signal: Any | None` — holds EVSignal fixture instance set by arbitrage_agent stub; typed as Any in Phase 2 (Phase 5 will narrow to EVSignal)
    - Bankroll params (bankroll_usd, max_kelly_fraction) are NOT in GraphState — they live in Settings per locked decision
    - Total: 13 fields on GraphState
  </action>
  <verify>
    <automated>pytest tests/test_graph.py::test_graphstate_reducer tests/test_graph.py::test_graphstate_has_required_fields -q</automated>
  </verify>
  <done>GraphState TypedDict defined with all 13 fields; Annotated reducer on error field; quant_result and ev_signal present as Any | None; 3 GraphState tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Master Router, stub agents, and compiled StateGraph</name>
  <files>src/sportsbet/graph/router.py, src/sportsbet/graph/agents.py, src/sportsbet/graph/graph.py, src/sportsbet/graph/__init__.py, tests/test_graph.py, pyproject.toml</files>
  <behavior>
    - test_router_dispatch_quant: invoke compiled graph with request_type="quant_analysis", confirm returned state has quant_result set to a QuantResult instance (not None)
    - test_router_dispatch_odds: invoke with request_type="odds_check", confirm returned state has ev_signal set to an EVSignal instance (not None)
    - test_router_dispatch_context: invoke with request_type="context_update", confirm graph terminates cleanly without error (context_agent stub returns state unchanged)
    - test_router_routes_to_end_on_error: set error="some failure" in initial state, confirm graph terminates without dispatching to any agent (quant_result stays None)
    - test_graph_compiles: create_graph() returns a compiled object without exception
    - test_ainvoke_returns_state: await graph.ainvoke(initial_state) returns a dict with all GraphState fields present
  </behavior>
  <action>
    First confirm structlog is importable: add `structlog` to the `[project.dependencies]` list in pyproject.toml if not already present.

    Add new test cases to tests/test_graph.py (RED).

    Then implement:

    src/sportsbet/graph/router.py:
    - `master_router(state: GraphState) -> GraphState` — passthrough node (LangGraph router nodes return state unchanged or set error)
    - `route_from_master(state: GraphState) -> str` — conditional edge function:
      - if state["error"] is not None → return "end"
      - if state["request_type"] == "quant_analysis" → return "quant_agent"
      - if state["request_type"] == "odds_check" → return "arbitrage_agent"
      - if state["request_type"] == "context_update" → return "context_agent"
      - else → set state["error"] = f"Unknown request_type: {state['request_type']}" and return "end"

    src/sportsbet/graph/agents.py:
    - Import: `import structlog`, `from sportsbet.graph.models import QuantResult, EVSignal`
    - `log = structlog.get_logger()`
    - `quant_agent(state: GraphState) -> dict`:
      - Logs: `log.info("stub_agent_called", agent="quant_agent", session_id=state["session_id"])`
      - Returns `{"quant_result": QuantResult(true_probability=Decimal("0.62"), sample_size=142, data_source="fixture")}` — hardcoded fixture instance so Phase 3 can develop against a real QuantResult immediately
      - Comment: "Stub: Phase 3 replaces this with QuantParams validation + SQL query. Fixture instance establishes interface contract."
    - `arbitrage_agent(state: GraphState) -> dict`:
      - Logs: `log.info("stub_agent_called", agent="arbitrage_agent", session_id=state["session_id"])`
      - Returns `{"ev_signal": EVSignal(ev_percentage=Decimal("0.07"), true_probability=Decimal("0.62"), implied_probability=Decimal("0.55"), kelly_fraction=Decimal("0.05"), trade_plan=["Fixture edge 1", "Fixture edge 2"], market_type="moneyline")}` — hardcoded fixture instance so Phase 5 can develop against a real EVSignal immediately
      - Comment: "Stub: Phase 5 replaces this with real odds comparison logic. Fixture instance establishes interface contract."
    - `context_agent(state: GraphState) -> dict`:
      - Logs: `log.info("stub_agent_called", agent="context_agent", session_id=state["session_id"])`
      - Returns `{}` — no output model yet; Phase 4 will define context agent outputs
      - Comment: "Stub: Phase 4 replaces this with real context stream processing."
    - Note: stub agents return dicts (LangGraph merges dict returns into GraphState), not full GraphState — this is the correct LangGraph pattern for partial state updates

    src/sportsbet/graph/graph.py:
    - `create_graph() -> CompiledStateGraph`:
      - `builder = StateGraph(GraphState)`
      - `builder.add_node("master_router", master_router)`
      - `builder.add_node("quant_agent", quant_agent)`
      - `builder.add_node("arbitrage_agent", arbitrage_agent)`
      - `builder.add_node("context_agent", context_agent)`
      - `builder.set_entry_point("master_router")`
      - `builder.add_conditional_edges("master_router", route_from_master, {"quant_agent": "quant_agent", "arbitrage_agent": "arbitrage_agent", "context_agent": "context_agent", "end": END})`
      - `builder.add_edge("quant_agent", END)`
      - `builder.add_edge("arbitrage_agent", END)`
      - `builder.add_edge("context_agent", END)`
      - Return `builder.compile()` (no checkpointer yet — added in Plan 02-03)

    src/sportsbet/graph/__init__.py:
    - Export `create_graph` for easy import by downstream plans

    Tests use MemorySaver for isolation (even without checkpointer in this plan, tests can inject one).
    Use `asyncio.run(...)` or `pytest-asyncio` `async def test_` with `asyncio_mode="auto"`.
    Build a `make_minimal_state` helper returning a valid 13-field GraphState dict with quant_result=None, ev_signal=None.
  </action>
  <verify>
    <automated>pytest tests/test_graph.py -q</automated>
  </verify>
  <done>
    All 9 graph tests green; `create_graph()` compiles without error; Master Router dispatches to correct stub based on request_type; quant_agent sets quant_result to a QuantResult fixture instance; arbitrage_agent sets ev_signal to an EVSignal fixture instance; error field routes to END; `pytest tests/ -q` exits 0 with no regressions
  </done>
</task>

</tasks>

<verification>
`pytest tests/test_graph.py -q` → 9 tests pass (3 GraphState + 6 routing/invocation)
`pytest tests/ -q` → full suite exits 0

Manual smoke test (optional): `python -c "import asyncio; from sportsbet.graph import create_graph; print(create_graph())"` — should print the compiled graph object without import error.
</verification>

<success_criteria>
- GraphState TypedDict with all 13 fields defined; Annotated reducer on `error` field; `quant_result: Any | None` and `ev_signal: Any | None` present
- Stub agents return hardcoded fixture instances of QuantResult / EVSignal stored on GraphState — Phase 3 and Phase 5 can develop against these interface contracts immediately (locked CONTEXT.md decision)
- Master Router dispatches to exactly one of 3 stub agents based on request_type (locked: quant_analysis, odds_check, context_update)
- Error state routes to END without visiting any agent node
- `create_graph()` returns a compiled StateGraph
- structlog present in pyproject.toml dependencies and importable
- 9 tests green in test_graph.py; 10 tests still green in test_models.py; full suite exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-infrastructure/02-02-SUMMARY.md` following the summary template.
</output>
