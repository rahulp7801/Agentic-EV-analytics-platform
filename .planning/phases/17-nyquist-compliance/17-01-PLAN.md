---
phase: 17-nyquist-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-quant-engine/03-VALIDATION.md
  - .planning/phases/04-context-and-odds-ingestion/04-VALIDATION.md
  - .planning/phases/05-arbitrage-kelly-and-risk-controls/05-VALIDATION.md
  - .planning/phases/06-kinematic-agent/06-VALIDATION.md
  - .planning/phases/07-production-runtime-wiring/07-VALIDATION.md
  - .planning/phases/08-data-pipeline-and-backtest/08-VALIDATION.md
  - .planning/phases/09-critical-pipeline-gap-closure/09-VALIDATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Phases 3–9 VALIDATION.md files all have nyquist_compliant: true and wave_0_complete: true in frontmatter"
    - "Phases 3–9 VALIDATION.md files all have status: approved and updated: 2026-03-24"
    - "Wave 0 checklist items in phases 3–9 VALIDATION.md are all marked [x]"
    - "Per-Task Status column shows green checkmarks (not pending) for all tasks in phases 3–9"
    - "Validation Sign-Off blocks in phases 3–9 have all six items marked [x]"
    - "Phase 5 Per-Task Map uses actual passing test names (test_arbt01_ev_signal_produced etc.) not planning-era stubs"
  artifacts:
    - path: ".planning/phases/03-quant-engine/03-VALIDATION.md"
      provides: "Phase 3 Nyquist sign-off"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/04-context-and-odds-ingestion/04-VALIDATION.md"
      provides: "Phase 4 Nyquist sign-off"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/05-arbitrage-kelly-and-risk-controls/05-VALIDATION.md"
      provides: "Phase 5 Nyquist sign-off with corrected test names"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/06-kinematic-agent/06-VALIDATION.md"
      provides: "Phase 6 Nyquist sign-off"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/07-production-runtime-wiring/07-VALIDATION.md"
      provides: "Phase 7 Nyquist sign-off"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/08-data-pipeline-and-backtest/08-VALIDATION.md"
      provides: "Phase 8 Nyquist sign-off"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/09-critical-pipeline-gap-closure/09-VALIDATION.md"
      provides: "Phase 9 Nyquist sign-off"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "pytest output"
      to: "VALIDATION.md Wave 0 checklist"
      via: "manual verification before marking [x]"
      pattern: "pytest.*-x -q.*passed"
    - from: "Phase 5 VALIDATION.md"
      to: "test_arbitrage.py actual test names"
      via: "pytest --co -q to confirm collected names"
      pattern: "test_arbt01_ev_signal_produced"
---

<objective>
Retroactively mark phases 3–9 VALIDATION.md files as Nyquist-compliant by verifying the existing test suite covers all wave_0 requirements, then updating frontmatter, Wave 0 checklists, Per-Task Status columns, and Validation Sign-Off blocks.

Purpose: Close the nyquist compliance documentation debt identified in the v1.0 audit. Tests exist and pass — the VALIDATION.md files were written during planning before test names were finalized and were never updated after execution.

Output: Seven VALIDATION.md files with nyquist_compliant: true, wave_0_complete: true, status: approved, and all checklist items marked [x].
</objective>

<execution_context>
@C:/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-nyquist-compliance/17-RESEARCH.md

<interfaces>
<!-- Key facts the executor needs — extracted from research and existing VALIDATION.md files. -->

CURRENT STATE (all 7 files):
  frontmatter: nyquist_compliant: false, wave_0_complete: false, status: draft
  Wave 0 checklist: all items marked [ ]
  Per-Task Status: all rows show ⬜ pending
  Validation Sign-Off: all items [ ], "Approval: pending"

TARGET STATE (after this plan):
  frontmatter: nyquist_compliant: true, wave_0_complete: true, status: approved, updated: 2026-03-24
  Wave 0 checklist: all items marked [x]
  Per-Task Status: ✅ green for passing tests, ⬜ pending (skipif DB) for DB-gated tests
  Validation Sign-Off: all 6 items [x], approval note added

PHASE 5 SPECIFIC CORRECTION:
  Current Per-Task Map uses stubs: test_arbt01, test_arbt02, test_arbt03, test_arbt04
  Actual test names (from pytest --co): test_arbt01_ev_signal_produced, test_arbt02_kelly_fraction_non_flat,
    test_arbt03_conflict_blocked, test_arbt04_aggregator_daily_gate, plus e2e test
  Must update Per-Task Map commands to use actual names before marking [x]

DB-SKIPPED TESTS (count as wave_0 compliant per Phase 1 decision):
  Phase 3: test_run_quant_query_passing, test_quant_agent_live (skipif no SPORTSBET_TEST_DATABASE_URL)
  Mark these: ⬜ pending (skipif DB) NOT ✅ green — honest about environment requirement

PYTEST COMMANDS BY PHASE:
  Phase 3: pytest tests/test_quant.py tests/test_vig.py tests/test_backtest.py -x -q
  Phase 4: pytest tests/test_context.py -x -q
  Phase 5: pytest tests/test_arbitrage.py -x -q
  Phase 6: pytest tests/test_kinematic.py -x -q
  Phase 7: pytest tests/test_graph.py -k "Phase7Wiring" -x -q
  Phase 8: pytest tests/test_backtest.py::test_backtest_cli_main_prints_output tests/test_kinematic.py::test_matchup_query_returns_avg_time_to_throw -x -q
  Phase 9: pytest tests/test_context.py::test_context_agent_rejects_stale_odds -x -q

VALIDATION SIGN-OFF TEMPLATE (use this exact text in each file):
  - [x] All tasks have `<automated>` verify or Wave 0 dependencies
  - [x] Sampling continuity: no 3 consecutive tasks without automated verify
  - [x] Wave 0 covers all MISSING references
  - [x] No watch-mode flags
  - [x] Feedback latency < {N}s  (use the existing N from each file)
  - [x] `nyquist_compliant: true` set in frontmatter

  **Approval:** Retroactively approved 2026-03-24 — all wave_0 test files exist and pass in current test suite (163 passed, 11 skipped-DB, 0 failed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and sign off phases 3–6 VALIDATION.md files</name>
  <files>
    .planning/phases/03-quant-engine/03-VALIDATION.md
    .planning/phases/04-context-and-odds-ingestion/04-VALIDATION.md
    .planning/phases/05-arbitrage-kelly-and-risk-controls/05-VALIDATION.md
    .planning/phases/06-kinematic-agent/06-VALIDATION.md
  </files>
  <action>
Step 1 — Run targeted pytest for phases 3–6 to confirm tests pass:
  pytest tests/test_quant.py tests/test_vig.py tests/test_backtest.py -x -q
  pytest tests/test_context.py -x -q
  pytest tests/test_arbitrage.py -x -q
  pytest tests/test_kinematic.py -x -q

Step 2 — For Phase 5, collect actual test names to update the Per-Task Map:
  pytest tests/test_arbitrage.py --co -q
  Note the actual function names (they include _ev_signal_produced, _kelly_fraction_non_flat, etc.)

Step 3 — Update 03-VALIDATION.md:
  Frontmatter: set nyquist_compliant: true, wave_0_complete: true, status: approved, add updated: 2026-03-24
  Wave 0 Requirements: change all [ ] to [x]
  Per-Task Status: change all ⬜ pending to ✅ green EXCEPT:
    - test_run_quant_query_passing → ⬜ pending (skipif DB)
    - test_quant_agent_live → ⬜ pending (skipif DB)
  Validation Sign-Off: change all [ ] to [x], replace "Approval: pending" with the retroactive approval note

Step 4 — Update 04-VALIDATION.md:
  Same frontmatter changes. All Wave 0 items → [x].
  Per-Task Status: all 8 rows → ✅ green (test_context.py tests are unit tests using mocks, not DB-gated).
  Validation Sign-Off: all [x] + retroactive approval note.

Step 5 — Update 05-VALIDATION.md:
  Same frontmatter changes.
  Per-Task Map: UPDATE the test commands to use actual test names from step 2. Replace generic stub IDs:
    - test_arbt01 → test_arbt01_ev_signal_produced
    - test_arbt02 → test_arbt02_kelly_fraction_non_flat
    - test_arbt03 → test_arbt03_conflict_blocked
    - test_arbt04 → test_arbt04_aggregator_daily_gate
    For rows referencing test_e2e_pipeline, use the actual e2e test name collected from --co output.
  All Per-Task Status rows → ✅ green.
  Wave 0 items: change [ ] to [x] (tests/test_arbitrage.py exists and passes).
  Validation Sign-Off: all [x] + retroactive approval note.

Step 6 — Update 06-VALIDATION.md:
  Same frontmatter changes. All Wave 0 items → [x].
  Per-Task Status: rows for unit tests → ✅ green. The integration test (test_make_kinematic_agent_end_to_end) — check if it's DB-gated or pure mock; if skipif DB → ⬜ pending (skipif DB).
  Validation Sign-Off: all [x] + retroactive approval note.
  </action>
  <verify>
    <automated>pytest tests/test_quant.py tests/test_vig.py tests/test_backtest.py tests/test_context.py tests/test_arbitrage.py tests/test_kinematic.py -x -q --tb=short</automated>
  </verify>
  <done>
    - Phases 3–6 VALIDATION.md files all have nyquist_compliant: true, wave_0_complete: true, status: approved, updated: 2026-03-24 in frontmatter
    - All Wave 0 checklist items marked [x]
    - Phase 5 Per-Task Map uses actual test function names that exist in test_arbitrage.py
    - All Validation Sign-Off blocks have [x] on all 6 items and the retroactive approval note
    - DB-gated tests in Phase 3 are marked ⬜ pending (skipif DB) not ✅ green
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and sign off phases 7–9 VALIDATION.md files</name>
  <files>
    .planning/phases/07-production-runtime-wiring/07-VALIDATION.md
    .planning/phases/08-data-pipeline-and-backtest/08-VALIDATION.md
    .planning/phases/09-critical-pipeline-gap-closure/09-VALIDATION.md
  </files>
  <action>
Step 1 — Run targeted pytest for phases 7–9 to confirm tests pass:
  pytest tests/test_graph.py -k "Phase7Wiring" -x -q
  pytest tests/test_backtest.py::test_backtest_cli_main_prints_output tests/test_kinematic.py::test_matchup_query_returns_avg_time_to_throw -x -q
  pytest tests/test_context.py::test_context_agent_rejects_stale_odds -x -q

Step 2 — Update 07-VALIDATION.md:
  Frontmatter: set nyquist_compliant: true, wave_0_complete: true, status: approved, add updated: 2026-03-24
  Wave 0 Requirements: change all three [ ] items to [x] — the three test functions listed all exist in test_graph.py and were added during Phase 7 execution
  Per-Task Status: the VALIDATION.md has duplicate rows (Wave 0 stubs then Wave 1 implementations). Mark all 6 rows → ✅ green since all pass via TestPhase7Wiring class
  Validation Sign-Off: all [x] + retroactive approval note

Step 3 — Update 08-VALIDATION.md:
  Frontmatter: same changes.
  Wave 0 Requirements: both [ ] items → [x] (test_context_agent_persists_odds_snapshot exists in test_context.py, test_backtest_cli_main_prints_output exists in test_backtest.py, test_matchup_query_returns_avg_time_to_throw exists in test_kinematic.py)
  Per-Task Status: all 3 rows → ✅ green (these are mock-based tests, not DB-gated)
  Validation Sign-Off: all [x] + retroactive approval note

Step 4 — Update 09-VALIDATION.md:
  Frontmatter: same changes.
  Wave 0 Requirements: both [ ] items → [x]
  Per-Task Status:
    - 9-01-01 test_context_agent_rejects_stale_odds → ✅ green
    - 9-01-02 test_context_agent_persists_odds_snapshot → ✅ green
    - 9-01-03 test_run_quant_query_passing → ⬜ pending (skipif DB)
    - 9-01-04 test_run_quant_query_mock_adequate_sample → ✅ green
    - 9-01-05 test_quant_agent_live → ⬜ pending (skipif DB)
  Validation Sign-Off: all [x] + retroactive approval note

Step 5 — Run full suite to confirm nothing was broken:
  pytest tests/ -x -q --tb=short
  Expected: 163 passed, 11 skipped, 0 failed
  </action>
  <verify>
    <automated>pytest tests/test_graph.py tests/test_backtest.py tests/test_kinematic.py tests/test_context.py tests/test_quant.py -x -q --tb=short</automated>
  </verify>
  <done>
    - Phases 7–9 VALIDATION.md files all have nyquist_compliant: true, wave_0_complete: true, status: approved, updated: 2026-03-24 in frontmatter
    - All Wave 0 checklist items marked [x]
    - Per-Task Status rows correctly distinguish ✅ green (mock-based) from ⬜ pending (skipif DB)
    - All Validation Sign-Off blocks have [x] on all 6 items and the retroactive approval note
    - Full test suite still passes: 163 passed, 11 skipped, 0 failed
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -r "nyquist_compliant: false" .planning/phases/0[3456789]-*/` returns no results
2. `grep -r "wave_0_complete: false" .planning/phases/0[3456789]-*/` returns no results
3. `grep -r "status: draft" .planning/phases/0[3456789]-*/` returns no results (for VALIDATION.md only)
4. `pytest tests/ -x -q --tb=short` passes 163 tests, 11 skipped, 0 failed
</verification>

<success_criteria>
- All 7 VALIDATION.md files (phases 3–9) have nyquist_compliant: true and wave_0_complete: true
- Phase 5 Per-Task Map commands use actual pytest-collected test names
- DB-gated tests are honestly marked ⬜ pending (skipif DB) rather than ✅ green
- No new Python code was written — this is documentation repair only
- Full test suite remains green: `pytest tests/ -x -q` → 163 passed, 11 skipped, 0 failed
</success_criteria>

<output>
After completion, create `.planning/phases/17-nyquist-compliance/17-01-SUMMARY.md`
</output>
