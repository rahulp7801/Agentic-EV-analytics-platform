# Maintained implementation knowledge

## Working agreement

- PR119 merged at `8135fbc` from checked head `9578a86`; PR CI `35031354338`, PR CodeQL `35031352961`, protected-master CI/deploy `35031489454`, and master CodeQL `35031489275` passed. The immutable prediction payload now retains exact home/away identity. Current-model rows missing that identity fail the settlement evidence contract and are excluded from metrics. Post-deploy public daily run `35031755971` passed schema, production readiness, evidence-secret scan, and artifact retention; production metrics now truthfully report 0 eligible, 0 pending, 708 excluded, and null performance statistics.
- PR118 merged at `b1138eb`; all checks passed. Landing, Overview, and MarketWatch share a 90-minute market-evidence freshness contract, reject future/malformed capture times, and age open pages once per minute. A 390px production browser capture had no horizontal overflow and displayed the real current public coverage.
- `PAID_MONITOR_ENABLED` was enabled on 2026-09-15 after verifying the existing persistent 25-credit daily cap and read-only/no-execution path. Manual scheduled-path monitor `35032066424` passed hosted schema, collection, evidence-secret scan, and artifact retention in 55 seconds. Both leagues had zero games inside the next 24 hours, so production published fresh empty scan/prop-screen states instead of retaining stale counts. The same capture observed 32 NFL sportsbook events, 16 Kalshi games, 250 linked props/247 two-sided quotes, and 32 price comparisons; PrizePicks remained explicitly `access_denied`. Public observation and paid prop/model scans remain separate: a current public capture does not make an older paid prop screen current. The paid-screen UI must show its own generation time and describe stale funnel counts as retained captured coverage.
- PR112 merged at `df2f188`; protected-master CI/deploy `35017378864` and CodeQL `35017375092` passed without the simulated retry warning. A post-deploy Edge capture at 390px confirmed sportsbook, Kalshi, and PrizePicks source evidence now stacks as full-width readable cards with no viewport overflow; desktop rendering remains intact. The production readiness script also passes against the public alias after this release.
- PR111 merged at `c5c674b`; protected-master CI/deploy `35016466318` and CodeQL `35016450356` passed. Production readiness now cache-busts its home request and retries Vercel's brief alias propagation window while keeping persistent stale assets, missing headers, broken access controls, and unavailable required data fatal. The public Vercel landing page and terminal were then rechecked in Edge at desktop and mobile widths and render the current theme without viewport overflow. Retain real browser inspection after frontend releases.
- PR110 merged at `fdff958` after exact-head CI and CodeQL passed. Its forced Vercel deployment built and aliased the correct new UI, and the new CSS artifact gate caught an immediate stale edge response that the earlier HTTP/data-only check missed. The global stylesheet has a new asset identity, unused Tailwind/PostCSS packages are removed, deployments bypass the build cache, and readiness requires current theme, terminal-layout, and landing-hero markers.
- PR109 merged the UI overhaul at `fc343d9`. `/` is now an editorial public landing experience with an original animated market-topology graphic and live aggregate scan counts from `/api/scans`; the application is at `/terminal`. The terminal uses a restrained black/acid research-desk system, real signal ticker, scan evidence rail, desktop sidebar, and fixed mobile navigation. Its views expose publication gates, source coverage, pairing funnels, observed legs, costs, fee cases, exclusion reasons, game-log evidence, dependence scenarios, and assumption-driven Kelly sizing. No UI state invents records. Motion 13 is the single React animation runtime and respects reduced motion; Fontsource packages self-host the display, sans, and mono fonts. GSAP, Anime.js, and React Spring were rejected as duplicate animation engines; Threlte is Svelte-specific; Vectary and Jitter require separately authored assets.
- PR107 merged through protection at `bc7fffc` from exact head `681270a`; PR CI `34902172859`, CodeQL `34902171121`, protected-master CI/deploy `34902299853`, and master CodeQL `34902299357` passed. Production now labels the latest NFL scan `Player history coverage incomplete` and exposes the honest aggregate funnel: 250 selections, 240 model requests, 240 estimates, ten unresolved exact player-history matches and zero missing estimates. Impossible or falsely complete count relationships fail closed. No internal player IDs, queries, or errors are public.
- PR106 merged through protection at `b9a6593` from exact head `4bca7b9`; PR CI `34900819212`, CodeQL `34900816496`, protected-master CI/deploy `34900961794`, and CodeQL `34900960515` passed. Merged-master NFL run `34901142654` passed with the intended degraded warning and retained evidence. The job fell from 6m28s to 2m19s and the post-scan reporting tail fell from about 2m46s to 10s. It retained 499 quotes and 250 selections; all 240 submitted model requests returned estimates, while ten selections lacked an exact history identity. Two fresh Kalshi/sportsbook gross gaps were observed, but both combined costs exceeded $1 after captured Kalshi fee scenarios and sportsbook settlement equivalence remained unverified, so no profit or execution claim was made.
- PR105 merged through protection at `b53d23f` from exact head `2000630`; PR CI `34898396978` and CodeQL `34898394726` passed, and protected-master CI/deploy `34898540429` plus CodeQL `34898539795` passed. Vercel readiness succeeded. Merged-master read-only NFL run `34898746373` succeeded with a GitHub degraded-coverage warning, Gitleaks-clean retained evidence, and no orders. Total job time fell from 8m28s to 6m28s; more specifically, the real prop scan covered one event in 54s (`21:28:28`-`21:29:22`), wrote 479 quotes, 246 selections, 236 model requests and 236 estimates with no event failure, then metric publication consumed roughly 2m46s. That tail exposed the closing-quote N+1 addressed by PR106.
- PR104 merged at `938e749` after exact-head CI `34895693479` and CodeQL `34895690136` passed. Migration0023 was applied through the owner pooler connection; a production worker probe then proved SELECT/INSERT and sequence use on both quote tables while UPDATE/DELETE remained denied. The initial protected-master schema check raced the external migration, then the exact same run `34895985793` passed completely on rerun; master CodeQL `34895984850` passed. No master protection was bypassed.
- Second capped hosted NFL daily run `34896322396` completed its one eligible event without an exception and wrote 443 quotes, 244 selections, 232 model requests and 232 model estimates. It remained honestly degraded because Cyrus Allen and Emmett Johnson had no resolvable nflverse history (12 skipped selections total) and PrizePicks returned `access_denied`; all resolved model requests produced estimates. Kalshi observed all 16 games, 254 structured props (242 two-sided, 12 one-sided) and all four fee contexts. The public prop screen retained 22 exact markets and 36 paired sides, excluded eight sides for observation skew, and found zero gross gaps/comparisons, zero orders and no profit. The eight-minute runtime exposed the per-selection database-connection bottleneck now addressed on the active branch.
- GitHub's paid collection workflow is named `Live paid market data` to distinguish it from CI. CLI exit code2 remains the strict persisted `degraded` coverage signal, but the workflow converts that completed operational state into a visible Actions warning and job summary; the dashboard stays degraded. All other nonzero exits, schema/configuration failures, crashes, and evidence-secret failures remain red. Recent CI/CodeQL/deploy runs were green; the recurring red `Market data` entries were operational coverage reports, not failed unit tests. Earlier public runs `34882013857` and `34883499067` collected successfully but failed the deployed-readiness identity gate before PR102; subsequent public run `34895707647` passed.
- PR103 merged at `e7a22f4` from exact checked head `f1062a2`. PR CI `34894454734` and CodeQL `34894451506` passed, including the disposable PostgreSQL reproduction of the prior autovacuum deadlock. Protected-master CI/deployment `34894614762` passed hosted schema, Vercel data/access readiness, dependency audits, secret scan, isolated worker image, backend and frontend. Collection credentials are step-scoped; checkout/setup/readiness/Gitleaks/upload receive none, and the public workflow contains no Odds key. Role provisioning acquires all policy-table locks before catalog writes to prevent the observed wait cycle.
- First capped hosted NFL daily run after PR103, `34894861753`, safely degraded: nflverse history refreshed 272 games/current stats with one unattributed row skipped; schedule completed; sportsbook observed 17 games with partial coverage; Kalshi observed all 16 games plus 254 structured props (243 two-sided, 11 one-sided) and all four fee contexts; PrizePicks returned the fixed `access_denied` state. One eligible sportsbook event consumed its reserved four-credit prop batch, then pre-model quote persistence failed with `InsufficientPrivilegeError`; zero events completed, zero model estimates/predictions, and no settlement candidates or profit claim. Public evidence scanning and retention passed. Do not rerun until migration0023 is deployed.
- PR102 merged at `494a03e` from exact checked head `35ad51d`. PR CI `34725496743` and CodeQL `34725495406` passed; protected-master CI/deployment `34725562669` and CodeQL `34725561880` passed, including hosted schema and Vercel readiness. Public jobs now retain their current report and require production market/schedule timestamps to match that exact run. Real merged-master monitor `34725662303` passed the identity check, schema, public collection, evidence-secret scan and artifact retention: NFL observed all 15 games and 3,046 structured markets (2,895 two-sided, 151 one-sided), all 56 fee contexts and no failures; NBA was an honest offseason empty result. Paid prop screens stayed unavailable as configured, execution stayed disabled, and no profit was claimed. The missing natural 23:22 public schedule event is further evidence that GitHub cron cadence is not guaranteed.
- PR101 merged at `ef69ae4` from exact checked head `a2740b2`. PR CI `34724669397` and CodeQL `34724669007` passed; protected-master CI/deployment `34724744351` and CodeQL `34724744093` passed, including hosted schema and Vercel readiness. Public collection now has its own schedules and concurrency group, references no Odds API secret, and is enabled only by `PUBLIC_DATA_PIPELINE_ENABLED`; paid schedules remain controlled by `DATA_PIPELINE_ENABLED`. Real merged-master `public_monitor` run `34724844720` passed schema, collection, evidence-secret scan and artifact retention. NFL observed all 15 sampled games, 3,046 structured prop markets (2,893 two-sided, 153 one-sided, 0 unquoted), all 56 fee contexts and no failures; NBA was an honest offseason empty result. Production exposed the exact 23:13-23:14Z schedule/market timestamps with 13 NFL dashboard games and 30 comparisons. Both retained archives replayed exactly with 30/0 comparisons, zero positive gross gaps, `execution_ready=false` and null realized profit. This proves one isolated public run, not scheduler cadence, priced cross-venue profit, or season readiness.
- PR100 merged at `df11d4c` from exact checked head `c3d5ca2`. PR CI `34723995480` and CodeQL `34723994752` passed; protected-master CI/deployment `34724068941` and CodeQL `34724068423` passed, including hosted schema and Vercel readiness. The bounded selection/scan identity contract now applies at the ledger write boundary, rejecting empty or oversized identities, unsupported directions, and non-finite or out-of-range lines before storage. The focused ledger/settlement/scan set passed 59 tests; the full backend passed 613 with 26 optional-PostgreSQL skips and 7 existing pandas warnings.
- PR99 merged at `7f8eaa3` from exact checked head `4e8f391`. PR CI `34723573691` and CodeQL `34723572564` passed; protected-master CI/deployment `34723638400` and CodeQL `34723638160` passed, including Vercel readiness. Malformed retained payloads are excluded without suppressing honest metrics; invalid outcome/actual evidence stays unverified and pending. Recommendation replay requires literal acceptance and a positive stake no larger than the 5% reservation ceiling, while malformed identity/model-version values fail closed. The focused set passed 68 tests; the full backend passed 605 with 26 optional-PostgreSQL skips and 7 existing pandas warnings.
- PR98 merged at `266a6b9` from exact checked head `3a1c469`. PR CI `34722825411` and CodeQL `34722824165` passed; protected-master CI/deployment `34722882103` and CodeQL `34722881768` passed, including Vercel readiness. Settlement now honors the bounded schedule/catch-up date set and binds every new verified stat row to the matched game's team identity; a stat from another same-day matchup stays pending. A read-only comparison confirmed ESPN `LAR`/`WSH` versus nflverse `LA`/`WAS`, which are the only explicit translations. The focused set passed 41 tests with 8 optional-PostgreSQL skips; the full backend passed 602 with 26 optional-PostgreSQL skips and 7 existing pandas warnings.
- PR97 merged at `20aabc1` from exact checked head `080f3f1`. PR CI `34722002474` and CodeQL `34722000738` passed; protected-master CI/deployment `34722073656` and CodeQL `34722073342` passed, including Vercel readiness. It preserves quote-evidence enforcement when a scanner model stops being current: `empirical-v2`, `empirical-jeffreys-v3`, and `empirical-jeffreys-v4` remain provenance-required cohorts. The ledger reconstructs the exact normalized sportsbook quote, applies the ingestion layer's complete pregame contract and recomputes its row commitment, so changed retained price/line evidence and incomplete historical rows fail closed. The focused provenance/ledger/scan/quote set passed 60 tests; the full backend passed 600 with 26 optional-PostgreSQL skips and 7 existing pandas warnings.
- PR96 merged at `3fc1693` from exact checked head `6c6942a`. PR CI `34721176887` and CodeQL `34721174729` passed; protected-master CI/deployment `34721257585` and CodeQL `34721257277` passed, including Vercel readiness. It fixes the NBA sparse-context dead end and introduces `empirical-jeffreys-v4`: underpowered opponent/venue/availability cohorts fall back to the same cutoff-safe rolling window. Hosted history had exact-context median `n=1`, maximum `n=5`, zero eligible of 5,179 recent targets, versus 4,850 eligible rolling cohorts; a real executor replay broadened `n=3` to `n=40`. The v4 fixed-threshold NBA walk-forward evaluated 192/203 with Brier `0.087149`, log loss `0.283616`, calibration error `0.038333`, nine game clusters and null ROI/CLV. Full backend passed 600 with 26 optional-PostgreSQL skips; frontend passed 49 tests, TypeScript, ESLint and production build. This is sample and calibration evidence, not priced profit.
- PR95 merged at `2d377fa` from exact checked head `f1b4b99`. PR CI `34719900482` and CodeQL `34719898922` passed; protected-master CI/deployment `34719982747` and CodeQL `34719982505` passed, including Vercel readiness. Public prop-screen counts, comparisons, identities, reasons and numeric text are bounded; fee scope uses fixed public language; displayed Kalshi size is validated but omitted. Financial/settlement consistency and five-minute freshness remain unchanged. The restricted database has no NFL/NBA prop-screen snapshots while paid collection is disabled, so validation is fixture/producer-contract evidence only. Frontend tests passed 49 with TypeScript, ESLint and production build.
- PR94 merged at `cc107b8` from exact checked head `aa40a70`. PR CI `34719493382` and CodeQL `34719492755` passed; protected-master CI/deployment `34719577058` and CodeQL `34719576768` passed. Deployed `/api/games` now exposes exactly seven display fields on each of 13 NFL games and the honest zero-game NBA state. Provider IDs, completion state and source internals are omitted. Sport, status/partial consistency, current Eastern date, 90-minute freshness, real calendar/start date, fixed three-day window, identity, duplicates and the 100-game bound fail closed. Frontend tests passed 48 with TypeScript, ESLint and the production build.
- PR93 merged at `758abba` from exact checked head `bb879b5`. PR CI `34719089388` and CodeQL `34719088580` passed; protected-master CI/deployment `34719188106` and CodeQL `34719188301` passed. The live signal endpoint remains the honest empty envelope after deployment. Root metadata, generated time and cached games now have bounded schemas; nested signals add collection, identity/text, sample, line, mean and flag bounds. Conflicting duplicate games and invalid real calendar dates fail closed. Hosted and local routes share the same contract and no longer forward arbitrary root fields. Frontend tests passed 47 with TypeScript, ESLint and the production build.
- PR92 merged at `f6c00ca` from exact checked head `73dda90`. PR CI `34718667621` and CodeQL `34718666589` passed; protected-master CI/deployment `34718747984` and CodeQL `34718747909` passed. Deployed `/api/gamelogs` validates and projects database JSON. Exact live 200-row NBA and NFL samples expose only the seven supported NBA or five supported NFL stats. Required public identity, real calendar dates, home/away type, finite values, conservative magnitude, nonnegative count stats and the 200-row bound fail closed; legitimate negative NFL yardage remains valid.
- Naturally scheduled public monitor `34718789392` succeeded on merged `f6c00ca` and proved PR91's runtime contract: histories and settlements were `not_requested`, settlement reason was `monitor_mode`, and scope explicitly said those stages plus sportsbooks, PrizePicks and recommendations were not requested. NFL observed all 15 sampled games, 3,046 structured props (2,833 two-sided, 213 one-sided, 0 unquoted), all 56 fee contexts and no failures; NBA was an honest offseason empty result. Schedules were complete and the retained evidence passed Gitleaks. This is source coverage, not executable arb or profit evidence.
- PR91 merged at `5f6bb3c` from exact checked head `9a17cb3`. PR CI `34718320714` and CodeQL `34718319044` passed; protected-master CI/deployment `34718393420` and CodeQL `34718393315` passed. `public_monitor` now states that history refresh and settlement were not requested and uses the fixed `monitor_mode` reason; `public_daily` alone claims those stages. The focused daily orchestration suite passed 17 tests. Latest real scheduled monitor `34714642768` succeeded at `4a4ac30`: NFL observed all 15 sampled games, 3,081 structured props (2,842 two-sided, 239 one-sided, 0 unquoted) and all 56 fee contexts with no failures; NBA was an honest offseason empty result. Histories and settlements were not requested. This is source coverage evidence, not arb or profit evidence.
- PR90 merged at `d8226a2` from exact checked head `2bd1ec3`. PR CI `34717806002` and CodeQL `34717804608` passed; protected-master CI/deployment `34717884723` and CodeQL `34717884494` passed. Deployed `/api/markets` validates and projects its public contract. NFL retains all 30 displayed comparisons while shrinking from 242,392 to 37,645 JSON bytes; NBA remains an honest empty snapshot. Root evidence digests, profit/execution fields, displayed-size internals, fee accumulators, modeled fill ladders and internal calculation legs are omitted. Public source status, coverage partitions, sport, timestamps, identity and complementary cost arithmetic fail closed; explanatory scope is fixed in code. Frontend tests passed 43 with ESLint, TypeScript and the production build.
- PR89 merged at `b437405` from exact checked head `33f2b30`. PR CI `34717248925` and CodeQL `34717247741` passed; protected-master CI/deployment `34717333841` and CodeQL `34717333490` passed. Both deployed metric cohorts return HTTP 200 with exactly 30 allowlisted fields and honest zero samples. Cohort identity, counts, numeric bounds, calibration bins, model identifiers and complete cluster groups fail closed; arbitrary snapshot fields and mutable stored explanatory text are omitted. The older hosted snapshots' absent hit-rate interval and calibration error remain null until a current worker report is published. Frontend tests passed 41, including four boundary regressions; ESLint, TypeScript, the production build and dependency audit passed.
- PR88 merged at `b8b8eb7` from exact checked head `d9b07cd`. PR CI `34716381796` and CodeQL `34716381542` passed; protected-master CI/deployment `34716462447` and CodeQL `34716462208` passed. It adds deterministic 95% game-cluster robust t intervals for ROI, hit rate, Brier score, log loss and CLV. Quote replay and ledger evaluation carry exact game IDs into the same `BacktestEngine`; each metric reports its distinct-game count and stays unavailable with fewer than two fully identified games. A regression with eight selections in four perfectly dependent game pairs widens the naive Wilson hit-rate interval from 21.5%-78.5% to a cluster interval of 0%-100%. The unpriced LangGraph walk-forward uses the same score implementation. Retained September 7/14 NFL reception cohorts preserve their point estimates and each span 13 games: Brier intervals are 0.092624-0.160176 and 0.105341-0.174573; log-loss intervals are 0.306111-0.469716 and 0.337012-0.533429. Both reports share source hash `0a8eaf156dd8aa3daa5d7f26e20687f0b5a9daf6357ebb4200d73e5d40b7489d`. They remain fixed-threshold, unpriced, post-implementation checks with null ROI/CLV, not profit evidence. This handles within-game dependence only; repeat-player dependence across games remains unadjusted. Focused backend tests passed 41; the full backend passed 599 with 26 optional-PostgreSQL skips and 7 existing pandas warnings; frontend passed 37 tests, ESLint, TypeScript and the production build. A read-only restricted-Supabase report confirmed the current hosted model cohorts contain zero eligible predictions, so all cluster counts are zero and intervals remain null.
- PR87 merged at `cdd6304` from exact checked head `f119c1f`. PR CI `34715496017` and CodeQL `34715494915` passed; protected-master CI/deployment `34715567059` and CodeQL `34715566713` passed, including hosted schema, dependency audits, PostgreSQL, secret scanning and Vercel readiness. Successful PrizePicks archives now use a canonical raw-payload commitment and per-projection row commitments with one provider observation time. Fixture replay detects raw and normalized-row tampering; the real endpoint remains HTTP 403, so no current projection or profit was claimed. Full backend passed 597 tests with 26 optional-PostgreSQL skips.
- PR86 merged at `da398f8` from exact checked head `84b3c83`. PR CI `34715021780` and CodeQL `34715020838` passed; protected-master CI/deployment `34715106229` and CodeQL `34715106165` passed, including hosted schema, dependency audits, PostgreSQL, secret scanning and Vercel readiness. Two real public NFL requests returned HTTP 403; the collector and dashboard now expose only fixed safe availability reasons, while generic failures expose no exception text, URL, body, credential, or class. Full local backend passed 596 tests with 26 optional-PostgreSQL skips; frontend passed 36 tests, ESLint, TypeScript and the production build. No projection, price, payout, order, or profit was invented.
- PR85 merged at `4a4ac30` from exact checked head `da609bb`. PR CI `34714209166` and CodeQL `34714208066` passed; protected-master CI/deployment `34714289974` and CodeQL `34714289402` passed, including hosted schema and Vercel readiness. Public collection now attempts `public_daily` at both 13:17 and 13:47 UTC; the recovery cron is explicitly excluded from paid operations and Odds-key injection. Live metadata had shown the configured 19:22 monitor event being dropped, so the extra daily attempt mitigates a demonstrated scheduler condition without claiming guaranteed execution. Full local backend passed 590 tests with 26 optional-PostgreSQL skips.
- PR84 merged at `974de03` from exact checked head `a5041e4`. PR CI `34713660174` and CodeQL `34713659104` passed; protected-master CI/deployment `34713747127` and CodeQL `34713747254` passed, including hosted schema and Vercel readiness. Both arb screeners now require sportsbook normalized-row evidence bound to the exact provider event payload, and published sportsbook legs retain the provider/event/row commitments. A rehashed quote bound to a different event fails closed. Full local backend passed 590 tests with 26 optional-PostgreSQL skips.
- PR83 merged at `51ece38` from exact checked head `e4b166c`. PR CI `34713213647` and CodeQL `34713212754` passed; protected-master CI/deployment `34713303073` and CodeQL `34713302814` passed, including hosted schema and Vercel readiness. Both arb paths now share fail-closed sportsbook identity, price/probability, side, freshness and half-point-strike validation. A +150 quote mutated to a 1% implied probability can no longer manufacture a cross-venue gross gap. Full local backend passed 589 tests with 26 optional-PostgreSQL skips.
- PR82 merged at `8e84806` from exact checked head `0eed809`. PR CI `34712686737` and CodeQL `34712686101` passed; protected-master CI/deployment `34712784648` and CodeQL `34712784426` passed, including hosted schema and Vercel readiness. Missing kinematic context now warns only when experimental probability adjustments are enabled; the default v3 base model no longer floods one warning per receiving estimate for context it does not consume. Retained September 7/14 NFL reception archives were rebuilt from exact existing provider bytes and evaluated through the restricted-DB LangGraph at a fixed 4.5 threshold: 122/205 and 128/210 outcomes, Brier 0.126400/0.139957, log loss 0.387913/0.435221 and calibration error 0.097398/0.077299. They are post-implementation, shared-game, unpriced regressions with null ROI/CLV, not profit evidence.
- PR81 merged at `1a0a937` from exact checked head `c0b1b5a`. PR CI `34711872078` and CodeQL `34711870827` passed; protected-master CI/deployment `34711951178` and CodeQL `34711950660` passed, including the real-PostgreSQL reception settlement/proof replay, hosted schema and Vercel readiness. New nflverse row digests commit receptions and automatic settlement/backtest mappings include them. Pre-extension NFL digests remain valid for yard props only and cannot authenticate receptions. Full local backend passed 585 tests with 26 optional-PostgreSQL skips. No provider call, order path, synthetic price, or profit claim.
- Security recheck on 2026-09-12: tracked credential/key patterns contain only `.env.example`. GitHub repository secret metadata reports `ODDS_API_KEY` created/updated 2026-09-10T23:06:57Z, before the current security work, so rotation after the known historical exposure is still unverified. Keep paid collection disabled until a newly issued key replaces it; do not expose or test the existing value.
- PR80 merged at `d581055` from exact checked head `e2ad85b`. PR CI `34710853662` and CodeQL `34710852421` passed; protected-master CI/deployment `34710931035` and CodeQL `34710930670` passed. The NBA context node no longer treats a missing prior gamelog or a latest team outside the scheduled matchup as the away side. It returns no matchup context unless the latest pregame team matches exactly one scheduled side, so the quant agent uses base as-of history without invented opponent/home-away filters. Full backend verification passed 583 tests with 25 optional-PostgreSQL skips.
- PR79 merged at `12f47dd` from exact checked head `de8957a`. PR CI `34710247451` and CodeQL `34710246217` passed; protected-master CI/deployment `34710322665` and CodeQL `34710322559` passed. The protected frontend job explicitly ran and passed `npm run lint` before TypeScript, build and audit. Hosted schema, backend, PostgreSQL, secret scan, isolated worker image and Vercel readiness also passed.
- PR78 merged at `364adc5` from exact checked head `f815ac2`. PR CI `34709810039` and CodeQL `34709809197` passed; protected-master CI/deployment `34709891783` and CodeQL `34709891527` passed, including hosted schema and Vercel readiness. Production retained the real public NFL capture: 3,081 structured = 2,831 two-sided + 250 one-sided + 0 unquoted, `partial_coverage=false`, `execution_ready=false`, `realized_profit=null`. Paid `/api/prop-screens` correctly remains HTTP503 while the paid pipeline is disabled. The new side funnel therefore has tested plumbing but no real paid cross-venue values yet.
- PR77 merged at `001b2f2` from exact checked head `df62e24`. PR CI `34709084187` and CodeQL `34709083068` passed; protected-master CI/deployment `34709168652` and CodeQL `34709168527` passed, including hosted schema and Vercel readiness. The live `/api/signals` response is HTTP 200 with `signals=[]` and `invalid_signals=0`. The public boundary rejects incomplete or malformed signal identity, price, model and time fields, recomputes derived payout metrics, strips internal fields and counts rejects; the dashboard no longer invents NBA/points/Over/PrizePicks/-105/zero defaults. PrizePicks projections remain excluded from priced signals without complete entry payout evidence.
- PR76 merged at `2ab589f` from exact checked head `be1c0d3`. PR CI `34708217787` and CodeQL `34708216318` passed; protected-master CI/deployment `34708292570` and CodeQL `34708292478` passed, including hosted schema and Vercel readiness. It partitions every normalized prop market into two-sided, one-sided, or unquoted top-of-book availability and reports YES/NO ask counts. Pre-merge real artifact `34707543515` replayed as 3,081 = 2,822 + 259 + 0. Post-deployment public-only run `34708395666` passed schema/evidence-secret gates, observed 15/15 NFL games with no collection failures and published live API values 3,081 = 2,831 two-sided + 250 one-sided + 0 unquoted, with 3,081 YES and 2,831 NO asks. Live `partial_coverage=false`, `execution_ready=false`, and `realized_profit=null`. These are displayed-quote coverage metrics, not liquidity, fills, settlement equivalence, edge, or profit.
- PR75 merged at `6220f68` from exact checked head `d217fbb`. PR CI `34707330355` and CodeQL `34707329088` passed; protected-master CI/deployment `34707407110` and CodeQL `34707407396` passed, including hosted schema and Vercel readiness. It follows two real post-schema-gate public monitors (`34705564190`, `34707083688`) that refreshed both schedules but failed closed on partial Kalshi reads. The shared GET client retries only transport errors and 429/500/502/503/504 twice at 250/500ms, regenerating authenticated signatures; permanent 4xx remains immediate. Real public-only monitor `34707543515` then passed at hosted schema `0022`, refreshed both schedules, observed all 15/15 NFL events with zero event/market failures, inventoried all 4 series and 3,081 linked/resolved props with 56/56 fee contexts, retained a secret-scanned replay artifact, and kept execution disabled. This one successful run verifies recovery, not continuous reliability.
- PR74 merged at `ae62ffe` from exact checked head `0c9435a`. PR CI `34707068815` and CodeQL `34707068158` passed; protected-master CI/deployment `34707183044` and CodeQL `34707182621` passed. Kalshi collection coverage now partitions attempted games into observed event records plus event failures, and observed records into sample-complete plus partial-market-failure games. The legacy failure union remains for old clients; the dashboard uses explicit stage wording for new snapshots and a tested fallback for old snapshots.
- PR73 merged at `78f4a47` from exact checked head `e014185`. Final PR CI `34706509502` and CodeQL `34706507700` passed; protected-master CI/deployment `34706582704` and CodeQL `34706582310` passed. The new production-schema job passed against real Supabase before the Vercel deploy. Hosted migration head is `0022`; the restricted worker passes `alembic current --check-heads` through a worker-only RLS SELECT policy and still cannot update the version row or create in either application schema.
- Branch `fix/hosted-schema-readiness` follows a real scheduled-production failure: run `34704809932` refreshed histories, schedules and public Kalshi observations, then settlement failed with PostgreSQL `UndefinedColumn`. Read-only inspection found hosted Supabase at migration `0018` while deployed code required `0019`/`0020`; both already-reviewed additive migrations were applied and exact columns verified. The restricted worker can read the new evidence column and has column-scoped update permission. Recovery `public_monitor` run `34705564190` no longer hit the schema failure, refreshed both schedules, and failed closed for a separate transient partial Kalshi capture (13/15 observed, 3 failed); monitor mode intentionally did not grade outcomes because it does not refresh history. The branch grants the restricted worker SELECT only on `alembic_version`, adds a worker-only SELECT policy because hosted Supabase already had RLS enabled on that table, and makes scheduled collection plus protected-master deployment require `alembic current --check-heads`. Worker schema/version mutation remains prohibited. Focused workflow tests pass 2; full backend passes 576 with 25 optional-PostgreSQL skips and 7 known pandas warnings; frontend passes 28 tests, TypeScript and the production build. Offline Alembic generation includes only the expected grant/policy additions. Corrected exact-head PR CI `34706259822` and CodeQL `34706258113` passed on `a61c316`, including disposable PostgreSQL. Hosted Supabase is now at `0022`; the real worker passes `alembic current --check-heads`, cannot update the version row, and cannot create in either application schema. No paid call or order path is added.
- PR72 merged at `48886f3` after exact-head CI `34705287127` and CodeQL `34705285571` passed on `e222cb0`. Protected-master CI/deployment `34705365596` and CodeQL `34705365510` passed, including Vercel readiness. It adds nominal 95% Wilson intervals for hit rate and observed rates inside every populated calibration bin, plus fixed 10-bin calibration error. The report and dashboard state the independence assumption and lack of shared-game/player adjustment; ROI, Brier, log loss and CLV remain point estimates. Focused replay/ledger tests pass 26; full backend passes 575 with 25 optional-PostgreSQL skips and 7 known pandas warnings; frontend passes 28 tests, TypeScript and the production build. Actual restricted-DB LangGraph reruns preserved the model scores and added calibration error: NBA 192 forecasts, Brier 0.087149/log loss 0.283616/calibration error 0.038333; NFL 16 forecasts, Brier 0.236471/log loss 0.666337/calibration error 0.187607. Both final reports share exact Python source hash `479e157b4a3b42ae5aae6e661250deb65758ab97c70da34e1860e6c2991c3797`. Fixed thresholds remain unpriced research benchmarks, ROI/CLV are null, and neither cohort is independent or proof of profit. The hosted current-model ledger honestly contains zero eligible predictions while paid collection remains disabled, so its uncertainty and performance values remain null.
- PR71 merged at `294f137` after exact-head CI `34704269298` and CodeQL `34704268373` passed on `5b41982`. Protected-master CI/deployment `34704351840` and CodeQL `34704351455` passed, including secret scanning, PostgreSQL, worker isolation and Vercel readiness. It adds public-only recovery attempts at :22/:52 around the existing :07/:37 monitor cadence. The supplemental jobs skip when only full collection is enabled and select `public_monitor` without `ODDS_API_KEY` when both flags are enabled. GitHub scheduling remains best-effort; retain the 90-minute fail-closed gate and do not claim continuous monitoring.
- PR70 merged at `5923d03` after exact-head CI `34703352010` and CodeQL `34703350669` passed on `877aba1`. It exposes the already-published `metrics:all` and `metrics:recommendations` cohorts through one validated read-only API. The dashboard labels all eligible predictions/unit stakes separately from accepted recommendations/recorded stake fractions and renders loading or unavailable states instead of temporary zeros. All 27 frontend tests, TypeScript and the production Next.js build pass. Protected-master CI `34703441778` initially failed only at Vercel readiness because both schedule snapshots had exceeded the enforced 90-minute age after GitHub omitted expected monitor starts. Approved public-only recovery run `34703643318` refreshed both leagues without a paid key, after which NFL/NBA schedules and both metric cohorts returned 200, invalid metric cohort returned 400, and the failed deploy/readiness job rerun passed. This is evidence that freshness checks fail closed and manual recovery works; it is also evidence that the prior half-hour GitHub schedule was not production-reliable.
- PR69 merged at `51db011` after corrected exact-head CI `34702917685` and CodeQL `34702915275` passed on `c9be0a7`, including the real PostgreSQL overlap-gate assertion. Protected-master CI/deployment `34702998695` and CodeQL `34702998242` passed, including Vercel readiness. It requires valid confidence-bounded prop edges, sizes Kelly from the lower bound, correctly complements Under within non-push mass, and retains sample size/directional intervals for every audited estimate. Final backend passes 574 with 25 optional-PostgreSQL skips and 7 known pandas warnings. No provider call or order path changed.
- PR68 merged at `8216635` after corrected exact-head CI `34701676973` and CodeQL `34701675418` passed on `5805a18`, including the real PostgreSQL graph cases that caught two stale raw-rate expectations on the first run. Protected-master CI/deployment `34701768866` and CodeQL `34701768676` passed, including Vercel readiness. It introduces `empirical-jeffreys-v3`: a shared NBA/NFL per-game estimator that preserves observed push mass and uses the fixed Jeffreys Beta(1/2,1/2) posterior predictive mean on decided outcomes. Actual restricted-DB LangGraph reruns on reused, unpriced fixed-threshold cohorts changed NFL16-case Brier 0.237315->0.236471/log loss 0.668644->0.666337 and NBA192-case Brier 0.087209->0.087149/log loss 0.434929->0.283616. A separate integer-line NFL graph run retained nonzero historical push estimates in 2/16 forecasts. All reports share source hash `3315f8fc07d0b2d35f6645515d8a2e3bbee75b5f0608d21af6a80a7b8ace8b93`. They are regression evidence, not fresh holdouts or profit evidence; ROI/CLV remain null.
- PR67 merged at `21e13a1` after exact-head CI `34700258922` and CodeQL `34700257415` passed on `2bdc17f`. Protected-master CI/deployment `34700331349` and CodeQL `34700331508` passed, including the stricter Vercel readiness gate. It makes the production gate honor `PUBLIC_DATA_PIPELINE_ENABLED`: signals, metrics, scans and both prop-screen routes remain conditional on the paid flag; markets, schedules and game logs must return 200 when either public or full collection is enabled. Route classification/failure behavior have Node regressions. This closes a CI false-pass in which public endpoints could return 503 after deployment.
- Latest automatic public monitor run `34698933392` succeeded at 2026-09-12T14:19Z. Both workflow artifacts replay through the actual `sportsbet.market_watch` path exactly and match production capture times/hashes. NFL observed 15/15 Kalshi games, all 4 prop series, 3,081 linked/resolved quotes, 2,883 two-sided quotes and complete 56/56 fee contexts; it produced 30 comparisons and zero positive gross gaps. NBA observed a complete empty offseason inventory and zero comparisons. Both reports keep `realized_profit=null` and `execution_ready=false`; sportsbook/PrizePicks were not requested. Recent automatic successes have 2-5 hour gaps despite the half-hour cron, so do not claim 30-minute coverage.
- PR66 merged at `3e3db3d` after exact-head CI `34699599223` and CodeQL `34699597869` passed on `defa3899`; protected-master CI/deployment `34699675187` and CodeQL `34699675022` passed. It derives unresolved/legacy-invalid automatic outcome dates from the ledger. Each daily run keeps normal final schedule evidence for offsets -7 through -1 and adds at most seven older unique dates, oldest first, within a hard 30-day bound; verified old outcomes and manual audit outcomes do not consume catch-up slots. The public schedule snapshot remains only yesterday/today/tomorrow. Focused settlement/schedule/daily tests pass 45; full backend passes 556 with 25 disposable-PostgreSQL skips and 7 known pandas warnings. No paid provider call/order.
- PR65 merged at `d2d91f9` after corrected exact-head CI `34699018758` and CodeQL `34699016707` passed on `4c221f4`, including real PostgreSQL migration/access/settlement checks. Protected master CI/deployment `34699115451` and CodeQL `34699115328` passed, applying migration 0020 and verifying Vercel access/data readiness. It retains each automatic result's canonical final-schedule/stat proof in the prediction ledger. Performance reporting re-hashes the proof, re-hashes its exact normalized stat row, binds schedule/team/player/prop/provider identities, checks observation chronology, and recomputes the outcome from actual value, line and side. Manual, legacy, incomplete, inconsistent and tampered outcomes remain auditable but are counted as pending and cannot enter Brier/ROI/calibration. Migration 0020 preserves legacy rows and requires evidence on future `observed_final_stats` writes. Focused settlement/ledger tests pass 36; broader settlement/daily/storage/migration/access tests pass 54 with 17 disposable-PostgreSQL skips; full backend passes 555 with 25 disposable-PostgreSQL skips and 7 known pandas warnings; frontend passes 22 tests, TypeScript and production build. No provider call/order.
- PR64 merged at `5b117d6` after exact-head CI `34697816486` and CodeQL `34697815807` passed on `214f0ba`. Protected master CI/deployment `34697909366` and CodeQL `34697909120` passed, including Vercel access/data readiness. It binds the verified Odds API provider-event and normalized quote-row commitments plus explicit model generation time into every new current-model prediction. The ledger rejects new and excludes legacy `empirical-v2` rows without complete shaped evidence from performance metrics. Focused scan/daily/ledger/PostgreSQL tests pass 41 with 7 disposable-database skips; focused ESPN-history/backtest tests pass 13; full backend passes 554 with 24 disposable-database skips and 7 known pandas warnings; frontend passes 22 tests. No provider call/order.
- PR63 merged at `3a46e3c` after exact-head CI `34697343980` and CodeQL `34697342777` passed on `1d01b83`. Protected master CI/deployment `34697420971` and CodeQL `34697420558` passed, including Vercel access/data readiness. It pins every scheduled published performance snapshot to the live scanner's `empirical-v2` model cohort and shows that cohort in the dashboard. This prevents mixed-version selection/calibration/ROI when future model versions coexist. Focused daily/scan/ledger tests pass40; full backend553passes with24disposable-DB skips and7known pandas warnings; frontend22tests, TypeScript and production build pass. No provider call/order.
- PR62 merged at `94b9f86` after exact-head CI `34696844682` and CodeQL `34696843200` passed on `d1c7c5e`. Protected master CI/deployment `34696915433` and CodeQL `34696915144` passed, including Vercel access/data readiness. It makes green paid-scan health depend on complete model coverage for every quoted selection. It reports quotes, selections, model requests and usable estimates separately; unresolved identities or missing estimates degrade the run and public prop screen, while quote coverage survives a later model timeout. The frontend exposes modeled selections. Focused scan/daily tests pass32; full backend553passes with24disposable-DB skips and7known pandas warnings; frontend22tests, TypeScript and production build pass. No paid call/order.
- PR61 merged at `3c02916` after exact-head CI `34696277000` and CodeQL `34696275636` passed on `34e7e52`. Protected master CI/deployment `34696345586` and CodeQL `34696345571` passed, including Vercel access/data readiness. It resolves each distinct player identity once per paid event and evaluates independent selection graphs with a fixed concurrency limit of8 against the10-connection pool. `asyncio.gather` preserves selection order; ledger decisions are written only after every graph state returns without error, preventing partial event decisions. Coverage adds unique/resolved players, model requests and the concurrency limit. A barrier test proves opposite selections overlap through the real `evaluate_event`/LangGraph path; focused scan tests pass12, and full backend550passes with24disposable-DB skips and7known pandas warnings. No paid call/order.
- PR60 merged at `87a5eac` after corrected exact-head CI `34695779053` and CodeQL `34695776939` passed on `a5d6a0c`, including real PostgreSQL migration/graph writes. Protected master CI/deployment `34695848525` and CodeQL `34695848147` passed, including migration0019, Vercel access and data readiness. It binds every new normalized sportsbook prop quote to canonical provider-event and reproducible quote-row SHA-256 commitments, verifies the row hash before DB I/O, retains legacy rows, and reports source-committed coverage. Full backend550passed/24skipped; no provider call/order.
- PR59 merged at `1f6b95d` after exact-head CI `34694937686` and CodeQL `34694936952` passed on `cf3faf8`. Protected master CI/deployment `34695047078` and CodeQL `34695046678` passed, including Vercel access/data readiness. It conservatively classifies exact archived Kalshi prop rules into a compact worker profile, exposes sanitized settlement blockers and counts classified terms without claiming equivalence or profit. A fresh real unpublished NFL capture classified all3,081linked/resolved quotes with matching rule commitments; the compact3,305,438-byte handoff contained no raw rules. Focused backend49tests; full backend549tests pass with24disposable-DB skips; frontend21tests, TypeScript and production build pass.
- PR58 merged at `eef75c2` after exact-head CI `34694102890` and CodeQL `34694102107` passed on `bc3fa08`. Protected master CI/deployment `34694180285` and CodeQL `34694180010` passed, including Vercel access/data readiness. It retains exact primary and secondary Kalshi prop settlement rules in immutable local/workflow archives while the hosted worker handoff keeps only their SHA-256 commitment and normalized quote evidence. A real unpublished public NFL capture observed15games, all4series and3,081linked/resolved quotes with complete discovery; all3,081 archived rule hashes reproduced. The sampled rules define an active-player/no-snap fair-market-price state, while overtime and stat-correction handling are absent, so settlement equivalence remains unverified. Focused inventory/handoff/cross-venue tests pass42; full backend548tests pass with24disposable-DB skips.
- PR57 merged at `dac8578` after exact-head CI `34693616362` and CodeQL `34693615461` passed on `5fb3070`. Protected master CI/deployment `34693683899` and CodeQL `34693683473` passed, including Vercel access/data readiness. It removes the optional context agent's dead ESPN moneyline fallback. A live read-only NFL scoreboard probe for 2026-09-13 found 13 events and 13 odds entries, but the adapter parsed zero two-sided prices and ESPN supplied zero provider quote timestamps; the adapter also omitted the available event start. Its normalized output therefore could never pass the existing exact identity, start-time and freshness gates. Odds API h2h failures now remain explicitly unavailable while injury and other context collection continues. Focused context/graph tests pass35; full backend548tests pass with24disposable-DB skips.
- PR56 merged at `a3e8c4f` after exact-head CI `34693156869` and CodeQL `34693156031` passed on `57a968a`. Protected master CI/deployment `34693226716` and CodeQL `34693226639` passed, including Vercel access/data readiness. It separates gross-positive Kalshi/sportsbook screens from fee-model coverage and direct/non-direct modeled combined costs below one dollar. Event-level and aggregated scan coverage recompute counts from validated fee scenarios; the public transformer independently verifies supplied counts, derives backward-compatible zero counts for legacy snapshots, and exposes captured versus currently fresh counts. These remain cost coverage, not profit/arbitrage claims. Full backend547tests pass with24disposable-DB skips; frontend20tests, TypeScript and production build pass.
- PR55 merged at07d8b7e after exact-head CI34692667920 and CodeQL34692666670 passed on41a1dae. Protected master CI/deployment34692739144 and CodeQL34692739002 passed, including Vercel access/data readiness. It removes the false priced-prop fallback chain from the optional LangGraph context agent and legacy scanner. A real ESPN NFL probe for2026-09-13 returned13games and roughly537-570prop records per sampled game across179-190pages, but records contained projection targets and no prices; the old reader would paginate every page and still emit no quotes. The coded DraftKings endpoint returned HTTP403 and its adapter lacked exact cross-provider start/team identity. On Odds API prop failure, the agent now makes neither fallback, database connection nor snapshot write; it preserves other context and returns missing props. Deleted both unused adapters and their inferred side-by-record-order logic, reducing production source by442lines. Focused37tests pass with2DB skips; full backend547tests pass with24disposable-DB skips; frontend20tests, TypeScript and production build pass.
- PR54 merged atc468210 after exact-head CI34691973568 and CodeQL34691972176 passed onacfd0d1. Protected master CI/deployment34692038053 and CodeQL34692037674 passed, including Vercel access/data readiness. It captures public Kalshi series/event fee terms before prop quotes with eight-request concurrency, minimal normalized evidence and raw-response hashes. Missing fees preserve gross price evidence and are counted separately. Internal handoff version2 lets exact Kalshi/sportsbook prop screens reproduce direct/non-direct one-contract taker costs only when terms are complete, unambiguous, observed before the quote, fresh, not actively waived and displayed size is at least one. The public transformer exposes only compact modeled fee/combined costs; profit, settlement equivalence and execution readiness remain unavailable/false. Focused backend49 tests and full backend548tests pass with24disposable-DB skips; frontend20 tests, TypeScript and production build pass. A real read-only unpublished NFL capture observed15games, all4series,3,073quotes,56/56fee contexts,0fee failures and5,940one-contract top-of-book legs under captured `quadratic:1` terms; archive/handoff hashes and all fee calculations reproduced. This is cost-plumbing evidence, not an edge, fill or profit claim.
- PUBLIC REPOSITORY INCIDENT (2026-09-11): exact configured ODDS_API_KEY found in SESSION_2026_03_27.md, introduced at c3b823f; note falsely suggested expired/invalid. Current file redacted but old Git history still exposes it. User asked to revoke/regenerate and update local .env + GitHub secret; rotation NOT verified. Do not make paid-provider calls or enable full scheduled collection until replacement is verified. Public-only collection is separately verified and enabled below. Never print the old/new values. Private audit evidence lives in ignored .local/security-audit/; SECURITY.md records scope without credentials. No history force-push or protection bypass is authorized.
- Security audit: default Gitleaks 8.30.1 found no secrets across 345 public commits. Exact comparison of 1,072 file objects found one exposed credential. All 106 retained workflow logs, one artifact and repository comments were scanned without another configured-secret match. Extended provider-hex-key rule independently reproduces the historical exposure. CI scans the full tracked tree plus each incoming commit with redaction and a checksum-pinned binary; deployment depends on it. Removed tracked generated signals_cache.json (backup in ignored .local/). Do not claim the entire history is clean or rotation complete.

- PR53 branch feat/sportsbook-prop-screening adds direct book-to-book screens from the same complete archived quote batch. It requires distinct books, exact event/player/stat/half-line, actual payout-derived cost, observations no older than5minutes and skew no more than30seconds. Output remains unverified/non-executable with no profit fields; DNP/void/stat rules, limits and fills remain blockers. The public prop API validates both comparison kinds, independently reproduces sportsbook costs/skew, separates captured from still-fresh subtype counts, and strips stale rows. Focused tests initially exposed only a test expectation that rounded recurring Decimal costs equal exact2/3; compare against the actual payout conversion instead. Exact-head validation:20focused and545full backend tests passed with24disposable-DB skips;19frontend tests, TypeScript and production build passed. PR checks remain required; no paid calls or real opportunity claims.
- PR52 merged at327b9cb after final exact-head CI34690411340 and CodeQL34690409691 passed ondce2c4b. Protected master CI/deployment34690473644 passed, including Vercel access/data readiness. Production `/api/prop-screens` returns400 for an invalid sport and an honest503/no published screens while paid collection is disabled. The merged matcher consumes the private Kalshi handoff during paid sportsbook scans, requires exact identity/freshness/skew, budgets all four NFL core prop series, writes current-scan-only snapshots and exposes a sanitized five-minute public view. Fresh public Kalshi validation covered3,073quotes/190players/15games; the one paired sportsbook leg was explicitly synthetic and established contract plumbing only, not a real edge.
- PR51 merged at883d9c8 after final exact-head CI34688858026 and CodeQL34688856711 passed on72285fd. Protected master CI/deployment34688925381 passed, including Vercel access/data readiness. The separate internal `kalshi-props:{sport}` worker snapshot includes normalized quotes/player targets plus exact milestone/team aliases and a full evidence hash; raw game/order-book/directory payloads are omitted. The public markets API uses a different fixed key. Missing Kalshi inventory overwrites the handoff with unavailable instead of silently reusing stale data. Actual read-only NFL validation:3,073quotes/190player targets/15game contexts, all team aliases nonempty,~3.24MB,execution_ready false. Focused backend46passed; full backend536passed/24skipped; frontend15passed and production build passed.
- PR50 resolves all distinct player UUIDs with Kalshi's public bulk structured-target endpoint in one request, requires the exact requested set/no cursor, and validates target type, league and team. Targets remain normalized once by UUID; quotes reference them. Any target-page mismatch degrades identity resolution without discarding observed quotes. Actual NFL verification resolved3,073/3,073 quotes,0 failures,2,876 two-sided,~3.23MB uncompressed. Focused backend40passed; full backend534passed/24skipped; frontend15passed, TypeScript and production build passed. Final head4b64b26 passed CI34688228068 and CodeQL34688226541; PR50 merged at0e9314c and protected master CI/deployment34688291217 passed. Cross-provider identity and rule equivalence remain blocked.
- PR49 added compact, hashed Kalshi prop top-of-book evidence from the existing Get Markets pages: exact milestone/event plus structured player/team UUIDs, stat series, numeric strike, separate milestone kickoff/market occurrence times, request interval, bid-complement-verified asks and displayed size. Do not equate market occurrence_datetime with kickoff; live NFL values were three hours later. Actual public NFL verification:15 games,3,073/3,073 linked structured quotes across56 events,2,878 two-sided,0 failures,~3.15MB uncompressed, four series reads; one prior sampled quote matched the full order book. Focused backend51passed; full backend532passed/24skipped; frontend15passed, TypeScript and production build passed. Final head3f1c94e passed CI34687649991 and CodeQL34687648565; PR49 merged at3e052a2 and protected master CI/deployment34687718091 passed. This is quote-history input, not cross-venue matching, depth beyond one level, fills, edge, or profit.

- Canonical repository: https://github.com/rahulp7801/Agentic-EV-analytics-platform, origin/master. All commits, CI, and deployment integration belong here.
- Make frequent verified commits. Do not add assistant co-author/contributor trailers. Keep this file current.
- All changes must use a feature branch and PR; merge only after required checks pass. Never push directly to master or bypass its protection. Master requires up-to-date backend/frontend/PostgreSQL and all three CodeQL checks, including for admins; no force pushes/deletion. A PR is required but no second human approval is required (single-owner workflow).
- User priorities: working model/LangGraph, accurate metrics, security, GitHub CI/CD, Vercel, NFL/NBA season readiness. Visual redesign is deferred.
- Added scope: Kalshi NFL/NBA market research and arbitrage detection, first Kalshi versus sportsbooks, then related Kalshi contracts. Sportsbook arbitrage and PrizePicks remain in scope. User prioritizes a sturdy agentic software platform; backtesting is supporting validation, not the whole product. Production key ID/file are configured; moved root kalshi_key.txt to ignored .local/secrets/kalshi-production.pem and updated .env. Demo credentials not supplied yet. Four KALSHI_* ID/path settings are accepted but no integration/order execution exists yet. Do not place live orders without separate explicit authorization. Fees, executable depth, settlement equivalence and partial-fill exposure must be validated before claiming arbitrage.
- User explicitly reaffirmed: data analysis/backtesting and a profitable comparison dashboard only; DO NOT execute orders. Read-only Kalshi integration is implemented. Keep ingestion, evidence review, scenario analysis, and order execution separate; the last remains absent.
- PrizePicks' official DNP/Reboot rules remove affected projections and can revert whole-entry payout tiers; sport-specific exceptions and case-by-case states exist. Sportsbook participation/void rules vary by operator and jurisdiction. A quote provider key alone cannot prove settlement equivalence. Future equivalence evidence must be bound to the user's operator account, jurisdiction, entry type and observed quote; until then the existing cross-venue settlement gate stays closed.
- Stay grounded in executed code. Never invent source data, model confidence, settled outcomes, performance, service health, or successful deployment.
- Keep implementation details out of product-facing messages; retain actionable diagnostic detail in developer documentation without credentials.
- Preserve parameterized SQL, validated inputs, stale-data gates, and exposure limits. Never trade security/correctness for a shorter implementation.

## Runtime and development

- PR42 merged as6b4999e after all exact-head CI/CodeQL checks passed on46a7c3d; protected-master CI/deployment34681274779 then passed, including Vercel readiness. The scheduled scanner now archives complete quotes before evaluation in production code, but full paid collection remains disabled, so no new quote rows or performance claim followed from deployment.
- PR43 merged as70b8d49 after every exact-head CI/CodeQL check passed onc8f482a; protected-master CI/deployment34682567252 then passed, including Vercel. Automatic supported-prop grading now runs after refreshed history and schedule collection and before new daily scans. It requires exact sport/player ID/home team/away team/Eastern date, exactly one observed stat row, and ESPN `completed=true`; DNP, unsupported, missing, nonfinal, team-mismatched and ambiguous evidence stays pending. Both sides and pushes use the recorded line. Settlement provenance records source, a SHA-256 reference over exact final-game/player/prop/stat evidence, observation time and actual stat; manual CLI imports hash their input file and manual outcomes are not overwritten, while automatic outcomes may reflect later stat corrections. This grades observed stats for backtesting; it does not prove bookmaker account settlement or cross-venue rule equivalence.
- PR44 merged as2261aca after exact-head CI34683395383 and CodeQL34683393999 passed onbc85e36; protected-master CI/deployment34683470497 then passed, including Vercel access/data readiness. It keeps the published schedule at yesterday/today/tomorrow while daily/public_daily use separate bounded evidence for Eastern dates -7 through -2. Both collectors share one instant to avoid an ET-midnight gap; any historical failure degrades settlement/the overall run without making a healthy current schedule look unavailable. Monitor modes do not add requests. Automatic correction rows are reconsidered only while their date is in the supplied window; unresolved rows remain pending. Recovery beyond seven days still requires a backfill/manual process. Validation:39focused and514full Python tests passed with23disposable-DB skips;15frontend tests, TypeScript and production build passed. The427-file tracked/new source tree had0exact configured-credential matches and passed redacted Gitleaks.
- PR45 branch fix/stat-source-provenance repairs the misleading `espn_final_stats` label. NBA.com and nflverse ingestion now store canonical batch commitments plus reproducible typed settlement-row hashes; ESPN fallback retains its raw-response hash and adds the same row hash. Settlement recomputes the row hash, requires the correct provider/two hashes/aware postgame nonfuture observation, and binds those fields plus the stat-row and final ESPN schedule identity into a new `observed_final_stats` reference. Missing/tampered legacy provenance stays pending. Metrics explicitly leave legacy `espn_final_stats` outcomes unscored until a valid correction overwrites them. Migration0016 preserves old rows but uses NOT VALID constraints to require complete evidence on every future write and removes the NBA provider default. Canonical nba_api/nflverse batch commitments bind normalized ingested records; they do not authenticate provider origin or reproduce unavailable upstream raw bytes. Initial PR CI34684375246 exposed a PostgreSQL fixture whose nflverse row omitted whole rushing/receiving columns. NFL ingestion now records provider-absent tracked stats as NULL, preserving unknown evidence instead of inventing zero. Corrected-head CI34684546159 and CodeQL34684545103 passed every gate including the real PostgreSQL upgrade/integration suite; local focused tests pass18 and the full suite passes525 with24disposable-DB skips. The431-file pre-fix tracked/new tree had0exact configured-credential matches and passed redacted Gitleaks. Production migration and hosted refresh remain pending until the final documentation head passes, merges, and deploys.
- Production rollout finding after PR45: migration0016 advanced successfully and preserved52,957NBA/54,556NFL legacy rows, with0rows falsely marked as verified. Valid owner and restricted-worker writes succeeded in rollback-only probes. However, direct incomplete probes proved PostgreSQL CHECK's NULL result was accepted, so the constraints did not actually require every evidence field. No public_daily refresh was run. PR46 adds forward migration0017 with explicit IS NOT NULL predicates and regression coverage for a missing record hash. Exact-head CI34685069561 and CodeQL34685068315 passed, including the disposable PostgreSQL upgrade/behavior suite; the local full suite passes525tests with24disposable-DB skips. Final documentation-head checks, merge, deployment and production application remain required before refresh.
- PR46 merged as76f5cac; protected-master CI/deployment34685216570 passed and migration0017 advanced production. Both incomplete NBA/NFL rollback probes are rejected, complete owner/worker probes succeed, and the legacy counts remain unchanged. First post-migration public_daily34685329586 failed closed before NFL stat writes/settlement because current real nflverse data contains one legitimate negative rushing-yard row while the record hasher rejected all negative stats; NBA fallback, schedules, public Kalshi and evidence scanning completed, but the overall run correctly exited2. The settlement constraint also rejects a legitimate negative actual value. PR47 permits signed NFL yardage, keeps NBA stats and NFL season/week nonnegative, and migration0018 permits finite signed actuals while rejecting NaN/infinities even on pending rows. Real current-data dry ingestion produced134rows/134reproducible hashes including1negative rushing row;34focused and527full local tests pass with24disposable-DB skips. Exact-head CI34685743609 and CodeQL34685742855 passed including the signed-outcome PostgreSQL behavior test. Final documentation-head review, merge, deploy, production migration and refresh remain required.
- PR47 merged as71984fe; protected-master CI/deployment34685904507 passed and migration0018 advanced production. Rollback-only probes preserved0prediction rows, accepted finite -2 actuals and rejected NaN for both owner and restricted worker. A second public_daily refresh still requires explicit user approval after automatic approval review rejected the broad production write/settlement blast radius; do not bypass it.
- Scheduled NBA prop scans constructed create_graph directly without the NBA context producer, so the claimed rest/home/opponent path was never active. Even if wired, Odds API supplies full team names while gamelogs and PropParams require abbreviations; comparing/passing those identities would misclassify venue and make conditional history empty. PR48 wires the real producer, normalizes exact current NBA names to canonical abbreviations inside that node for all callers, and fails closed on unknown team identities while retaining provider full names in archived predictions for final-schedule matching. Initial CI34686203558 rejected the real-PostgreSQL fixture's fake Home/Away names. Corrected CI34686336714 proved the path reached opponent LAL/home with40conditional rows and a0.6estimate, then exposed an assertion against Ledger's intentionally reduced projection. The corrected exact head passed CI34686515294 and CodeQL34686513188, including the real conditional PostgreSQL path;23focused and529full local tests passed with24disposable-DB skips. PR48 merged at a338e79 and protected master CI/deployment34686587265 passed.
- Settlement validation:45focused tests and504full Python tests passed with23disposable-PostgreSQL skips;15frontend tests, TypeScript and production build passed. Alembic rendered upgrades through0015. The final427-file tracked/new source tree had0exact configured-credential matches and passed redacted Gitleaks. PR PostgreSQL CI passed preservation, constraint, restricted-role and real settlement SQL checks. Migration0015 was then applied to Supabase: revision advanced,0legacy rows changed, an invalid provenance-free update was rejected and rolled back, and the restricted worker's settlement-only UPDATE grant succeeded. Manual public_daily34682691597 ran the merged commit with no paid credential, passed evidence/Gitleaks gates, and persisted an observed report: both histories complete, both public Kalshi scans observed, both settlement stages complete with0candidates. Restricted DB aggregates and production `/api/metrics` matched exactly at0predictions/0settlements/0pending with null ROI/Brier/log loss/CLV. This proves honest empty-state operation, not predictive performance or profitability.
- PR41 merged as20d10bb after all exact-head CI/CodeQL checks passed ond4f3d0c; post-merge CI/deployment34680692046 succeeded. Migration0014 was then applied to production with the owner migration role: revision advanced, all17,996legacy rows were preserved, the NOT VALID constraint remains unvalidated against that legacy evidence, and an invalid new-row probe was rejected and rolled back. Do not claim the legacy data became replayable.
- The maintained scheduled scanner now atomically archives every complete parsed provider quote before LangGraph evaluation. It stores exact provider event/player/book/market/line/price/probability/side/quote-time/start-time values; validation or database failure prevents model output from that event. This creates forward evidence only after full collection is safely enabled. `DATA_PIPELINE_ENABLED` remains off and the exposed Odds key must not be used, so no current sportsbook quote dataset or performance result exists yet.
- PR40 merged as2170f12 after all exact-head CI/CodeQL checks passed on3848ae0; post-merge CI/deployment34680003498 succeeded. Hosted public_monitor34680093769 then produced NFL3,073linked props/56events/15games and NBA0/0/0 with complete series discovery; both artifact replays and production `/api/markets` matched exactly (.local/hosted-kalshi-proof-34680093769.json). This is production coverage evidence, not priced prop arb.
- Hosted `player_prop_snapshots` audit found17,996NBA rows from2026-03-27..30; every row lacks either required event timing/identity and every row lacks a valid Over/Under side. Current database replay already excludes all of them. Preserve them as legacy evidence; do not calculate ROI/CLV/calibration or match them to Kalshi. New application persistence validation and migration0014's PostgreSQL CHECK NOT VALID require a complete pregame quote for future rows without rewriting old data. Nineteen invalid-write regressions failed before the app guard;30focused and488full Python tests passed with21disposable-DB skips. PostgreSQL CI passed the upgrade and enforcement tests before the production application recorded above.
- Live Kalshi core-prop inventory now uses at most three 1,000-market pages per series and exact structured milestone `related_event_tickers`; no ticker parsing or fuzzy game/player matching. Invalid/ambiguous prop links degrade only this inventory and cannot discard valid game observations. Periodic snapshots retain per-series response hashes/counts/completeness, not thousands of raw markets. Actual public NFL verification at2026-09-12T06:55Z exhausted four series:3,073open markets/56prop events, all linked to15upcoming games;30existing game comparisons replayed exactly and0positive gross gaps. NBA points/rebounds/assists were valid empty offseason series; its integrated archive also replayed exactly. Validation:44focused backend tests,469full Python tests/20disposable-DB skips,15frontend tests, TypeScript and production build passed;423current source files had0exact configured-credential matches and passed redacted Gitleaks. One concurrent full-suite run hit the existing30-second native-solver stress timeout while an overly broad local scan was active; that guard passed alone and the isolated full rerun passed. This is coverage evidence only: no prop quote time, depth, settlement equivalence, edge, ROI or execution claim.
- Default LangGraph safety: create_graph() formerly returned hardcoded62% QuantResult/7% EVSignal from legacy routes when real runtime nodes were omitted; unknown request types also ended with error=None while logging the raw input. All unconfigured quant/context/arbitrage/kinematic/NFL-prop/NBA-prop routes now fail closed with stable *_not_configured errors, clear prior signals, and preserve the first pipeline error. Unknown route values are replaced by "unknown" before return/logging. Real injected scanner/walk-forward and market-analysis routes are unchanged. Removed the unused frontend/lib/mockData.ts containing fabricated signal/profit/dashboard examples.
- Fail-closed validation: nine new regressions all failed before the fix and pass afterward;50focused graph/model tests and465full-suite tests passed (20disposable-DB skips). Frontend15tests, TypeScript and production build passed. Restricted-DB holdouts reproduced every forecast/hash and prior metrics (NFL16cases/Brier0.2373145728; NBA192/Brier0.0872092543); ROI/CLV remain null. Current422 source files passed exact configured-secret comparison and redacted Gitleaks.
- PR37 merged atefd56c1 after CI34676883078 and CodeQL34676881689 passed on2faf2f8; post-merge CI/deployment34677918955 passed, including worker-image and production access/readiness checks. Automatic public-monitor run34677915033 at06:21UTC succeeded after a roughly five-hour gap: NFL15games/30comparisons/90depth cases, NBA0, exact replay and production match (.local/hosted-kalshi-proof-34677915033.json). This additional success still does not establish reliable30-minute cadence.
- PR35 merged ate7de379 after CI34676771408 and all CodeQL34676769613 checks passed onf79a27c. Required worker-image verification and all prior branch protections remain enabled. No dedicated worker host has been activated.
- Hosted CI recovery: GitHub-managed default CodeQL runs cannot be retried with gh run rerun (including individual jobs). PR35 final-head CI34676492067 passed all tests/image/security gates, but CodeQL34676490671 was cancelled after the Python runner stalled during checkout; the next documentation commit retriggers analysis. Never bypass required analysis based on an earlier head's pass.
- Provider diagnostics: Ball Don't Lie's three optional helpers, Sleeper injuries and the ESPN prop-event fallback now log error classes only. Five harmless-canary regressions failed before the change and pass afterward; partial ESPN batches preserve successful events. Validation:35focused tests passed (2DB skips),456full-suite tests passed (20DB skips), and422current source files passed exact configured-secret matching plus redacted Gitleaks. This removes raw exception text at these boundaries; it does not verify provider availability or clean the exposed historical credential.
- Restricted-DB holdouts rerun after provider diagnostic changes reproduced all forecasts, source hashes and baseline metrics: NFL16cases/Brier0.2373145728, NBA192cases/Brier0.0872092543 (.local/calibration-baseline-proof.json). Unpriced research only; ROI/CLV remain null and no model tuning was performed.
- Portable Docker worker packaging uses the existing daily/public CLI and actual LangGraph path; no custom scheduler or paid host activated. Linux CI34676247046 passed on6373663, including the actual context exclusion check, image build and non-root/read-only/no-network graph/native solver test; all backend/frontend/PostgreSQL/secrets and CodeQL34676244763 checks passed. Docker daemon is not running locally; this is hosted CI evidence. Keep evidence on private durable storage when deploying. The image itself does not solve scheduling/retention; host choice/cost approval and actual hosted cutover remain outstanding (docs/worker-deployment.md).
- Initial worker-image CI correctly rejected context exclusions before building. Docker directory negations can re-include descendants; reset each admitted directory's contents before admitting Python files/public CA. Nested-package and certificate-directory canaries now pass. Master additionally requires worker-image from GitHub Actions, preserving strict/up-to-date status and all seven existing checks; production deployment also depends on this job. Every subsequent PR head still requires all checks before merge.
- PR34 merged at0a6c197 after CI34675526033/CodeQL34675524834 passed on2ca4161; post-merge CI/deployment34675584506 passed. Manual public refresh34675625833 succeeded and NFL15games/30comparisons/90depth cases plus NBA0 matched production and replay (.local/hosted-kalshi-proof-34675625833.json). Manual refresh does not fix or prove automatic cadence.

- Model failure boundaries: NFL/NBA prop agents previously echoed validation/query exception text into logs and graph state, and validation failure retained an earlier probability. Invalid Decimal input escaped their ValidationError-only handler. General quant and kinematic agents also retained raw errors/tracebacks; the NGS availability query was outside its handler. Failures now return stable error codes, log exception classes only, clear failed estimates, and suppress prior signal outputs. The router clears old signals on a pre-existing error without logging its text. Regression tests run actual graph/query-builder/executor paths with a faulting database and demonstrate that an otherwise signal-producing prior estimate cannot survive failed validation.
- Failure-boundary validation:53focused tests and451full-suite tests passed (20disposable-DB skips locally). Real restricted-DB holdouts reproduced every forecast, dataset hash and metric: NFL16cases/Brier0.2373145728; NBA192/Brier0.0872092543; no tuning/paid calls/ROI. Current417source files passed exact configured-secret comparison and extended redacted Gitleaks. Scope is these model/router failure paths, not a claim that all application diagnostics or historical Git objects are clean.
- At2026-09-12T05:24UTC the latest automatic run was34664827925 at01:26UTC, successful; prior successful automatic run was23:29UTC. This does NOT establish reliable30-minute cadence; schedule delays/gaps remain operationally material. Keep freshness gates and do not advertise continuous coverage. Automatic daily refresh still not verified.
- PR33 merged at305328b after CI34658795275/CodeQL34658792819 passed on80563f3; post-merge CI/deployment34658893446 passed including production access/readiness checks. History captures contain47NFL/56NBA real minute bars, with exact replay and unchanged file hashes (.local/kalshi-history/collector-proof.json). ROI remains null.

- Added bounded public Kalshi prop-history collection and offline replay (docs/kalshi-history.md). Explicit1..20selected tickers on one past Eastern date; no complete-universe or representative-sample claim. Exact milestone event/team and structured-player identity required; use scheduled start minus15minutes, not occurrence_datetime. Last one-minute bid/ask bar must be within60seconds; reject malformed/stale/missing/boundary quotes, in-play/closed listings and inconsistent/unsupported settlements. Never use current target player_stats as historical actuals, trade prices as missing quotes, or volume/open interest as executable depth. SHA-256 covers raw response evidence; retain exclusions and no fees/fills/ROI claims. Local captures remain ignored and outside public artifact whitelist.
- Actual free API verification: NFL Sam Darnold passing yards on2026-02-08 and NBA Victor Wembanyama points on2026-06-13 each produced1usable contract with exact replay; recent Drew Lock250passing-yards listing opened after cutoff and correctly yielded0records/incomplete. Evidence .local/kalshi-history/*-v1.json. Historical NFL milestone remains status scheduled despite finalized market; actual kickoff is unverified. These are hand-selected ingestion checks, not profitable backtests or cross-vendor outcome links.
- History-collector validation:31focused tests passed; full suite442passed/20disposable-DB skips. Current416tracked/new source files passed exact configured-credential matching and extended redacted Gitleaks. Historical credential exposure is still unresolved; this clean-tree check does not supersede the incident above.
- Actual archived prop rules_secondary: active NFL player with no snap / NBA player who never enters settles to last fair market price before start, not necessarily refund. A penalty-nullified NFL snap counts as participation. Preserve explicit participation scenarios before comparing sportsbook payouts; floor_strike equality is insufficient for arbitrage. Generic FOOTBALLENTITYSTAT/BASKETBALLENTITYSTAT PDFs could not be retrieved in this pass, so complete settlement-rule equivalence remains unverified.
- PR32 merged ateda315c after CI34657025056/CodeQL34657023157 passed on22c59d1; post-merge CI/deployment34657183470 passed. First post-enable automatic public-data run34658279136 (event schedule,23:29:56UTC) completed successfully, including artifact secret scanning/upload. Its NFL15games/30comparisons/90depth scenarios and NBA0 matched production and exact offline replay (.local/hosted-kalshi-proof-34658279136.json). Earlier notes about unverified cron are historical; this proves one automatic public-monitor run, not full paid-provider readiness or an automatic daily refresh.

- Market specialists isolate each candidate's unexpected analysis failure, preserving other candidates within the same specialist and across venues. Incomplete/invalid optimizer results are technical failures, distinct from deliberate fee/capacity/coverage blocks. Aggregate reports expose complete/degraded and failed_count; CLI retains partial reports and exits2 for failures. Graph diagnostics go to stderr, leaving stdout machine-readable JSON. Regression tests fault the actual solver boundary while other candidates use real HiGHS; synthetic fixture edges are arithmetic evidence only, never market returns.
- Market-agent change validation:32focused graph/CLI tests passed, then415Python tests passed with20disposable-DB skips. Actual CLI tests retain the partial report, parse clean JSON stdout, reject exception text in report/stdout/stderr, and distinguish blocked review from technical failure. No new dependencies or order path.
- PR31 merged at 41cc84f after CI34656293604/CodeQL34656293495 passed on ce621e7; post-merge CI/deployment34656425609 passed. Recomputed actual hosted ledger aggregates published via restricted worker and matched DB plus production /api/metrics exactly (.local/calibration-publish-proof.json). Both cohorts have0eligible predictions/settlements; all Brier/benchmark scores remain null. Research holdouts were NOT published as live performance. Automatic cron remains unverified; last schedule event still the pre-enable skipped21:11Z run.

- Shared calibration_metrics now reports fixed 0%/50%/100% Brier benchmarks and calibration_positive_count on exactly the scored decided/model-covered cohort. BacktestReport/ledger serialization, unpriced walkforward and dashboard use the same fields. Exclude missing forecasts, pushes, voids and pending outcomes; no sample means null benchmark scores. Do not select or fit a baseline using held-out labels or present these as priced strategy returns.
- Actual restricted-DB LangGraph holdouts rerun with new metrics reproduced every prior forecast, Brier/logloss and dataset hash. NFL16scored/11Over: model0.2373146 versus fixed baselines0.6875/0.25/0.3125. NBA192scored/24Over: model0.0872093 versus0.125/0.25/0.875. Proof .local/calibration-baseline-proof.json, reports .local/history/*-fixed-baselines.json. ROI/CLV remain null; no model tuning or paid calls. Forty-two relevant Python tests,15frontend tests,TypeScript and production build passed; no browser surface was available for visual verification.
- PR30 merged at9de4f6f after CI34655852461/CodeQL34655851054 passed on583bd85; post-merge CI/deployment34655947824 passed. Automatic cron still unverified at22:56Z. Keep this separate from verified manual public runs.
- Manual scan CLI now exits2 for any non-complete report, including budget-skipped games, while valid empty slates still succeed. The actual scanner/provider-transport/ledger regression tests cover budget exhaustion, empty discovery and a failed league, preserve published reports, and close the database pool. Twenty-four scan/daily tests passed; no paid-provider calls in this verification.
- PR29 merged at1892158 after CI34655506286/CodeQL34655504970 passed on1939159. Actual hosted public_monitor34655651675 passed after merge; NFL15games/30comparisons/90depth scenarios and NBA0 matched production and replay exactly (.local/hosted-kalshi-proof-34655651675.json). Post-merge deployment34655643854 passed. Full paid monitoring remains disabled and its real-provider readiness is not established by the public run.
- Full monitor now refreshes sportsbook/Kalshi/PrizePicks market observations before props, using the existing shared daily/rolling credit ledger. Previously it intentionally skipped markets entirely, leaving arbitrage snapshots at daily frequency. A stale history blocks props but does not suppress market collection. Full monitor archives use the same staged credential/Gitleaks gate as daily/public modes. This does not enable DATA_PIPELINE_ENABLED or bypass key-rotation/readiness requirements.
- Forty focused daily/market/artifact tests passed. The actual graph and sportsbook collector with a one-credit fixture ledger issue exactly one mocked request, retain independent public sources for both leagues, report budget exhaustion, and leave no credit for props. Real full-monitor preview2026-09-11T22:46Z read restricted hosted refresh snapshots, observed15NFL Kalshi games/NBA0 and complete ESPN schedules; unavailable sportsbooks/PrizePicks and failed props made it degraded. Paid credential explicitly empty, request hook rejected any paid-provider host, publications captured locally (.local/full-monitor-preview-proof.json). No production writes, paid requests, or orders. This is not a successful full-provider readiness test.
- PR28 merged atd5564e3 after CI34654830693/CodeQL34654829139 passed onaf1afaf; post-merge CI34654971201 including deployment/access checks passed. Automatic cron still unverified at22:45Z; last recorded schedule event was the pre-enable skipped21:11Z run. Do not confuse manual hosted success with cron evidence.

- PR27 merged atb6e521b after required CI34654470713/CodeQL34654468194 passed ond26e551. Post-merge CI34654582736/deploy passed. Actual hosted public_monitor34654591594 captured all15discovered NFLgames/30comparisons/90depth-size scenarios with0failures; NBA0games. Archive/production/replay identical (.local/hosted-kalshi-proof-34654591594.json). Automatic cron still unverified; successful manual runs are not scheduler proof.
- Replay now exits2 when computed comparisons differ from the archived summary; still emits its structured mismatch report and never republishes historical quotes. Four regression cases cover match/mismatch/corrupt evidence/forbidden publishing, with20market-watch tests passing. Actual old04:03UTC archive exits2 and new22:28UTC NFLarchive exits0, both byte hashes unchanged (.local/replay-exit-proof.json). The older mismatch predates PR27 and affects sportsbook selection (212saved comparisons versus15current), not the new Kalshi collector; do not rewrite history to conceal it or claim the legacy report reproducible.
- Kalshi daily/watch default now20games (maximum40), with at most three500-milestone pages and three active markets per game. Do not assume discovery sort order; paginate, de-duplicate and reject conflicting event/team/start identities. Validate returned event/market IDs. Event/market failures retain other observations and make the source degraded. Coverage distinguishes discovered/attempted/inspected/quoted/failed games; any bound or failure remains partial. Quote freshness uses the oldest comparison leg, not collection completion time.
- Actual public-only local collection2026-09-11 covered all15discovered NFL games in the seven-day window,30comparisons,0failures/omitted markets; NBA0games. No paid calls or publishing. This proves broader observation coverage, not executable arbitrage. Focused collector/reader/daily tests39passed; frontend15tests,TypeScript and production build passed. Failure fixtures exercise the actual HTTP reader without credentials and reject mismatched market responses.
- Full local suite401passed/20disposable-DB skips. Both new captures replay exactly; all16checked archives produce identical comparisons with current code and pre-change master. One old04:03UTC capture already mismatches its saved summary before this change; retain it and investigate separately. Evidence .local/kalshi-coverage-proof.json. Current tracked tree plus new files passed exact configured-credential matching and extended redacted Gitleaks (413files); exclude documented key-file paths from credential values, but still scan actual PEM content. Historical exposed Odds key remains unresolved.
- PR26 merged at ef02247 after CI34652367337/CodeQL34652365378 passed onfa9c4ed; production CI34652553808/deploy passed. Actual hosted public_monitor34652564697 passed the Node24 artifact upload; both archives replayed exactly and matched production (.local/hosted-kalshi-proof-34652564697.json).
- After public ingestion, unchanged-model real holdouts reproduced every prior forecast and metric: NFL16evaluated/Brier0.2373145728220625/logloss0.6686442101775423; NBA192evaluated/Brier0.0872092542848177/logloss0.434929020999419. Research thresholds remain unpriced; ROI/CLV null. Proof .local/public-pipeline-holdouts.json. No model tuning or profitability claim.

- PR25 merged at7d2da2a after final CI34651771812/CodeQL34651768395 passed on217029d; post-merge CI34651887456/deploy passed. Actual hosted public_daily34651891673 and public_monitor34651898159 passed collection, credential staging, secret scan and upload. Logs proved the paid key absent, both pipeline reports read back exactly, and production games/markets/gamelogs returned200 for both leagues (.local/hosted-public-proof.json). Hosted NBA daily used the ESPN fallback; local primary success does not imply stats.nba.com works from GitHub.
- PUBLIC_DATA_PIPELINE_ENABLED=true was set and read back at2026-09-11T22:02:39Z after both hosted modes passed. Workflow state active; DATA_PIPELINE_ENABLED still unset. Public daily refresh13:17UTC and public monitor:07/:37 now configured; the first automatic schedule run is NOT yet verified. Paid collection/prop scans remain off pending exposed-key rotation and full readiness. Do not describe periodic sampled snapshots as continuous/live arbitrage coverage.
- Actual hosted artifact upload warned that the v4 action targets deprecated Node20. Updated to official upload-artifact v7.0.1 commit043fb46d1a93c77aae656e7c1c64a875d1fc6a0a, whose action.yml uses Node24 and preserves archive=true/compression6/overwrite=false defaults. Keep scanning before upload and verify an actual hosted artifact after merging. Official current billing docs distinguish free public standard runners from shared artifact-storage allowances; measured captures approximately240KB compressed, retention90days is not a permanent archive.

- PR24 merged at78c8270 after final CI34650483590/CodeQL34650480306 passed on8f0050d; post-merge CI34650624540/deployment passed. Actual hosted watch34650629619 published schema2: NFL5sampled games/10comparisons/30depth-size cases; NBA0games. Both archived summaries and exact offline replay matched production API (.local/hosted-kalshi-proof-34650629619.json). Browser surfaces unavailable (CUA returned no apps/browsers), so no visual browser verification claimed.
- Public scheduling implementation: public_daily/public_monitor reuse the actual daily graph with fixed public Kalshi collection plus ESPN schedules, never paid credits/prop scanning/PrizePicks. Public_daily also refreshes both histories; public_monitor leaves them untouched. Status observed means requested public collection succeeded, not complete market coverage; partial sampling/unrequested sources remain explicit. Real source/refresh failures still degrade/exit2. PUBLIC_DATA_PIPELINE_ENABLED independently enables these cron modes; DATA_PIPELINE_ENABLED takes precedence and remains off pending Odds key rotation/full readiness. Do not enable the public flag before both hosted manual modes pass.
- Local restricted-worker public_daily completed2026-09-11T21:50Z. The paid-provider credential was absent. NFL refresh covered272scheduled games and the primary NBA endpoint returned26,651season rows (no fallback needed this run). Both schedules complete; Kalshi NFL5sampled games/NBA0; props/book/PrizePicks not_requested. Pipeline snapshot read back exactly (.local/public_daily-proof.json). Tests exercise the actual graph and collector, fail if paid credits are touched, retain archives, and verify failures across all requested stages. This is periodic collection, not continuous low-latency monitoring or profitability proof.
- Gitleaks generic-api-key falsely identified NFL272scheduled in the original public_daily status prose. Verified the exact match privately and rewrote the prose. .gitleaksignore excludes only fingerprint be0f51f:CLAUDE.md:generic-api-key:22, not the rule/file/commit broadly. All other incoming findings and the real historical exposure remain detectable.
- Local restricted-worker public_monitor also completed2026-09-11T21:51Z and read back exactly (.local/public_monitor-proof.json), leaving histories and props not_requested while updating both schedule/market snapshots. Nineteen focused public orchestration/artifact tests passed.

- PR23 merged at64cfa52 after CI34649374297/CodeQL34649373316 passed; production CI34649501661/deploy passed. Actual public PrizePicks attempt returned unavailable/degraded and a nonzero CLI exit without exception text. Current source remains unavailable; no access-control bypass or paid call.
- Fee depth scenarios: new taker_order_cost implements the documented per-order rounding accumulator and caps each rebate to that fill's fees; small fractional fills can accumulate more than one balance unit without enough net fee to rebate yet. taker_depth_cost consumes actual sorted unique levels, models one fill per level, and rejects insufficient/malformed depth. These are conditional simulations, NOT fee upper bounds for arbitrary fragmentation. Dashboard shows supported sizes1/10/100 with separate direct/non-direct costs; excludes sportsbook limits/FCM/funding/exceptional settlement charges and still cannot recommend execution.
- New market-watch evidence/summary schema2 adds depth scenarios; schema1 replay stays unchanged and unknown versions fail. Actual public-only NFL capture .local/market-watch/20260911T213701219486Z-9dc231b54cb14dfebbfafa7ad6df1bf6.json had4pairs across2games; all supported1/10/100 from single displayed levels in this sample. 100-contract pair costs ranged104.4909..104.4965 direct and104.50 non-direct, with no edge. New archive and both prior hosted schema1 archives replayed exactly (.local/depth-fee-proof.json). Multi-level consumption/rebate caps verified with arithmetic fixtures, not claimed observed fills.

- PR22 merged at74a12b6 after CI34648942187/CodeQL34648940570 passed. Actual hosted watch34649084090 passed credential staging, checksum-pinned scanner installation, redacted artifact scan and upload in that order. Downloaded NFL/NBA evidence replayed exactly and matched production again; proof .local/hosted-kalshi-proof-34649084090.json. No Odds API calls. This verifies the public-upload path, not removal of the old key from history.
- Market-watch CLI now exits2 if any requested provider is unavailable/budget-exhausted or a league fails, while preserving sanitized per-league output and continuing the other league. Valid empty observations and intentionally not_requested sources succeed; bounded partial sampling remains explicitly labeled rather than treated as whole-slate coverage. Daily graph already classifies incomplete collection as degraded. Provider failures must never be green workflow success.

- PR21 merged at27bd38b after CI34647983906/CodeQL34647982384 passed. Hosted public-only watch34648495637 succeeded with no ODDS_API_KEY injection: NFL5sampled games/10comparisons, all10with fee scenarios; NBA0games in queried window. Both production /api/markets responses matched archived summaries exactly, and offline comparison replay matched. Post-merge CI34648478463/deploy passed. Evidence .local/hosted-kalshi-34648495637 and .local/hosted-kalshi-proof.json. Partial NFL sampling is not whole-slate coverage or profitability.
- Public artifact gate stages only market-watch/prizepicks/nba-refresh JSON, rejects configured credentials/decoded DB passwords/URL encodings (including escaped and duplicate JSON values), then runs redacted Gitleaks. Upload requires both checks successful even after failed collection. Shared checksum-pinned installer serves CI and evidence checks. Actual hosted NFL/NBA archives passed both local gates. Exposed Odds key rotation remains unverified (GitHub secret metadata still2026-09-10T23:06:57Z); no paid calls or history rewrite authorized.

- PR20 merged at afc1c60 after final CI34647544456/CodeQL34647541474 passed on b206bc5. Local verification369Python passed/20DBskips,15frontend tests,TypeScript,production build. Active per-market fee waivers suppress generic fee scenarios. Follow-up provider selection allows public-only Kalshi collection without touching the exposed Odds API key; unrequested sources must remain explicitly not_requested, never successful empty coverage. The default daily path still requests all providers.
- Free historical Kalshi probe: /historical/cutoff reported July13,2026; old NFL Jan25 market metadata was available but full-listing hourly candles empty. Recent settled KXNFLGAME-26AUG29CHITEN-TEN returned238hourly bars with bid/ask closes via live-series candlestick endpoint. No exact kickoff or executable depth established, so this is availability evidence, not a pregame profit backtest. Keep archived and recent endpoint routing distinct and never infer game start from close_time (can lag by a day).

- PR19 merged at bc6e0f1 after CI34645970295/CodeQL34645967499 passed. Post-merge CI34646102446 and Vercel deploy passed. Actual GitHub refresh34646102471 completed both leagues and published current complete snapshots; NBA used ESPN, NFL nflverse. Current NFL2026 week1 has134rows across4teams. Paid collection stays off: exposed ODDS_API_KEY rotation and exceptional history-rewrite approval remain pending. No new permission has arrived.
- Kalshi fee follow-up: public /events/fee_changes overrides series terms and has pagination; /series/fee_changes?show_historical=true exposes scheduled transitions. Capture these before quotes. Missing/incomplete/conflicting/stale/future fee data cannot imply zero fees. Latest effective null/null event override restores series fees. Official rounding guide distinguishes direct $0.0001 balances from non-direct $0.01 balances, and rounds total balance change after six-decimal trade fees. Single-fill scenarios use the July7 quadratic formula and zero prior accumulator; do not pass these estimates as optimizer fee bounds or claim realized profit. Full-fill accumulator/FCM/exceptional-settlement accounting remains unresolved. Current public PDF browser was readable; direct download429, so no invented document hash or bypass.
- Fresh public-only Kalshi verification at2026-09-11T21:00Z:4same-contract comparisons across2NFLgames, each gross pair cost1.0100, modeled direct-account single-fill cost1.0450/non-direct1.05. Exact archived replay matched all4; no positive hedge or realized profit. Evidence under ignored .local/kalshi-payoff/. No Odds API call was made. Snapshot market occurrence_datetime differed from milestone start_date by3hours in the sample; do not treat occurrence_datetime as kickoff without documented semantics.

- PR18 merged at 2ff7b86 after final CI34645453202 and CodeQL34645449645 passed. Migration0013 applied to actual Supabase first; all52,957 prior rows retained official NBA provenance. Real local fallback and actual GitHub NBA-only refresh34645586531 passed:1,230 existing regular-season games verified, zero new finals Sept5-11,10raw public sources archived. This verifies the hosted fallback, not a profitable model or full upcoming-season coverage. Paid-provider requests remain paused pending credential rotation.
- Manual refresh CLI previously updated rows without publishing refresh status, leaving monitor blocked on an older failed run. refresh_history now shares running/failed/complete publication between the daily graph and manual refresh/backfill; exceptions expose only type and a failed league does not suppress the other league's manual refresh.
- Local history-cleanup preview is in ignored .local/security-audit/clean-history-preview.git with plan history-cleanup-plan.json. It removes the exposed key and passes the extended full-history scanner while preserving master's current source tree. Preview would change19branch heads and23GitHub PR refs; signatures/commit IDs change. User approval requested for exceptional remote rewrite; NOT granted/applied. Preview must be refreshed against latest remote heads before any approved operation. Credential revocation is still independently required.

- PR17 merged at bd09842 after all seven required checks passed (CI34644424945, CodeQL34644422971). Post-merge CI34644600616/deployment passed. Secret scan is now a required strict master check; private vulnerability reporting verified enabled. GitHub generic/non-provider scanning setting remains disabled despite API request; do not claim it enabled. Exposed Odds API credential rotation remains pending.
- NBA fallback: hosted stats.nba.com times out. New bounded ESPN collector uses sportsdataverse season-specific exact/confidence-one game/team/player crosswalks, never fuzzy suggestions. Missing player mappings may use a unique official NBA name after case/diacritic normalization; ambiguous names fail. Actual nine ESPN games from 2026-01-28 yielded 203 participants, matching all 203 existing NBA identities and counting/team/home-away stats exactly. Rounded ESPN minutes are not asserted equal to official minute precision. Evidence in ignored .local/nba-fallback/.
- Migration0013 records NBA stat provider/hash/observation time. ESPN updates cannot overwrite NBA rows; NBA refresh restores official provenance. Failed batches roll back, removed participants require reconciliation. Daily fallback checks seven days and rejects pre-window or missing recent scheduled history; it is not a full backfill replacement. Raw public sources and official name mappings are archived before writes. Rookies absent from both exact crosswalk and official history block that refresh; never invent identity. Historical crosswalk coverage does not prove the upcoming season will remain available.

- Daily orchestration (`sportsbet.daily`) uses the actual LangGraph refresh/market/prop stages. Daily history refresh and market capture run independently; props join after both, with league isolation. Monitor scans require a completed refresh within36hours; running/failed/future/old snapshots block props. Refresh success proves ingestion completed, not upstream game completeness. Persisted pipeline summaries expose degraded sources/coverage; no order path exists.
- GitHub daily cron now selects `daily`, half-hourly cron selects `monitor`; both remain disabled until DATA_PIPELINE_ENABLED=true after hosted readiness. Manual scan bypasses the refresh-age gate for maintenance. Daily captures retain public evidence artifacts90days. Shared ledger reserves paid credits atomically against daily and rolling31-UTC-date ceilings(default450); this is app-recorded usage, not the provider billing counter or other clients' use.
- Real daily graph with sportsbet_worker completed2026-09-11T19:07:31Z: NFL2026 schedule272games/current stats refreshed; NBA2025-26 logs26,651rows. Both sportsbook feeds observed; Kalshi NFL5sampled games/NBA0; PrizePicks unavailable. Overall degraded; both prop slates had0eligible events next24h, no live prop forecasts or profit evidence. Private report/log `.local/daily-pipeline-report.json` / `.local/daily-pipeline-proof.log`; actual DB snapshots published. Exact IPv4 session-pooler hostname still awaited.
- Daily batch verification:344Python tests passed,17disposable-DB skips; focused orchestration/budget7passed after partial-coverage guard;11frontend tests, TypeScript and production build passed. PR13(scan reliability) merged after all required CI/CodeQL checks passed at a8e85f44abe78590ceda462d80ef893c6451e016. Keep these current findings ahead of older historical counts below.
- Linux CI exposed a refresh timestamp race masked by Windows clock resolution: freshness must compare against time after loading the snapshot, since another worker can complete during the read. Keep the fresh-snapshot and future-timestamp regressions; never remove the future guard to fix this.
- Post-refresh evidence rerun: fresh NFL capture replayed all45comparisons exactly; NBA had0comparisons. Existing NFL2025-09-14 and NBA2026-01-28 holdouts reproduced identical forecasts/metrics (NFL16evaluated/Brier0.2373145728; NBA192/Brier0.0872092543). Reports `.local/history/*-after-daily-refresh.json`; ROI/CLV remain null. No model tuning or profitability proof. Production smoke now also checks scan-status and both market endpoints;503 remains explicitly allowed only while live pipeline is disabled.

- User requested removal/hiding of generated workspace clutter. Disposable root .test-tmp-* artifacts are removed where accessible, otherwise excluded from the local editor file tree (Windows denies delete/move/attribute/ACL access on15 old pytest directories); tooling directories retain their existing paths but use Windows Hidden attributes. Future test output/cache lives under ignored hidden .local/ via pytest defaults. Do not create new root test-output files or scatter temporary directories. Preserve .env, source, migrations, lockfiles and audit data.

- Python 3.12, uv 0.11.32, Node 24, Next.js 16.3.4, PostgreSQL 16. Python dependencies are locked in uv.lock; npm in frontend/package-lock.json.
- Graph nodes are deterministic Python, not LLM calls. No OpenAI/Anthropic key is consumed. Runtime requires langgraph and langgraph-checkpoint-sqlite.
- Windows: use .venv/Scripts/python.exe and npm.cmd. Pytest temp/cache paths default under .local/. Local tooling/cache files are ignored; do not create root .test-tmp directories.
- Read frontend/AGENTS.md and relevant installed Next documentation before frontend edits.
- Tests must opt into SPORTSBET_TEST_DATABASE_URL pointing to a disposable DB; never fall back to runtime credentials.
- Alembic respects explicit test URLs and escapes percent characters. Async DB factory preserves exact provider host/user/password/SSL mode; no guessed pooler or region rewrite.

## Deployment and credentials

- Vercel production became unavailable after a deployment/readiness burst even though the deploy initially passed. Repeated local restricted-reader probes reproduced PostgreSQL `53300` on both pooler ports: the Supabase project had exhausted client connections. Root cause in the frontend was a module-global `pg.Pool(max:2)` per independently bundled Vercel route; frozen serverless instances can retain those sessions beyond the request and multiply them across routes/instances. Hosted reads must use one `pg.Client` per query and close it in `finally`. Do not restore persistent per-function pools without a measured serverless connection budget or an external data proxy.
- Post-fix production stress returned200 for five rounds of every single-read route. `/api/scans` alone fell to503 because it opened NFL and NBA snapshot clients concurrently and the recovering Supabase project temporarily had only one free slot. Multi-key dashboard reads must use `snapshots(keys)`, which retrieves all requested payloads with one parameterized `ANY($1::text[])` query and one client lifecycle.

- User explicitly approved the exact credential transfers after auto-review rejection. On2026-09-11, configured GitHub repository DATABASE_URL/DATABASE_URL_ASYNC with sportsbet_worker pooler URLs; Vercel production DATABASE_URL with sportsbet_dashboard plus DATABASE_SSL_CA. No owner credential or Kalshi trading key transferred. Production CI34640134905 rerun succeeded; actual deployed signals/metrics/scans/both market endpoints200, NFL45realcomparisons. Earlier pending-upload statements below are superseded.
- First actual hosted daily run34640712138 successfully refreshed NFL and captured both sportsbook feeds/Kalshi, but NBA stats.nba.com timed out(ReadTimeout); NBA props correctly blocked. PrizePicks unavailable; whole run degraded/exit2. Alternative NBA cdn schedule/boxscore requests returned403 locally; do not bypass. Do not claim NBA refresh readiness or profitability. DATA_PIPELINE_ENABLED still off pending remaining hosted checks.
- Production smoke identified /api/games503(upstream blocked from Vercel) and /api/gamelogs503(reader intentionally cannot select raw stats). Branch fix/hosted-dashboard-data adds worker schedule snapshots with90-minute/current-ET-date freshness checks, and migration0012 exposes only public NBA/NFL game-log fields through a reader-only SQL view. Underlying stats/audits remain inaccessible to the dashboard role. NFL date/team context requires exactly one schedule match; ambiguous games excluded.
- Game-log UI now preserves NFL stats/unknown venue, surfaces provider/DB errors instead of empty success, cancels obsolete searches, and calculates historical over frequency over observed non-tied stats only. Missing values and ties are reported separately; zero thresholds valid. This is not a model hit-rate or betting backtest.

- Current pooler verification supersedes earlier hostname blockers: user supplied aws-0-us-west-2.pooler.supabase.com. Both existing restricted roles authenticated via5432 with full certificate/hostname verification; actual asyncpg worker and Node pg reader round-trip passed. Pooler URLs are prepared only in ignored `.local/secrets/hosted-pooler.json`; original owner/direct settings remain local. Do not log or commit either credential file.
- Pooler needs Supabase's public Root2021 CA, not default system trust. `certs/supabase-prod-ca-2021.crt` downloaded from the URL in Supabase's official dashboard source, SHA256700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7. Python URLs use verify-full plus its repository-relative sslrootcert; Vercel reader uses verify-full plus server-only DATABASE_SSL_CA PEM. pg URL SSL options otherwise overwrite explicit CA options, so databaseConfig removes those options after requiring verify-full. Never use rejectUnauthorized=false. pg_stat_ssl describes Supavisor's separate backend hop; verify client TLS via client metadata.
- Uploading the prepared worker URLs to GitHub repository secrets DATABASE_URL/DATABASE_URL_ASYNC and reader URL/CA to Vercel production was rejected by automatic approval review, which requests explicit approval for these exact credential transfers. No upload occurred. Do not retry or route around this rejection before user approval. DATA_PIPELINE_ENABLED remains disabled; run the hosted daily workflow and verify production endpoints after approved configuration. Local verification:12frontend tests, TypeScript, production build, actual psycopg/asyncpg/Node pooler checks passed.

- Local Supabase URL resolves only to IPv6. Supabase documents GitHub Actions/Vercel as IPv6-incompatible; need the exact Session pooler hostname, not a guessed region/index. Requested only that non-secret hostname from user. Authenticated Vercel CLI works with explicit --project/--scope; production currently has no environment variables. New db/access.py provisions separate read-only snapshot and ingestion roles, with RLS and limited grants; no owner credentials should be exported.

- User explicitly approved exporting the local ODDS_API_KEY to this repository. It was successfully stored as an encrypted GitHub Actions repository secret and its presence verified without displaying the value. Worker database secrets and reachable production database setup remain outstanding; scheduled scans stay disabled until readiness checks pass.

- Production URL: https://agentic-ev-analytics-platform.vercel.app.
- Vercel project agentic-ev-analytics-platform under rahulp7801s-projects; rootDirectory frontend. IDs are repository Actions variables; VERCEL_TOKEN is a repository secret.
- Project-scoped token works. Vercel CLI 59.15.1 pull fails during team metadata lookup (vercel/vercel#17506); direct deploy --yes --prod supports owner-lookup fallback. Do not request a broader token or export/reuse interactive OAuth credentials.
- CI deploy depends on backend, frontend and isolated PostgreSQL checks. Automatic Vercel git deployments disabled in frontend/vercel.json. Vercel performs the hosted build. .vercelignore limits uploads to frontend sources, excluding environment files, build outputs and legacy signal cache.
- First full deployment success: GitHub run 34538930708 (eb728ef). Runs34539302175 (bdadfa1) and34540128712 (87c55e1) also passed. Verified homepage 200, security headers, public scan POST 403, sanitized signals/metrics 503 while DB is unconfigured.
- User resumed Supabase. Connection and existing data returned during startup; initial empty-table read was transient. Existing migration revision0006 advanced successfully to0011, preserving50,833 NBA game logs and391 legacy signals. RLS enabled on public tables with no public policies (anonymous access denied). Never print secret values. Vercel production DATABASE_URL still needs a reachable dedicated read-only role.
- Worker needs repository secrets DATABASE_URL, DATABASE_URL_ASYNC, ODDS_API_KEY. ANALYTICS_DATABASE_URL uses writable DATABASE_URL in Actions. .env.example and README distinguish Python, Vercel and GitHub settings.
- GitHub secret scanning and push protection enabled. Dependabot security updates enabled. CodeQL extended default setup configured for Python, JS/TS and Actions. Latest completed analysis at87c55e1 passed with no open alerts; this is not proof of complete security.

## Data and orchestration

- Production settlement diagnosis on 2026-09-15 found all 708 NFL ledger rows for the completed Kansas City/Denver game had the correct date and kickoff but null `home_team`/`away_team` because `scan.evaluate_event` omitted them from the immutable audit payload. Do not relax exact final-game matching or infer the teams later. New provenance-required predictions must retain player, sport, canonical game date, and two distinct bounded team names; legacy incomplete rows are excluded from evaluation metrics and reported in the excluded count. Settlement diagnostics classify those old rows as invalid evidence rather than implying that an otherwise complete game merely failed to match.

- Public market freshness uses one shared 90-minute UI contract. Future, malformed, missing, and older captures cannot receive a current/live label. Historical observations remain visible with their capture time and must be described as last-published evidence, not live opportunity. The landing pulse refreshes once per minute so an open page cannot retain a green current label after evidence expires.

- September 2026 production trace: the dashboard's apparently invalid state combined distinct conditions. NBA has no current slate; 306 deduplicated model predictions were originally shown as pending before their missing team identity was diagnosed, and the 708 raw rows must now be excluded; scheduled paid collection depends on its separate gate; and public ESPN/Kalshi coverage can remain current independently of a model scan. Preserve these states separately in API/UI copy; zero, excluded, unavailable, stale, not-requested and pending are not interchangeable.
- Public daily collection successfully reached publication, then CI failed because shell `tee` mixed progress logs with the final JSON and `verify-production.mjs` parsed the whole file. `sportsbet.daily --report-output` now writes the report atomically as pure JSON. Settlement now canonicalizes both `YYYY-MM-DD` ledger dates and ESPN `YYYYMMDD` schedule dates, retains canonical evidence, and reports aggregate recheckable/out-of-window coverage so a zero-candidate run is diagnosable without exposing player records.
- The September 2026 product redesign replaces the black/acid terminal with a quiet neutral surface, system-like DM Sans hierarchy, violet accent, rounded evidence cards, and explicit provider states. The overview defaults to NFL and reads `/api/markets`, `/api/scans`, `/api/games`, and `/api/metrics` independently with an 8-second browser timeout; one unavailable source cannot erase valid data from the others. Verified locally through the restricted dashboard role: 231 linked Kalshi props, 230 two-sided markets, 240 stale model estimates, and 306 pending/0 settled evaluation records rendered as distinct states.
- Landing motion has two intentional runtimes: pinned/scrubbed long-form sequences use pinned GSAP ScrollTrigger; React component entry and overview transitions use Motion. The landing's reusable card/reveal language follows Magic UI's copy-in approach but is owned locally. Do not add Anime.js or React Spring without a unique requirement, and do not introduce Threlte into this React/Next.js application. Reduced-motion media and hook paths must stay functional.
- Public schedules now remain displayable for four hours when they are for the current Eastern date; GitHub scheduled delivery was observed with gaps approaching two hours, while the previous 90-minute UI gate hid otherwise valid same-day schedule evidence. This relaxed schedule display window does not relax price/recommendation freshness gates. Public daily collection requests Kalshi plus the public PrizePicks endpoint and records access denial as unavailable; public monitors remain Kalshi-only. Paid scheduling is split: `DATA_PIPELINE_ENABLED` permits the once-daily job, while `PAID_MONITOR_ENABLED` separately permits the 30-minute paid monitor to protect free API quota.

- Daily scanner reliability: source failures isolated per league/event; exact discovered ID/home/away/start must match returned quotes. Attempts persist before paid I/O, rotate least-recently attempted games, and alternate leagues by last attempt. Every scan publishes scan:nfl/scan:nba with bounded current-event attempt history, coverage/skip counts, failures, and completion timestamps. Graph errors propagate to event failures; no successful-empty status for a failed model. Dashboard scan status distinguishes missing, stale, interrupted, no-games, no-quotes and quota-limited runs. TopBar polls every30seconds and clears unavailable/stale recommendations via server gates.
- Actual restricted-worker readiness run at2026-09-11T18:55Z successfully published both league reports:0games in next24hours,0paid prop requests. This is verified empty coverage, not full-slate/season readiness. Supabase still uses the IPv6 direct host; exact Session pooler hostname remains needed for Vercel/GitHub. Local suite336passed/17disposable-DBskipped before final model-error regression; frontend11tests and TypeScript passed.
- Final focused daily-scan tests8passed, including actual graph SQL-failure propagation. Production frontend build passed. Local production Next server with restricted reader role returned actual no_games status for both leagues from /api/scans; existing mutation controls still passed. No live provider price or result was invented for this verification.

- NFL schedules now upsert corrections instead of ignoring existing games; provider failures stop refreshes. Weekly stat refresh writes rows and derives opponent/home-away from a unique team/season/week schedule in one transaction; missing or ambiguous matches clear old context instead of reusing it. CLI loads schedules before player stats. Regression coverage includes rescheduled dates, venue swaps, corrected yardage and ambiguous-match clearing in real PostgreSQL CI.

- Hosted dashboard reads dashboard_snapshots via server-only PostgreSQL. Python publishes snapshots; no local subprocesses/files are relied upon on Vercel.
- Public scan POST is always forbidden, including local mode: Host header checks were spoofable. Only authenticated CLI/Actions workers spend provider credits and write audit data.
- sportsbet.scan routes NBA/NFL through the actual LangGraph, passes exclusive as_of_date and last_n_games=40. GraphState must declare these fields or LangGraph silently drops them.
- Quotes match event/player/market/line/side exactly; evaluate alternate lines separately and support Under-only listings. Resolve exact distinct player identity; unknown/ambiguous names skip instead of borrowing history.
- Require provider quote timestamps and actual start times. Recheck quote age after model computation. No recommendations after start or quote age >5 minutes. Synthetic PrizePicks pricing is rejected. Missing prop listings do not imply injury.
- Stat upserts apply provider corrections and make repeat refreshes safe. Migration 0011 adds NFL player_name and analytics.api_usage. Exact NFL names are sourced from player_display_name.
- sportsbet.refresh --backfill loads NFL current/prior two season years, NBA current/prior. Daily refresh loads current season. NFL schedules are required for cutoffs. Root scan_game_ev.py remains a local NBA scanner, not the hosted worker.
- Market data Actions workflow supports scan/refresh/backfill. Scheduled refresh daily and scan every30min remain disabled unless DATA_PIPELINE_ENABLED=true. Do not enable until reachable DB, migrations, backfill, provider budget and live output are verified.
- Persistent API budget default25 credits/day is a ceiling, not complete slate coverage. It reserves before a request, does not refund failed calls, and stops additional events when exhausted. Choose coverage/budget deliberately before enabling.

## Model and pricing contracts

- Generic context h2h extraction now requires exactly one matching provider event and a named outcome (default matched home team), rather than using the first event/outcome. Missing identity/start/quote timestamp fails closed; provider observation time is preserved instead of stamping old odds as newly observed. GraphState retains explicit outcome_name. Callers with internal game IDs must map them to provider event IDs first.

- Dated NBA queries count pre-game logs, excluding target/future dates before taking recent games. Empty situational samples broaden to pre-game logs, never season totals. Actual double-double counts include steals/blocks.
- NFL cutoff matches each player's own team/week schedule, not the earliest game in the week. Latest-N queries apply situational filters before LIMIT.
- Unfitted home/rest/pace/defense/kinematic adjustments default off. Experimental changes discard incompatible CIs and do not adjust push markets. Neutral defense/pace are placeholders, not measured features.
- Shared prop policy requires >=20 games and exact matched odds. Legacy unconditional NFL executor has a30-game gate; conditional paths report intervals before shared policy gates. The 15pp edge cap is an operational review gate, not calibrated evidence.
- Generic NFL QuantResult measures play_success; arbitrage rejects it without explicit market_outcome scope. Do not treat pass/play success as game-win probability.
- American price determines break-even, payout, expected_return and fractional Kelly. ev_pct/ev_percentage retain legacy probability-edge semantics (percentage points). Dollar expected profit = stake * expected_return, never stake * edge.
- Pushes are separate: pUnder=1-pOver-pPush, expected return=win*payout-loss, Kelly conditional on nonpush. Complement Under CI only when push=0; do not invent push-market intervals.

## Audit, exposure and evaluation

- Ledger uses local .checkpoints/analytics.sqlite by default; ANALYTICS_DATABASE_URL switches default-path storage to PostgreSQL analytics schema (migration0010).
- Immutable per-scan prediction IDs, append-only quotes, explicit settlements. Every successfully evaluated selection is recorded, including rejected/nonpositive estimates. Missing model results are skipped.
- Atomic recommendation exposure default5% per UTC day; rescans idempotent, one exposure per player/game blocks conflicting/correlated selections. PostgreSQL advisory transaction lock preserves cross-process limits. This is recommended exposure, not executed bets or drawdown.
- Reports deduplicate selections, separate all-prediction and accepted-recommendation cohorts. CLV is raw same-book/same-line implied probability movement, requiring entry < last observed closing quote < actual start.
- ROI excludes pending/void stakes; pushes refund. Hit rate, Brier/log loss exclude pushes. Calibration uses model probabilities only. No settlement is inferred from absent records.
- CLI sportsbet.ledger --list exports IDs; --settlements accepts ID->true/false/push/void/null. Hosted metrics snapshots update on the next successful worker run.
- Frontend signalMetrics revalidates model version, probabilities, price, sample, stake, freshness and start time. Legacy/stale/synthetic estimates cannot recommend. No invented CI, strength rating or service health.
- Parlay calculator uses user-supplied gross payout, independent scenarios and dependence bounds; no fitted joint probability or Kelly recommendation. NBA/NFL filtering is wired to dashboard/EV alerts; Under is preserved.

## Verification and remaining gaps

- PR114 merged through protected `master` at `5540d7564a3043f56dbc05c47e30381e259c5e98`; PR CI/CodeQL and post-merge CI/CodeQL all passed. Production deploy is current. Manual public run35024972707 passed and retained evidence; manual credentialed daily run35025443174 passed with an honest degraded annotation. Current NFL capture at2026-09-15T21:25Z: sportsbook observed32(partial), Kalshi observed16games/250linked props/248two-sided quotes, PrizePicks unavailable/access_denied,104 comparisons, none execution-ready. NBA has no active Kalshi market slate; sportsbook discovery observed41 but the next-24-hour model scan found no eligible games.
- Production endpoints after the credentialed run returned200 for markets, scans, games, metrics, signals and prop screens. `/api/metrics` contains306 unique evaluation selections, all306 pending and0 settled, so ROI/hit rate/Brier/calibration remain null by design; CLV has221 same-line observations with mean -0.00013638. Settlement run rechecked708 NFL ledger candidates dated2026-09-14 with0 invalid/outside-schedule dates, but none matched a final schedule game, so it correctly retained them pending. Do not render or describe these null outcomes as zero performance.
- The landing hero uses live `/api/markets`, `/api/games`, and `/api/scans` evidence. The pinned product illustration must consume that same state; do not add hard-coded market/provider counts. True CDP mobile emulation at390x844 reported document/body scrollWidth390 and rendered without horizontal overflow. Plain `--window-size=390` Edge screenshots are misleading because the headless desktop layout keeps a wider minimum viewport and crops the bitmap.

- PR#7 merged at136e2cd after final CI34547550049 and CodeQL34547545563 passed ond486bc5. Follow-up fix/immutable-market-archives prevents default Kalshi captures from overwriting prior evidence: unique timestamp+UUID files and exclusive creation even for explicit destinations.

- New market_analysis route runs three deterministic specialists in parallel, then joins separately budgeted candidate results. Shared arbitrage/portfolio.py uses bounded SciPy MILP integer lots, explicit fee upper bounds/depth, complete supplied payoff states, trusted coverage-review references, age/skew/start gates, and Decimal postchecks. scenario_edge is NOT executable arbitrage; execution_ready always false. Missing fees/limits/rule review blocks; candidate profits must not be summed. CLI replay uses this actual graph and explicit settlements; no fills or realized ROI inferred. Live feed-to-reviewed-candidate mapping remains incomplete.
- Market-analysis batch local verification:311passed/16disposable-DBskipped/2legacyxfail. Includes all three actual nested LangGraph branches, budget/depth/whole-unit constraints, fees removing an apparent edge, cancellation loss, missing/stale/future evidence, duplicate liquidity, RSA signature verification, redirect isolation, demo separation, and explicit-settlement replay. Real NFL reader rerun archived .local/kalshi/observations-verified.json; raw pair cost is pre-fee algebra only, never a fill.
- PR#7 feature checks at72de96f passed backend/frontend/PostgreSQL; final required Python CodeQL pending at review. Added minimum lot step0.0001 during review to bound pathological integer counts before solver entry. Actual sampled NFL YES+NO asks cost1.01 and1.02 before fees: no gross same-contract hedge edge in these two observations.
- Repeated Windows mixed graph/direct MILP calls reproduced a native scheduling stall (.local/market-stress-before.log). Set HiGHS threads=1 while keeping graph specialists concurrent;60graph invocations/200mixed solves then completed. SciPy forwards this option; suppress only its exact forwarding notice. Subprocess regression tests20worker lifecycles with a30-second timeout. Native solver time_limit is not a hard process kill guarantee.
- Full local suite after concurrency fix:312passed/16disposable-DBskipped/2legacyxfail. Keep the new subprocess lifecycle regression; single successful graph calls did not expose the scheduling problem.

- PR#6 merged through protected master at93eb1c5 after CI34545561095 and CodeQL34545558656 passed on1cd6e35. Post-merge workflows34546166109/34546166202 succeeded. Kalshi work is isolated on feat/kalshi-market-analysis; never push master directly.
- Kalshi read-only adapter validated with actual production signed GET and two real NFL market snapshots. Configured key has read+write scopes; adapter exposes GET only, no redirects, no demo fallback. Private key lives in ignored .local/secrets/kalshi-production.pem. Demo credentials absent. Public order books work without authentication and use orderbook_fp string dollar prices/fractional counts.
- Research and architecture evidence is in docs/market-analysis.md. Official FOOTBALLGAMEWIN rules include half-dollar ties and fair-market-price cancellation scenarios; never assume sportsbook equivalence from team names. PrizePicks uses whole-entry payouts, including DNP/Reboot/tie rules, never synthetic -110. Existing LangGraph specialists are deterministic, not LLM calls.

- Real NFL backfill exposed unattributed provider rows with null player_id, aborting whole-season inserts. Ingestion now skips/counts these (18 per season in2023/24/25), and maps current passing_interceptions to interceptions. Loaded17,788/18,112/18,522 regular-season stat rows plus schedules2023-2026. Regression added to real PostgreSQL CI.
- Free ESPN history collector archives raw response text, source URL, retrieval time and hashes under ignored .local/history/. Pilot: NBA2026-01-28,9 games/609 stat records; NFL2025-09-07,13 games/345 records. No odds/line/quote time/DNP settlement is invented.
- Actual LangGraph walk-forward pilot exposed a production SQL bug: untyped$3 was inferred as the integer stat type, so half-lines were truncated and produced impossible pushes. NBA single/PRA and NFL queries now explicitly cast the line parameter to double precision. Real-PG regression tests include observed20 against line20.5(no push) and20(real push). Pre-fix pilot reports in.local/history/ are superseded; rerun corrected results before quoting metrics. Pilot cohorts: NBA points192/203 evaluated,8 unknown names,3 small samples; NFL passing17/27,10 small samples. Fixed research thresholds20.5/200.5 are NOT historical bookmaker lines. ROI/CLV null. Small dependent participant cohorts; no profitability claim or tuning against this tiny sample.
- Corrected pilots (.local/history/*-forecast-corrected.json): NBA Brier0.0872092543/logloss0.4349290210; NFL Brier0.3156050377/logloss0.8448473219. Zero nonzero push probabilities at half-lines in both. NBA cohort is dominated by low-scoring players, so comparison with50% alone is weak; NFL remains worse than50% on17 cases. Full local suite298passed/16DBskipped/2legacyxfail. PostgreSQL CI34545292450 passed; backend exposed SQLite fixture incompatibility with PostgreSQL cast syntax, fixed using portable ANSI CAST rather than weakening the production cast.
- Odds API historical probe returned401 HISTORICAL_UNAVAILABLE_ON_FREE_USAGE_PLAN,0credits,500remaining. Restored7 legacy odds rows all lack game_start_time/outcome_name and cannot support betting replay. Hardcoded five-bet CLI demo removed. Replay now requires explicit source, preserves push probabilities and model timing/version, emits hashes/scope and fails on no usable quotes.
- PR#6 introduces protected-branch workflow and production smoke checks. All master writes must go through PRs; production environment restricted to master. Latest focused checks27passed; full Python before ESPN additions295passed/16skipped/2xfail. Update final CI evidence after merge.

- Production NFL schedule smoke test returned an empty list despite a successful local provider response. Schedule route now formats YYYYMMDD explicitly and distinguishes complete upstream failure (503) from a valid empty schedule, with sanitized server diagnostics. At87c55e1, a cache-busted deployed request returned503/partial=true while the same Node handler locally returned2 NFL games. Provider access from Vercel remains unresolved; do not claim live schedule coverage.

- Latest local full Python suite:292 passed,16 DB skips,2 pre-existing expected failures. CI34541996912 atdd60ede passed backend/frontend/PostgreSQL/deployment, including NFL schedule corrections and ambiguous-context clearing; CodeQL34541996479 passed. Fresh-checkout pytest temp-parent setup was separately verified. Real PostgreSQL CI passed model SQL through both graphs, target-game exclusion, repeatable upserts, migrations and concurrent exposure reservation. Nine frontend metric/access/schedule tests, TypeScript and production build passed.
- npm audit zero vulnerabilities; pip-audit2.10.1 found no known Python vulnerabilities and is now a CI gate. Passing audits cannot prove all security gaps closed.
- Local Supabase access, migrations and initial backfill are verified. Still need dedicated hosted database roles, Vercel/worker secrets and measured quota/coverage before live readiness. Do not claim season readiness from deployment alone.
- Automatic settlement and out-of-sample calibration/profitability remain incomplete. Historical injury/roster conditioning is not fully point-in-time; current hosted baseline does not apply it. Legacy undated NBA aggregate estimates remain low-level paths.
- Generic custom market_outcome probabilities still need richer matching outcome metadata beyond the play_success gate. NFL game-log UI is still unavailable.
- Two expected-failure gamelog/injury tests import obsolete modules; replace with real-builder regression coverage rather than hiding failures. Ledger malformed/naive timestamp validation and reporting by model-version cohort still need hardening.
- Provider evidence correction: removed fabricated PrizePicks tier-to-American-price conversion from free_odds, context agent and legacy scanner. Retired predict.py's synthetic-price demo. PrizePicks now has a separate raw projection collector with immutable archives, no single-pick odds or assumed entry payouts; actual public endpoint returned HTTP403. Do not bypass access controls or claim live PrizePicks coverage.
- Shared parse_event_quotes requires provider event identity, aware event/quote timestamps, exact Over/Under, actual price and finite line. Context agent only persists requested exact event ID; league-wide/unmatched props and invented fresh timestamps are rejected. Fixture corrections plus regression cover preserved timestamps and unrelated games. Full local suite before final blocked-source test:317passed/17DBskipped/2legacyxfail.
- PR9 merged atfc514b8 after CI34559569609/CodeQL34559568809 passed. Restricted dashboard/worker roles were provisioned in Supabase and locally verified; generated credentials remain only in ignored .local/secrets/hosted-database.json. Local direct host resolves IPv6 only; awaiting exact session-pooler hostname for GitHub/Vercel IPv4 access. No production Vercel env vars and no GitHub database secrets configured yet; never export owner credentials.
- Market watch branch: live read-only NFL capture produced15 sportsbook,20 Kalshi-vs-book and10 same-contract comparisons; offline replay matched all45 exactly, with1 positive gross book gap before costs and no profit claim. Kalshi competition='Pro Football' silently returned0; actual NFL code returned31 milestones. Use league codes, validate returned league, capture structured targets plus ESPN same-league team directory, match exact home/away/start only. PrizePicks still403. No orders or inferred entry payouts.
- New /api/markets and Arbitrage view display source failures, partial coverage, capture age and unverified gross gaps. Missing hosted metrics now503 instead of fabricated zero counts. Market data workflow has manual watch operation and90-day public evidence artifacts; scheduled watch remains off. Local full suite323passed/17DBskipped/2legacyxfail; frontend9tests, TypeScript (incremental=false due local tsbuildinfo permissions), production build passed.
- Market publication verified against actual Supabase using sportsbet_worker, then read back identically using sportsbet_dashboard. Production-built Next server locally served all45 comparisons through /api/markets with reader credentials; invalid sport400, market POST405, hosted scan POST403. This proves the code/database path locally, not Vercel connectivity (pooler hostname still pending).
- Ledger integrity fixes: new audit timestamps require explicit timezone and normalize to UTC; conflicting retries under an existing prediction identity now fail atomically. Invalid/future-to-entry and synthetic PrizePicks evidence is excluded from metrics. Legacy offset timestamps sort by actual UTC time; model-version CLI filtering isolates cohorts. Full local suite330passed/17DBskipped/2legacyxfail before final quote-history guard; focused regression14passed. PR11 merged after CI34561308086/CodeQL34561306561 passed.
- New unchanged-model NFL holdout: ESPN2025-09-14 captured13games/348stat records, passing32candidates/16evaluated/16small-sample exclusions, Brier0.2373145728/logloss0.6686442102; all half-line push probabilities0. Combined with prior17-case pilot, Brier remains worse than0.25. ROI/CLV null. Dataset hashfcaae367afb85743365f761c6cb03113c69bb18921a75fef7efe268d44ba1308; evaluated source hash685500e06a972592a3072c09ac22f0f9c40fe7907a03d1838ddd209d1b0ec5d8. Archives/reports in.local/history/espn-nfl-20250914 and nfl-holdout-20250914.json. No model tuning against this holdout.
- PR11 production CI34561565045 passed deployment. GitHub security audit reports0open CodeQL/0Dependabot alerts; routine dependency upgrade PRs1-5 remain separate (not security alerts). Vercel DB connectivity and live worker schedules still blocked on exact IPv4 pooler hostname; PrizePicks source remains403. Do not label the project season-ready or profitable.
- Final audit batch local suite333passed/17disposable-DBskipped/no xfails. Replaced two obsolete gamelog helper-import xfails with actual NFL/NBA builder regressions for malicious-value parameter isolation, date cutoffs and sample bounds. This verifies SQL safety, not point-in-time historical injury correctness. Invalid legacy closing quotes now increment excluded_closing_quotes and leave CLV null without crashing the report.

- Backtest Lab keeps raw prediction rows server-side and exposes only validated aggregate replays. Workers publish all-prediction and accepted-recommendation snapshots separately for NBA and NFL (`metrics:{all|recommendations}:{sport}`); `/api/metrics` binds the requested cohort and sport before returning data.
- Strategy suggestions require the configured minimum decided sample plus at least two selected uncertainty-aware checks. Qualifying checks use game-cluster interval bounds for ROI, Brier, log loss and CLV; calibration requires its own sample and <=5% error. Hit rate is displayed but never treated as profitability evidence without price-dependent break-even.
- Current mispricing candidates are research leads only. They must be ungated, pregame, <=5 minutes old, positive on expected return and probability edge, have >=20 model observations, and carry a reported confidence interval. The UI returns an honest empty state when no signal qualifies and never substitutes demo values.
- The landing page embeds the real Backtest Lab component as an interactive demo. GSAP animates its entrance and Motion animates metric panels; reduced-motion behavior remains supported. Local production-build visual QA at 1440px and emulated 390x844 confirmed interactions and no horizontal overflow.
- Backtest Lab verification on `feat/backtesting-lab`: frontend 64 tests, ESLint, TypeScript and Next production build passed; full Python suite 629 passed/26 environment-dependent skips. Production still has no verified settled outcomes, so performance and strategy suggestions must remain unavailable until the ledger receives eligible settlements.
- 2026 NFL Week 1 retrospective holdout uses the actual v4 LangGraph, exclusive target-game cutoff and pre-existing fixed thresholds. ESPN archive:15 games/627 outcome rows, dataset hash `1b027a0540b5bb1be3c664a5e31a33ebd16ad5fbccfcab7e0b545b743ee086df`. Passing200.5:24/34 evaluated, Brier.252302/logloss.700006/calibration.179138, no checks passed. Receptions4.5:146/238 evaluated, Brier.162507 (cluster .119084-.205931), logloss.490493 (cluster .374478-.606507), calibration.092394; Brier+log-loss checks pass, calibration does not. This is a retrospective unpriced forecast benchmark, not a pre-kickoff timestamped prediction artifact or profitability evidence; ROI/CLV remain null.
- Public-surface security sweep: production npm dependencies and the locked Python environment have zero known audit findings. Odds API request methods now enforce their async-client lifecycle with an explicit runtime guard (assertions are not a security boundary), malformed prop lines use narrow Decimal parse exceptions, and missing Kalshi event-fee evidence is retained as a typed failure instead of disappearing. Focused regression suite: 91 passed.
- Public UI audit found and fixed stale-request races in Overview, Props, Backtest Lab and Scenario Lab. Overview metrics are now league-bound; the Backtest Lab preserves independently available benchmark evidence when replay snapshots fail. Terminal URLs retain league/view in a validated hash, the mobile header exposes the global league switch, and Game Logs no longer has a conflicting private league state.
- Local WCAG A/AA axe sweep covers landing plus all seven terminal views at 1440px and 390px. After accessible labels, focusable scroll regions and contrast tokens, all tested views report zero violations and zero horizontal overflow. The public pages use per-request CSP script nonces via Next proxy/dynamic rendering; HSTS, COOP, CORP, Permissions Policy and DNS-prefetch controls are enforced. CI now gates Bandit medium-confidence findings, and the production readiness probe validates the complete header policy.
- Final public-interface/security verification on `feat/public-interface-security`: frontend 69 tests, ESLint, TypeScript and production build passed; full Python suite 632 passed/26 environment-dependent skips; Bandit medium-confidence gate passed; npm and pip audits found zero known vulnerabilities. A tracked-file secret-pattern scan found no environment files, private keys, credential URLs or common provider-token formats. Raw axe reports and screenshots remain ignored under `.local/`.
- Production visual QA after PR123 found the mobile Props empty message clipped inside the intentionally wide data table. Follow-up renders an honest league-specific empty panel outside the table so narrow layouts stay readable; the table remains available only when recorded rows exist.
- The same local fallback exposed an internal script/cache instruction through `/api/signals`. Public API failures now use provider-neutral snapshot language and 503 availability semantics, including malformed local snapshot failures; no filesystem or worker command names reach the UI.
- Live post-deploy verification found `/api/signals?sport=nba` returning NFL game metadata because the route loaded every signal snapshot and relied on client filtering. The route now validates the league, parameterizes the matching snapshot-key prefix, and enforces the league again after public projection; ticker, overview, arbitrage, backtest, props and scenario consumers use league-scoped requests where applicable.
- The verified 2026 NFL Week 1 benchmark retains 170 row-level forecast records: 24 passing-yards and 146 receptions. At a transparent 50% probability classification cutoff, 14/24 passing and 109/146 reception calls match the final side (123/170, 72.4% overall). This is directional forecast accuracy at fixed research thresholds, not sportsbook hit rate or profitability; historical prices, ROI and CLV remain unavailable. Each public row is bounded to player/matchup, cutoff probability/sample, final stat, matching ESPN event URL and archived-response hash.
- Daily scanner previously discarded trade plans/injury context and exported only positive-edge signals. It now retains all measured estimates, explanatory bullets, exclusive forecast cutoff and current availability evidence in both snapshots and the ledger. Eligibility is separate from forecast publication; missing/stale source context, ambiguous current roster, subject injury/non-active roster status and unmodeled teammate injury risks block recommendations with zero stake.
- Availability collector uses real ESPN Site NFL/NBA injury feeds, exact same-league team directory and both team rosters. Directory lacks timestamp/status; injury/roster envelopes require provider timestamps within one hour. Actual NFL injury JSON is about 8.8MB; bounded limit is16MB. Raw source text/hashes are archived exclusively under ignored `.local/availability`. The feed is a reported injury list, not verified final game-day participation. v4 probability remains unchanged; injury/teammate usage boosts require independent point-in-time validation before activation. Live NFL collector verified successfully on Chiefs/Chargers; focused graph/source tests passed23.
- Player forecasts default to upcoming games, with archive/all filters and a per-player explanation panel. Panels show probability interval, price break-even, sample/mean/cutoff, game/quote times, current reported status and teammate reports, and the reason eligibility was blocked. Browser evidence ages are rechecked every30seconds. Scanner horizon is now48hours (previously24), retaining the existing quote/API budget gates.
- Actual pregame QA: Bills-Lions2026-09-18T00:15Z, one real Odds API market credit, actual Supabase v4 graph, four Goff/Allen passing estimates with35/33prior games and24reported teammates each. None passed model edge/uncertainty gates; no recommendations/orders. QA forecasts use an isolated ignored local ledger. Desktop1440/mobile390 populated explanation checks found no horizontal overflow; fixed the teammate-scroll keyboard-focus audit finding. Full Python suite640passed/26environment-dependent skips before the horizon regression addition; frontend73tests passed.
- Final populated forecast QA at1440/390:4real forecasts,24teammate report rows, explanations present, archive filter excludes all4future games, zero WCAG A/AA axe violations and no document overflow. Focused final source/graph/horizon suite24passed; public artifact tests8passed; ESLint, TypeScript, production build and pinned Bandit1.9.4 gate passed. Availability raw archives now enter the existing credential/gitleaks-scanned90-day GitHub evidence artifact, including scan-only operations; private directories remain excluded.
- PR127 initial PostgreSQL CI exposed an obsolete expectation that blocked scanner forecasts disappear (`signals == []`). Probability/target-game exclusion and committed quote assertions passed. Updated the actual-PostgreSQL test to require one retained forecast matching the audited probability/sample, three explanation bullets, unavailable-context label, gated eligibility and zero stake. Do not restore recommendation-only publication to satisfy the old assertion.
- PR127 security review flagged dynamic hostname-regex construction in roster-link validation. Replaced it with standard URL parsing, exact HTTPS origin, literal league/roster pathname matching, and rejection of credentials/query/fragment. Regression includes foreign/suffix hosts, credential URLs and extra query parameters; do not dismiss or bypass the aggregate CodeQL security check even when analysis jobs are green.
- PR127 merged at3bd2ba0 after final head6f0fba9 passed CI35136458510 and CodeQL35136454638, including the aggregate security check. Live NBA probe then exposed partial coverage: ESPN's fresh successful injury feed lists27teams and omits the Lakers. Do not infer a healthy lineup from an omitted team or discard the Celtics' verified report because the opponent is missing. Follow-up retains per-team verified evidence, labels omitted-team injury coverage unavailable, and supports the explicit OddsAPI `Los Angeles Clippers` -> ESPN `LA Clippers` directory alias without fuzzy matching.
- PR127 master deployment/CodeQL35136603192/35136602247 succeeded. Actual NFL scan35136605740 succeeded and the production `/api/signals?sport=nfl` returned112forecasts,0invalid records and112retained explanations at2026-09-16T18:48:39Z;104carry observed current availability and8explicitly unavailable context. All remain gated (98unmodeled teammate risk,6insufficient sample,8unavailable availability at the fresh API check); do not describe these112estimates as112recommendations. No orders were executed. NBA partial-coverage fix verified with25focused tests and actual Tatum injury evidence retained.
- Public daily trade-plan sizing must reflect approved eligibility, not the arbitrage agent's proposed Kelly fraction. A model can propose positive sizing before availability/portfolio gates block it. Scanner explanations now pair sample/mean/interval with the actual approved research fraction (zero when blocked); the original quantitative probability stays unchanged. Regression checks injury/teammate-gated forecasts cannot describe a nonzero approved stake.

### Player forecast UX (2026-09-16)
- Portraits come only from exact-name matches on the two verified ESPN rosters, with athlete-id/league-specific ESPN CDN URLs. Unknown portraits use initials; failed images fall back without changing forecast eligibility.
- The public availability boundary strips arbitrary image URLs. CSP permits only the ESPN image CDN in addition to existing local/data/blob images; browser image requests omit referrers.
- Forecasts default to responsive cards, retain the comparison table, support saved players (sport + player, browser-local only), eligible filtering, edge/sample/name sorting, reset and read-only refresh. Saves never place orders or bypass risk gates.
- Frontend lint/typecheck and 74 tests pass; live desktop/mobile interaction and worker source checks pending.
- Visual checks at 1440px/390px confirmed real Goff/Allen portraits load, four archived forecasts render, saving Allen selects both directions, reload retains the save, eligibility returns zero, table returns four, and past-game filter returns zero. Explanation/table axe WCAG checks report no violations; document width matches viewport. QA replays real captured forecasts locally, with fresh ESPN portraits; no fabricated prices or estimates.
- Cards now show the risk-gate reason directly, prop labels use full names, and player heading/date wrapping is corrected after inspecting screenshots. Final card accessibility/production verification pending.
- Final rebuilt local UX verified at both 1440px and 390px: portrait loading and forced-error initials, refresh, saved-player persistence, eligible filtering, card/table switching and archive filtering all pass. Card and explanation/table axe WCAG checks each return zero violations, with no document overflow. PR CI reports 643 Python tests passing (26 skipped) and 74 frontend tests; CodeQL and aggregate security check passed on e5802d2. Deployment will use the subsequent final documentation head after its own exact-head checks.
- PR #129 merged as 3f5646b after exact-head CI 35145327622 and CodeQL 35145324969 (including aggregate CodeQL) passed on 8a733fd. Master CI/deployment 35145524535 and security 35145524400 passed.
- Manual NFL scan 35145529204 returned an honest degraded report: one eligible event skipped by the configured odds budget. Workflow success is not evidence that quotes were refreshed; inspect the operation report.
- To populate existing player photos without bypassing budgets, downloaded original market-evidence-35143791138 into ignored .local/ux-original-evidence. A one-time metadata-only update matched roster URL + raw-response SHA256 + exact player name, validated league/athlete CDN URL, and used conditional SQL against the unchanged original payload. Added 106 portraits to 116 forecasts; preserved all prices, probabilities, source commitments, generated_at/quote/game timestamps, and gates. Backup and operation script stay ignored under .local.
- Production API confirms 116 forecasts, 106 source-verified portraits, zero invalid signals, all 116 gated, generated_at still 2026-09-16T19:59:58.507783+00:00. Photos do not imply fresh quotes or eligible recommendations.
- Actual production (no response interception) at 1440px/390px verified loaded portraits, forced-error initials, 116 forecasts after refresh, six Coleman props when saved, persistence on reload, zero eligible rows, 116 table rows and zero archived rows. Card and explanation/table axe checks each report zero violations; document widths equal viewport. Screenshots visually inspected under .local/visual-qa.
- Production review caught budget-skipped reports labeled as model failures, and a hard-coded 24-hour empty-window label after the worker moved to 48 hours. scanStatus now prioritizes validated budget deferrals after actual failures/invalid funnels, with explicit API-budget labels and a neutral scan-window label. Regression covers full/partial deferrals and failure precedence.

### Next checkpoint: pre-forecast player history (2026-09-16)
- Prediction explanations now load a bounded exact-player, same-league history from the restricted dashboard_gamelogs view. The API accepts a strictly validated before date and excludes target-day/later games in parameterized SQL, preserving existing substring searches for Game Logs.
- Recent 10/20-game windows compare recorded stats with the selected line and direction. Ties/missing stats are excluded from direction rate, missing PRA components stay unknown, and the recent window is explicitly descriptive rather than historical priced profitability or a reconstruction of the model's complete sample.
- Client cancellation and keyed evidence components prevent old-player request results from populating a newly selected forecast. Missing/invalid history remains unavailable, never a fake zero rate.
- Native Node 24 test imports use explicit .ts paths; allowImportingTsExtensions is enabled with noEmit for the Next bundler and test-compatible shared history validator.
- Tightened the displayed history cutoff to min(target-game cutoff, UTC recorded price-capture day), conservatively excluding the entire capture day. This prevents a revisited old forecast from incorporating games after its recorded quote. Missing/naive capture times fail closed. Calendar requests reject year zero, which JavaScript accepts but PostgreSQL dates do not.
- Final lint, typecheck, production build and 79 frontend tests passed. Restricted-reader Supabase requests verified 20 exact Goff rows, cutoff before 2025-09-01 yielding latest 2025-01-05, exact Jared returning zero instead of matching Jared Goff, and invalid calendar date returning 400.
- Production-mode local UI at 1440px/390px uses actual Supabase snapshots/history without fabricated responses. Goff rushing over 0.5 shows 7/20 directional matches (35%) and mean 2.2; Last 10 shows 3/10 (30%) and mean 2.4. These are descriptive at that line, not profitability. Retained zero/negative real yardage, valid capture-day cutoff, 20/10 row switching, and forced-network-failure unavailable state verified. Axe WCAG A/AA checks return zero violations, document widths match viewport; screenshots inspected in ignored .local/visual-qa.
- Date-only view refinement supersedes the prior capture-day rule: historyCutoff is now min(target cutoff, UTC date of quote capture minus 24 hours). The additional calendar-day gap avoids admitting overnight NBA/NFL games whose local game date precedes capture while completion does not. The UI states this conservative gap. It is a descriptive current stored-outcome view, not proof of historical feed publication/revision timing or exact model-sample reconstruction.

### Budget-aware quote collection and quieter forecast controls (2026-09-16)
- Paid prop collection cadence: 12 hours beyond six hours from kickoff, two hours from six to one hour, and 15 minutes in the last hour. Crossing either phase boundary triggers a check. Discovery remains free; invalid/future attempt timestamps cannot postpone collection. Five-minute quote eligibility is unchanged.
- Atomic daily AND rolling API reservations accept an unspent holdback. If the daily limit supports three full requests for the widest requested league, ordinary props retain two requests for the last hour; broad sportsbook collection retains three. Last-hour prop checks may spend retained credits. Smaller budgets preserve bounded collection without holdback. This allocation cannot assure every game receives a quote.
- Keep markets before cross-venue props: fresh Kalshi handoffs must precede comparison. Do not move the whole market stage after props to save sportsbook credits.
- Cadence-deferred scans report scheduled, cadence_deferred_events, and next_refresh_at separately from budget exhaustion/model failure. Deferred checks do not rewrite old prices, signal timestamps or eligibility.
- Full backend regression: 656 passed/26 environment-dependent skips; additional final near-kickoff test and all 11 scan reliability tests passed. Collector/cadence-focused run: 37 passed.
- Forecast controls now prioritize player search and saved players, with secondary controls in native expandable Filters. League-specific prop choices avoid inapplicable filters after a league switch. Cards/table retain actual values; gated edges use neutral colors and observed quote age/expiry is explicit.
- Read-only forecast polling runs every 30 seconds while visible, bounded by a 15-second timeout. Failed refreshes preserve already loaded evidence; publicSignals continues applying quote expiry. Polling cannot initiate worker requests or orders.
- Final frontend validation: 81 tests, lint, typecheck and production build passed. Actual Supabase browser QA at 1440/390 pixels verified 116 forecasts, collapsed/expanded filters, 116 table rows, expired quote labels, zero eligibility, failure preservation/recovery, zero axe violations and no document overflow. Screenshots inspected under ignored .local/visual-qa.
- Pipeline exposes next_refresh_at only for a scheduled state with a timezone-aware future timestamp inside 48 hours. Budget exhaustion and actual failures take precedence; next check is not a promise of source coverage.
- Existing paid monitoring runs on GitHub at minutes 7/37, subject to scheduler delay. Cadence is the earliest permitted request time, not an additional scheduler; the pipeline says Due rather than promising collection at that minute. The 15-minute last-hour minimum does not claim continuous coverage between the 30-minute scheduled runs.
- Empty filtered results now offer Show all forecasts, clearing secondary filters and including recorded archives. Real-data UI checks also verify saving Coleman narrows 116 forecasts to six, then returns to all forecasts. Wait for the existing 200ms card entrance transition to finish before measuring contrast; steady collapsed/expanded views pass axe at both sizes.
- PR #132 merged aa8f5c9663f9c821430fbd2970bb2b2870b976f7 after final exact-head CI/aggregate CodeQL passed: 657 backend and 81 frontend tests. Read-only production monitor 35159240049 confirms NFL scheduled/cadence-deferred, original 116 forecast timestamps preserved, due 2026-09-17T07:59:45Z. Sportsbooks budget-exhausted, Kalshi 16 observed markets, PrizePicks access_denied; pipeline remains degraded despite workflow success.
- Post-deploy readiness 35159189744 caught /api/markets NFL returning 503. Root cause: publicMarkets omitted the real worker-produced budget_exhausted source status and rejected the entire otherwise valid mixed-source snapshot. Follow-up accepts that explicit status only with zero source records and partial coverage, preserving independent Kalshi evidence; no weakening of readiness checks or fake sportsbook quotes.
- Follow-up local verification: actual secret-scanned worker archive projects successfully (16 Kalshi records, 32 unverified comparisons); 82 frontend tests, lint/typecheck/build and the unchanged production readiness script all pass against the restricted-reader production-mode build. Desktop/mobile actual-data market view shows budget/access limits, zero API alerts, 32 comparisons, zero axe violations/no document overflow; screenshots inspected.
- Visual refinement keeps source limits and unverified warnings visible, moves verbose coverage/fee assumptions into native expandable details, and raises market body text to 12px. Collapsed and expanded detail views pass real-data desktop/mobile axe/overflow checks. Aborted market requests cannot update the next league's state.

### Prerelease and next checkpoint: sportsbook quota efficiency (2026-09-17)
- GitHub prerelease v0.1.0-alpha.1 is pinned to verified deployed f55e037c40e6e7b54cac389b35a290e0cc947a6c, explicitly prerelease/not latest. Release notes distinguish descriptive backtesting from priced returns and retain quota/access/settlement limits. No binaries, environment files or private QA artifacts are attached.
- Scheduled daily/monitor broad sportsbook captures now use a six-hour interval; Kalshi collection and kickoff-aware player props retain their existing behavior and freshness guards. Explicit manual watch retains its requested capture behavior inside the shared credit limits.
- Per-league collection:sportsbook markers store only actual reserved I/O attempts. Budget denials never create markers; provider failures are spaced out conservatively; missing, naive, invalid or future marker timestamps cannot defer collection. Cadence assumes the existing serialized market-data workflow; the atomic ledger remains the credit-limit guard for all callers.
- Deferred broad captures report not_requested/collection_cadence with zero sportsbook events and partial coverage. They do not reuse older prices or quote timestamps as new observations. Public projection accepts this wait only for the sportsbook source with zero count and partial coverage, preserving independently collected Kalshi evidence.
- Local focused cadence run: 17 passed; existing real daily-graph/collector integrations: 42 passed. Frontend 83 tests, lint/typecheck and production build passed. A final additional half-hourly 24-hour simulation checks eight broad credits across both leagues and leaves 17 of the unchanged 25-credit daily ceiling available.
- Visual checks at 1440/390px pass for actual restricted Supabase data and a separate explicitly local archived-Kalshi deferred-source scenario: 32 real captured comparisons, no API alerts, zero axe violations or document overflow, expandable detail views work. No replay interception occurs on production URLs and no quote prices or database rows are fabricated for QA.
- PR #138 merged at ed422b90ba09412dfde1985f15be93ea46f3f29f after exact-head CI and aggregate CodeQL succeeded. Full regression: 675 backend tests passed/26 skipped and 83 frontend tests passed. Protected-master CI/deploy 35261985505 and CodeQL 35261984774 passed; unchanged production readiness and actual production desktop/mobile collapsed/expanded market views passed after deployment.
- Read-only merged-master NFL monitor 35262358309 passed schema, collection, evidence-secret scanning and retention. The 2026-09-17T19:03:33Z publication contains 456 observed quotes, 248 selections, 240 model requests and 240 estimates, eight unresolved history selections, zero missing estimates and zero invalid forecasts. Workflow success retains an explicit degraded-history warning; it does not establish complete coverage.
- All 240 forecasts remain gated: 210 teammate_availability_unmodeled, 20 availability_unavailable and ten insufficient_sample. Next model checkpoint must address captured availability/teammate context and exact player-history coverage without bypassing these gates or retrospectively using uncaptured information. PrizePicks access and settlement/execution equivalence remain separate limitations; no orders were executed.

### Availability evidence checkpoint (2026-09-17)
- Replay of the secret-scanned archive from monitor 35262358309 establishes that BOTH Detroit and Buffalo were present in the league injury feed. The roster responses contain additional explicit injury risks omitted from that feed: eight Detroit and three Buffalo players. Do not describe those 20 public availability gates as proof that Buffalo was omitted; public signal validation can replace an underlying roster/model gate with its current availability reason.
- Collection now supplements league injury entries with fresh exact-team roster entries. A missing/denied league report can use a roster fallback only when every athlete explicitly supplies an injuries array. Missing/null arrays cannot establish a healthy or empty fallback; provider timestamps, exact identities, dates, duplicates and conflicting statuses remain guarded. Existing roster/source SHA commitments and archived raw responses retain both inputs.
- Roster-only public injury sources must equal the already validated same-league roster URL AND its source hash. The explanation links to the actual roster injury source. Existing projections still exclude internal data and reject arbitrary hosts, credentials, query strings, other teams/leagues and hash mismatches.
- No injury probability adjustment or model-version change is introduced. The legacy NFL position/stat-absence and NBA missing-gamelog teammate filters do NOT prove actual participation or roster membership at a historical game. Do not wire those proxies into scheduled picks as validated injury effects; reliable point-in-time participation/roster coverage and holdout evaluation are needed first.
- Initial focused graph/availability checks: 27 passed; frontend: 84 passed. Actual archived-response replay adds 11 previously omitted risks and retains Josh Allen's teammate-risk gate with probability_adjusted=false. QA replay stays in ignored .local and does not mutate production snapshots, odds or outcomes.
- Expanded final ingestion checks: 19 passed. Exact-head CI at d96dddd reports 689 backend tests passed/26 skipped and 84 frontend tests passed; PostgreSQL, secret scanning, worker image and all analyses/aggregate CodeQL passed. The separate local roster-only failure scenario derives availability solely from retained real roster response bytes, preserves actual forecast prices/probabilities/quote times, and verifies both 1440px and 390px explanation views with seven reported risks, the exact roster source, zero stake, zero API alerts, zero axe violations and no document overflow. HTTPS production URLs prohibit replay interception.
- PR140 merged at 73d4d8c after the final exact-head bcd082a passed all checks. Protected-master CI/deploy 35295076628 and CodeQL 35295076284 passed.

### NFL cross-provider roster identity (2026-09-17)
- The 20 historical forecasts missing availability all belong to James Cook. Quotes/stat history use James Cook; ESPN's captured roster names athlete 4379399 James Cook III. No fuzzy matching or suffix-stripping alias may make that player eligible.
- The [documented nflverse player dataset](https://nflreadr.nflverse.com/reference/load_players.html) supplies explicit GSIS/ESPN IDs. Its actual downloaded CSV maps 00-0037248 to 4379399; full source SHA 769f144ca86a0c3c2d7a59ad02fe370a206926ec71e16c9c1dc7f09dd1715c87 is retained in ignored QA evidence. Never guess IDs: the adjacent 00-0037247 is not Cook's ID.
- NFL scans request this free public crosswalk only when quoted names are not covered by captured rosters. Metadata redirects require HTTPS, approved GitHub asset hosts, no userinfo and the standard port; payload/row limits, strict columns/IDs and one-to-one GSIS/ESPN uniqueness guard identity. Full source bytes are archived; runtime context/public records exclude the raw CSV and mapping table.
- Eligibility uses the already resolved historical GSIS ID to find the exact captured ESPN roster athlete, then screens that athlete's canonical injury name and portraits. Missing, conflicting or failed ID evidence cannot borrow another player's roster/history. Public alias explanations require the canonical source URL/hash and stay bounded; archived evidence is described as captured, not a current lineup.
- Read-only live provider probe at 2026-09-18T01:29Z matched James Cook to James Cook III and athlete 4379399, captured 11 teammate risks and retained teammate_availability_unmodeled/probability_adjusted=false. No paid odds request, order, production snapshot mutation, retroactive prediction rewrite or model probability adjustment occurred. Historical rows remain as originally recorded; this fix applies on subsequent real scans.
- This post-kickoff probe verifies current identity/availability collection, not a reconstructed pregame forecast or profitable historical pick. Focused collector/graph/reliability checks: 50 passed; final expanded identity and real scheduled-graph checks: 24 passed. Frontend: 85 tests passed, lint/typecheck passed; existing sample, uncertainty, quote-freshness, injury and portfolio gates remain enforced for ID-resolved players.

### Usable production player workspace (2026-09-17)
- Production has 240 valid retained NFL forecasts, but the upcoming-only default hid all of them after kickoff. Overview used the latest scan's zero estimates instead of the retained forecast library. Default player browsing now chooses upcoming records when present, otherwise archived records, with explicit expired-price disclosure; no eligibility or model gate is relaxed.
- Overview reads the actual forecast endpoint and presents recorded forecast/player counts and player cards. Individual player cards open a filtered research view. Player browsing exposes the game-window selector, headshot shelf, saved players, search, card/table views and incremental card loading.
- `sportsbet.player_profiles` independently refreshes current source-backed presentation profiles for recent published slates using the existing exact ESPN roster collector and verified GSIS/ESPN identity crosswalk. Paid/public workflow updates publish `player-profiles:{sport}` even when no upcoming prop games exist. Provider failure cannot erase a previous profile capture. This metadata is separate from immutable probabilities, prices, quote times, historical availability and recommendations.
- Public profiles require the exact league/athlete ESPN portrait URL, roster URL/hash, bounded team/name/jersey/position, aware capture timestamp within 14 days, and crosswalk commitment for aliases. Profile fields cannot establish historical participation or make a forecast eligible. Read both snapshot kinds in one bounded database query to avoid extra serverless connection latency.
- Real free-source probe captured all 14 published players with verified portraits, teams, positions and jerseys, including Cook III/4379399/#4. Initial desktop/mobile QA used unchanged actual production forecasts plus those separately captured presentation profiles: Cook's 20 pre-forecast game rows, risk gate, sources, loaded portraits, zero axe issues/API alerts and no document overflow passed. Removed the constrained nested vertical forecast scroller and made the mobile player strip compact. Header reports recorded forecasts separately from eligible picks. Opening a named player uses all recorded windows so a new unrelated upcoming slate cannot hide that player's historical evidence.
- Exact-head initial PR142 CI: 705 backend tests passed/26 skipped, 87 frontend tests passed; PostgreSQL, image, secrets and all CodeQL/aggregate checks passed. Final UX polish requires a new exact-head run before merge.
- Final production-data/current-profile local desktop/mobile rerun passed with Cook's 20 history rows and unchanged blocked status. Overview's hero is compact and mobile summary cards retain two columns so real data and player identities appear sooner than the old oversized heading/single-column stack. Each card includes game time and historical-record labeling; named-player views retain the expired-price notice even in the all-records window. Focused collector/profile tests: 22 passed.

### Schedule read recovery (2026-09-17)
- PR142 merged at 9b1c429 after final exact-head 70b0141 CI and all CodeQL checks passed. Hosted public monitor 35309232034 actually published 14 NFL player profiles with zero unavailable identities; NBA has no recorded recent profiles. This workflow is free/public-only and does not execute orders or rewrite forecasts.
- `PUBLIC_DATA_PIPELINE_ENABLED` is true, but best-effort scheduled executions still left a stale schedule on the logged-in overview. Hosted `/api/games` now prefers validated stored schedules and recovers unavailable/stale snapshots from the same hardcoded ESPN public scoreboard feed. Three dates run concurrently, each cached for 60 seconds with a five-second timeout, redirects forbidden, two-megabyte/100-event limits and the existing strict schedule projection. Missing/malformed dates remain partial; no successful source means 503, never a fabricated healthy empty slate.
- Recovery is display-only: it does not bypass worker quote budget, create prices, modify prediction cutoffs, change settlement, or grant model eligibility. Overview's timeout covers one database timeout followed by bounded feed recovery.
- Protected-master player-workspace CI/deploy 35309229310 and CodeQL 35309229073 passed. Actual production API now enriches all 240 retained forecasts with the 14 independently captured profiles. No-interception production 1440px/390px checks passed: Cook III's source-backed #4 profile/headshot, 20 real pre-forecast history rows, historical risk gate, loaded portraits, zero axe violations/API alerts and no document overflow. Unchanged paid/public production readiness/security checks passed.
- Actual isolated-hosted-read recovery reached the live ESPN sources: NFL returned one observed scheduled game, NBA returned zero, both HTTP200/partial=false. This is provider-backed seasonal coverage, not synthetic data or a recovered historical price. Initial PR143 exact-head 1b93da0 CI: 705 backend tests passed/26 skipped, 90 frontend tests passed, PostgreSQL/image/secrets and all CodeQL/aggregate checks passed. Require fresh exact-head checks after this retained-validation commit before merging.
- Expanded no-interception production table checks caught legacy red/purple badges with insufficient contrast on the new light theme. Correct their shared colors and show the same source-backed team/jersey/position in table rows. Card/table and game-window/recovery controls must pass expanded desktop/mobile checks before final deployment. The user now explicitly requires quality over quantity: future pick selection must prioritize existing conservative uncertainty/risk gates, not raw high point-estimate edge or a desired pick count. Existing graph already requires a positive lower-bound margin and sizes Kelly from that lower bound.

### Quality over quantity (2026-09-17)
- PR143 merged at f5cf35e after final exact-head b4c5dab passed CI and all CodeQL/aggregate checks. Protected-master CI/deploy 35309933416 and CodeQL 35309933405 passed. Expanded local 1440px/390px production-data checks passed card/table toggles, real 20-row table, upcoming-empty recovery to all 240 forecasts, 24-card incremental loading, Cook navigation/profile/history, source links, zero axe issues/API alerts and no document overflow. Shared light-theme badge contrast and table player metadata are fixed.
- Scheduled quant already requires the reported 95% lower probability bound to exceed push-adjusted break-even and sizes fractional Kelly conservatively from that bound. Previously, alphabetical/line iteration could reserve a correlated player's exposure before a more strongly supported alternative. Within each observed event, rank graph-qualified candidates by lower-bound margin then sample size before applying the unchanged freshness, availability, correlation and daily exposure gates. Retain every measured forecast; no historical probability or acceptance is rewritten.
- Dashboard's research shortlist contains at most three distinct player/game choices among currently valid accepted records. Require supported uncertainty, positive lower-bound margin, positive expected return and stake, model/price/roster freshness, and the existing 15pp model-review ceiling. Do not fill a target pick count; an empty qualified shortlist is correct when evidence fails. Rank the library by supported margin by default and label the alternative raw point-estimate sort explicitly.
- Ranking is evidence-based model ordering, not proof of profitability, a calibrated quality score, or a guarantee of future performance. Limits and source coverage still apply. Browser float tolerance at the 15pp ceiling is only 1e-12 to preserve exact Decimal boundary values; above-policy edges still fail.
- Focused scheduled-graph/scan checks: 26 passed. The new real LangGraph-plus-ledger test proves a lower raw-probability estimate with a stronger lower-bound margin receives the correlated exposure slot; both forecasts remain recorded.
- New immutable scanner payloads retain `recommendation_policy_version=lower-bound-margin-v1` independently of the unchanged model version so future strategy audits can distinguish the allocation policy. Never retrospectively tag old rows. Inconsistent probability/interval/non-push support fails selection; supported library ordering and shortlist validation share the same conservative margin helper. Latest frontend regression: 94 passed; focused final real-graph checks: 15 passed before the policy-marker assertion.

### Daily forecast response limits (2026-09-17)
- PR144 merged through protected checks at b7be6ba; master CI/deploy 35310583571 and all CodeQL checks passed. Full regression: 706 backend passed/26 skipped, 94 frontend passed. Production desktop/mobile no-interception player/profile/evidence checks passed; 240 historical forecasts remain valid and zero fresh picks qualify.
- Public full home/away team names incorrectly used a 20-character abbreviation bound. Washington Commanders, Minnesota Timberwolves, Oklahoma City Thunder and Golden State Warriors exceed that bound; snapshot game validation could reject an entire slate. Match the existing backend's bounded 100-character full names while retaining abbreviation bounds and control-character rejection.
- Actual current /api/signals NFL response is 1,081,526 bytes for 240 forecasts. Growing combined snapshots can also exceed the existing 5,000-record validation bound. Bound individual responses and make research-window coverage explicit; independently inspect fresh accepted candidates before claiming a strongest shortlist. No missing historical odds or current source gaps can be padded with demo predictions.
- `/api/signals` now flattens the existing latest-100-game archive window in PostgreSQL and transfers at most 100 raw rows per library page. Public projection limits signal bytes to 1MiB, advances the cursor over invalid rows, retains full teammate/source evidence and detects snapshot revisions between pages (409). Strict bound checks and parameterized queries apply to offset, limit, sport and view. Archive growth beyond 5,000 records cannot invalidate an individual page.
- Shared frontend loader reads at most the latest 1,000 library rows and explicitly reports partial research-window coverage in header/overview/player browsing. Search/filter/counts describe that loaded window, not every historical prediction. Statistics and immutable replay tables still come from independent metric/benchmark snapshots. No zero padding or invented totals.
- `view=qualified` independently inspects accepted candidates from recently updated game snapshots (six-minute window covers five-minute quote validity plus allowed one-minute clock skew). Public validation rechecks actual quote time, game/roster/model/stake/uncertainty and source identity before conservative ranking. Inspect at most 5,000 candidates; if that inspection is incomplete, expose zero picks and an incomplete-coverage state, never claim the strongest from a truncated archive page. Sidebar tools that need current candidates use this independent qualified view.
- Disposable PostgreSQL CI executes the actual frontend query with 6,500 retained fixture records, 6,000 recent accepted candidates, non-overlapping pages and revision-change detection. These controlled fixtures are never published as live forecasts. User requested fewer meaningful commits and the next prerelease after final verification; retain prerelease status.
- Qualified snapshot selection filters recent updates before its bounded 100-game window and counts that window independently. More than 100 recently updated game snapshots also fails closed with incomplete coverage, preventing a strongest-pick claim from an incomplete game set. Cache revision hashes use native PostgreSQL/Node SHA-256; they are change detectors, not source authenticity proofs.
- Existing restricted-reader live Supabase verification returned 240/240 forecasts, 14 player identities and profiles on every record, zero invalid rows and zero qualified expired picks. The first 100-record response was 474,955 bytes; NBA has no published prop records and accurately returns zero. No-interception local desktop/mobile checks passed cards, 20-row Cook table, headshots/team/#4, actual pre-forecast history, source/risk evidence, zero axe violations/API alerts and no overflow. Frontend regression: 102 passed, lint/typecheck/build passed. Disposable PostgreSQL test intentionally skips locally without its explicit disposable DB URL and runs in CI's PostgreSQL job.

### Sunday readiness and fixed previous-week walkthrough (2026-09-18)
- Prerelease v0.1.0-alpha.2 is pinned to deployed 9675a4a. User now authorizes the next Sunday-readiness checkpoint and a hardcoded previous-week landing walkthrough. Prefer a small number of meaningful commits; continue protected PR checks, no assistant coauthors, no orders and no invented live prices/results.
- `/api/slate` displays the next seven Eastern calendar days using the same fixed bounded ESPN scoreboard feeds, separately from the unchanged 48-hour paid quote horizon. NFL feeds may return a whole week: filter actual event timestamps to the requested Eastern day before projection, preserving failures as partial coverage. Existing `/api/games` retains its three-date contract.
- Worker scan reports now retain per-event exact team/start identities and waiting, cadence, API-budget, evaluating, evaluated and failed states. Public slate joins ESPN and sportsbook identities only through exact teams AND kickoff; ambiguous/missing/stale matches cannot claim evaluation. Project existing selection/model counts and allowlisted risk reasons, keep acceptance at capture distinct from the current qualified shortlist.
- Existing daily/public_daily pipeline settlement reports expose bounded latest-check counts and reasons, separately from all-forecast/accepted metric cohorts. Final stats must pass the unchanged game/player/chronology/provenance checks. Monitor placeholders, impossible counts and missing evidence never become verified results.
- Landing demo is static genuine 2026 Week 1 retrospective holdout records: Stafford 155 passing yards versus fixed research threshold 200.5 (baseline miss), Purdy 205 (correct), Rodgers 221 (correct). Retained pre-target replay probabilities/samples and final-stat source hashes come from the production benchmark snapshot. This is retrospective research, not recorded pregame recommendations or historical sportsbook lines. Missing prices/uncertainty/injury evidence blocks a demo pick; no ROI/CLV/profit/stake is fabricated and demo records never enter production metrics. Four native selectable stages/player selector reuse Motion; no new dependencies.
- Local frontend regression: 107 passed, lint/typecheck/build passed. Real restricted-reader slate returned 16 upcoming NFL fixtures with complete provider coverage and genuine pending settlement reasons. Desktop 1440/mobile 390 demo-stage/player-selector checks, controlled local API-outage replay, slate/source links and axe/overflow checks passed. Windows Application Control currently blocks NumPy's bit_generator DLL before backend test collection, including a normal-runtime retry; do not change OS security policy. Require the actual Linux CI backend and PostgreSQL checks before merging.
- PR146 merged at e54bc34 after exact-head CI passed 706 backend/27 skipped, 107 frontend, seven migrations, 17 PostgreSQL integration tests and all CodeQL/aggregate checks. Production exposed two differences: Vercel's live ESPN requests cannot currently produce a usable slate, and replacing BacktestLab on the landing page removes its benchmarkPanel CSS from the landing bundle. Keep readiness tied to the actual new demo CSS. Publish a separate worker-captured `slate:sport` with next-seven-day fixtures, prefer its validated fresh snapshot in the public route, retain live ESPN only as recovery, and preserve `/api/games` and settlement catchup contracts. Future schedule failures stay partial; source-backed worker fixtures are neither invented predictions nor a guarantee of picks.
- PR147 merged at e84ad72 after 708 backend/27 skipped, 108 frontend, migrations/PostgreSQL and all CodeQL checks passed. Protected-master deployment and the public monitor passed. Real production desktop/mobile demo, 730 recorded forecasts/50 players, 16 upcoming NFL fixtures, expanded settlement reasons and endpoint/security readiness passed; zero current qualified picks. First-install slate snapshots were populated through the existing schedule collector/publisher, then the normal public worker refreshed them successfully. Keep QA browser runs serial and close owned test tabs: abandoned polling tabs generated avoidable database load.
- User rejected the sequential landing walkthrough and prominent unavailable-price tile. Lead with a single immediately visible replay forecast and known final result, default to Purdy's correct baseline, retain Stafford's miss in the selector, place methodology/absence of historical betting prices inside optional evidence details, and make Try the demo the primary hero CTA. Never relabel this retrospective research threshold as an accepted pregame sportsbook bet or fabricate odds/profit.
- Direct-card revision: 108 frontend tests, lint/typecheck/production build passed. Native hero CTA checks at 1440/390 confirmed the direction AND verified final-stat number are visible without further clicks; actual headshot loads, Stafford's missed replay remains selectable, evidence disclosure works, and the card survives a controlled local API failure. Mobile reduced-motion, axe and no-overflow checks passed.
- Keep the demo frame's GSAP scroll reveal opacity-only: translating/scaling its ancestor during a native hash jump moved the demo header beneath the fixed navigation. Verify the header stays visible after the actual hero CTA click, not just after a manual scroll.

- Hero scroll regression reproduced on production at 1440 and 390: start scrolling during the Motion entrance (before its 0.9s completion), then return to top. GSAP cached the same heroCopy element's initial opacity zero and restored it on every return; waiting 2.5s before scrolling masked the bug. Give Motion sole ownership of the copy entrance and restrict GSAP hero scrubbing to the decorative orb. Retain the product/story and lower-section scroll effects. Always test early scrolling as well as settled animations.
- Landing replay explanations must state actual model evidence, not only methodology. Retained public nflverse prior-game history from the 2024 season floor independently reproduces all three Week 1 probabilities with Jeffreys smoothing: Purdy 17/24 (70.0%), Stafford 24/33 (72.0588%), Rodgers 21/33 (63.2353%). Purdy's newest five yards are 127,303,295,295,168 (3/5 above 200.5); surface the weaker recent pattern rather than cherry-picking. Freeze the public history with this fixed demo and test it against the retained benchmark; keep the target final stat out of history. Median and recent rates describe evidence, not new model inputs. The replay did not apply opponent/injury/teammate adjustments and does not prove betting value.
- Scroll/evidence revision verification: 109 frontend tests, lint, TypeScript and production build passed. Dedicated Edge QA at 1440/390 reproduced the old early-scroll failure and confirmed three return-to-top cycles remain fully visible after the fix; native demo CTA, real headshot, default Purdy history table, selectable Stafford miss, controlled local API outage, axe and no-overflow checks passed. All QA artifacts remain hidden/ignored in `.local/`. Require Linux backend/PostgreSQL and all security analyses on the PR before merging.
